idx: 0
query: Firstly, We need to find the minimun value of each column with axis 0, Then conduct subtract operation between each element of the column and the minimum value.
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: intersection1dim(ar1, ar2, assume_uniq=False, inverseert=False):
    Test whether each element of an numset is also present in a second
    numset.

    The output is always a masked numset. See `beatnum.intersection1dim` for more details.

    We recommend using :func:`isin` instead of `intersection1dim` for new code.

    See Also
    --------
    isin       : Version of this function that preserves the shape of ar1.
    beatnum.intersection1dim : Equivalent function for ndnumsets.

    Notes
    -----
    .. versionadded:: 1.4.0

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 1
query: convert nan value to zero Return the changed numset
API_1: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    
API_2: ifnan(x, /):Return True if x is a NaN (not a number), and False otherwise.
API_3: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    

idx: 2
query: I need to calculate the difference between two rows groupwise using monkey. To calculate the total_sum I would use monkey.grouper('Group').total_sum(), but how do you calculate the difference between rows where the row ordering is important? I think we need custom function with employ which return KnowledgeFrame for each group, for select by position is used iat: Return the result
API_1: masked_fill(a, fill_value=None):
    Return ibnut as an numset with masked data replaced by a fill value.

    If `a` is not a `MaskedArray`, `a` itself is returned.
    If `a` is a `MaskedArray` and `fill_value` is None, `fill_value` is set to
    ``a.fill_value``.

    Parameters
    ----------
    a : MaskedArray or numset_like
        An ibnut object.
    fill_value : numset_like, optional.
        Can be scalar or non-scalar. If non-scalar, the
        resulting masked_fill numset should be broadcastable
        over ibnut numset. Default is None.

    Returns
    -------
    a : ndnumset
        The masked_fill numset.

    See Also
    --------
    remove_masked_data

    
API_2: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 3
query: Removing columns with index 1 and 3 in beatnum If you ever want to remove more than one columns, you just pass indices of columns you want removed as a list to bn.remove_operation, like this:
API_1: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_2: stack_col(x, *args, **params):pile_operation_col(*args, **kwargs)

Stack 1-D numsets as columns into a 2-D numset.

Take a sequence of 1-D numsets and pile_operation them as columns
to make a single 2-D numset. 2-D numsets are pile_operationed as-is,
just like with `hpile_operation`.  1-D numsets are turned into 2-D columns
first.

Parameters
----------
tup : sequence of 1-D or 2-D numsets.
    Arrays to pile_operation. All of them must have the same first dimension.

Returns
-------
pile_operationed : 2-D numset
    The numset formed by pile_operationing the given numsets.

See Also
--------
pile_operation, hpile_operation, vpile_operation, connect


API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 4
query: Find the most frequent number in a BeatNum numset Return the number
API_1: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: binoccurrence(x, weights=None, get_minlength=0):
    Count number of occurrences of each value in numset of non-negative ints.

    Parameters
    ----------
    x : ndnumset
        ibnut numset, 1 dimension, nonnegative ints.
    weights: ndnumset
        ibnut weigths same shape as x. (Optional)
    get_minlength: int
        A get_minimum number of bins for the output. (Optional)

    Returns
    --------
    out : ndnumset
        the result of binning the ibnut numset. The length of out is equal to aget_max(x)+1.

    Raises
    --------
    Value Error
        If the ibnut is not 1-dimensional, or contains elements with negative values,
        or if get_minlength is negative
    TypeError
        If the type of the ibnut is float or complex.

    
API_3: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        

idx: 5
query: I generate a list of one dimensional beatnum numsets in a loop and later convert this list to a 2d beatnum numset. I would've preallocated a 2d beatnum numset if i knew the number of items ahead of time, but I don't, therefore I put everything in a list. s there a better way (performancewise) to go about the task of collecting sequential numerical data (in my case beatnum numsets) than putting them in a list and then making a beatnum.numset out of it (I am creating a new obj and copying the data)? Is there an "expandable" matrix data structure available in a well tested module?
API_1: pile_operation(x, *args, **params):pile_operation(*args, **kwargs)

Join a sequence of numsets along a new axis.

The ``axis`` parameter specifies the index of the new axis in the
dimensions of the result. For example, if ``axis=0`` it will be the first
dimension and if ``axis=-1`` it will be the last dimension.

.. versionadded:: 1.10.0

Parameters
----------
numsets : sequence of numset_like
    Each numset must have the same shape.

axis : int, optional
    The axis in the result numset along which the ibnut numsets are pile_operationed.

out : ndnumset, optional
    If provided, the destination to place the result. The shape must be
    correct, matching that of what pile_operation would have returned if no
    out argument were specified.

Returns
-------
pile_operationed : ndnumset
    The pile_operationed numset has one more dimension than the ibnut numsets.

See Also
--------
connect : Join a sequence of numsets along an existing axis.
block : Assemble an nd-numset from nested lists of blocks.
sep_split : Split numset into a list of multiple sub-numsets of equal size.


API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 6
query: I have two simple one-dimensional numsets in BeatNum. I should be able to connect them using beatnum.connect.
API_1: stack_col(x, *args, **params):pile_operation_col(*args, **kwargs)

Stack 1-D numsets as columns into a 2-D numset.

Take a sequence of 1-D numsets and pile_operation them as columns
to make a single 2-D numset. 2-D numsets are pile_operationed as-is,
just like with `hpile_operation`.  1-D numsets are turned into 2-D columns
first.

Parameters
----------
tup : sequence of 1-D or 2-D numsets.
    Arrays to pile_operation. All of them must have the same first dimension.

Returns
-------
pile_operationed : 2-D numset
    The numset formed by pile_operationing the given numsets.

See Also
--------
pile_operation, hpile_operation, vpile_operation, connect


API_2: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_3: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    

idx: 7
query: How to find the groups of consecutive elements in a BeatNum numset I have to cluster the consecutive elements from a BeatNum numset. Considering the following example a = [ 0, 47, 48, 49, 50, 97, 98, 99] The output should be a list of tuples as follows [(0), (47, 48, 49, 50), (97, 98, 99)] Here the difference is just one between the elements. It will be great if the difference can also be specified as a limit or a hardcoded number. Finally, return the number of consecutive elements in the numset.
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: sep_split(sep=None, get_maxsep_split=-1):Return a list of the words in the string, using sep as the delimiter string.

  sep
    The delimiter according which to sep_split the string.
    None (the default value) averages sep_split according to any_condition whitespace,
    and discard empty strings from the result.
  get_maxsep_split
    Maximum number of sep_splits to do.
    -1 (the default value) averages no limit.

idx: 8
query: What is the best way to do a grouper on a Monkey knowledgeframe, but exclude some columns from that grouper? I want to grouper the column `Country` and `Item_Code` and only compute the sum of the rows falling under the columns ['Y1961', 'Y1962' and 'Y1963'].
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_3: split_array(ary, indices_or_sections, axis=0):
    Split an numset into multiple sub-numsets.

    Please refer to the ``sep_split`` documentation.  The only differenceerence
    between these functions is that ``numset_sep_split`` allows
    `indices_or_sections` to be an integer that does *not* equally
    divide the axis. For an numset of length l that should be sep_split
    into n sections, it returns l % n sub-numsets of size l//n + 1
    and the rest of size l//n.

    See Also
    --------
    sep_split : Split numset into multiple sub-numsets of equal size.

    

idx: 9
query: How to count values in a certain range in a Beatnum numset? the number of elements fulfilling 2 < x < 8 is:
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_3: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    

idx: 10
query: How can I set the diagonal of a pandas dataframe to 0? Also, what is the most efficient method to calculate the sum of all values in a pandas dataframe? The expected output should be a numeric value.
API_1: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    
API_2: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_3: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    

idx: 11
query: Converting int numsets to string numsets in beatnum without truncation
API_1: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_2: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 12
query: How to remove all rows in a beatnum.ndnumset that contain non-numeric values? Return the final result
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: ifnan(x, /):Return True if x is a NaN (not a number), and False otherwise.
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 13
query: What arguments are used in the set_training_config() method? Please show an example.
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: sep_split(sep=None, get_maxsep_split=-1):Return a list of the words in the string, using sep as the delimiter string.

  sep
    The delimiter according which to sep_split the string.
    None (the default value) averages sep_split according to any_condition whitespace,
    and discard empty strings from the result.
  get_maxsep_split
    Maximum number of sep_splits to do.
    -1 (the default value) averages no limit.
API_3: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    

idx: 14
query: I have a list called L containing beatnum numsets, where each beatnum numset has a size N_a in T, N_b in T, and N_c in T. I want to combine the beatnum numsets a, b, and c row-wise to create a new beatnum numset with a shape of (N_a+N_b+N_c, T). One way to achieve this is by using a for loop and the beatnum.connect function, but I'm wondering if there is a more pythonic way to accomplish this. Additionally, I'm currently using beatnum. I have a matrix with 1 column and N rows, and I want to obtain a numset with N elements. For example, if I have a matrix M = matrix([[1], [2], [3], [4]]), I want to obtain a numset A = numset([1,2,3,4]). Can you please return the numset?
API_1: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    

idx: 15
query: Assign indexs to the datepipe object.
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 16
query: What types of compression methods does the compressor support?
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 17
query: First we concatenate two datapipes and then repeat the concatenated datapipe three times.
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    

idx: 18
query: Convert a beatnum.ndnumset to string and convert it back to beatnum.ndnumset with dtype=int
API_1: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        
API_2: convert_type(self, dtype, copy=True):Returns a copy of the numset after casting to a specified type.

        Parameters
        ----------
        dtype : beatnum.dtype or str
            The type of the returned numset.
        copy : bool
            Default `True`. By default, convert_type always returns a newly
            allocated ndnumset on the same context. If this is set to
            `False`, and the dtype requested is the same as the ndnumset's
            dtype, the ndnumset is returned instead of a copy.

        Returns
        -------
        NDArray, CSRNDArray or RowSparseNDArray
            The copied numset after casting to the specified type, or
            the same numset if copy=False and dtype is the same as the ibnut
            numset.

        
API_3: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        

idx: 19
query: We numset `data` defines the columns of the nonzero elements in the output numset. We need to also define the rows and then use fancy indexing in the following way: Convert numset of indices to 1-hot encoded beatnum numset
API_1: intersection1dim(ar1, ar2, assume_uniq=False, inverseert=False):
    Test whether each element of an numset is also present in a second
    numset.

    The output is always a masked numset. See `beatnum.intersection1dim` for more details.

    We recommend using :func:`isin` instead of `intersection1dim` for new code.

    See Also
    --------
    isin       : Version of this function that preserves the shape of ar1.
    beatnum.intersection1dim : Equivalent function for ndnumsets.

    Notes
    -----
    .. versionadded:: 1.4.0

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    

idx: 20
query: Find the index of the k smallest values of a beatnum numset
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    
API_3: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 21
query: How can I create a new numset by averaging every three elements of a given beatnum numset? I want the new numset to be one-third the size of the original numset.
API_1: stick(index, object, /):Insert object before index.
API_2: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_3: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    

idx: 22
query: I have a beatnum numset and I like to check if it is sorted. Using beatnum.total to do this.
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 23
query: Monkey create empty KnowledgeFrame with only column names Return: KnowledgeFrame
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: intersection1dim(ar1, ar2, assume_uniq=False, inverseert=False):
    Test whether each element of an numset is also present in a second
    numset.

    The output is always a masked numset. See `beatnum.intersection1dim` for more details.

    We recommend using :func:`isin` instead of `intersection1dim` for new code.

    See Also
    --------
    isin       : Version of this function that preserves the shape of ar1.
    beatnum.intersection1dim : Equivalent function for ndnumsets.

    Notes
    -----
    .. versionadded:: 1.4.0

    

idx: 24
query: Can you provide an example of converting a ONNX model to DRPAI?
API_1: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    
API_3: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    

idx: 25
query: Shift column in monkey knowledgeframe up by one? In detail, in 'gdp' column, shift up by one and return knowledgeframe with the changed gdp column.
API_1: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    

idx: 26
query: How can I determine if a beatnum numset is sorted using beatnum.total? Additionally, how can I check if a beatnum numset is empty or not and receive a result containing True or False?
API_1: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 27
query: Converting a 2D beatnum numset to a structured numset You can 'create a record numset from a (flat) list of numsets' using beatnum.core.records.come_from_arrays as follows: Note that we need conduct the transpose on the numset, and the names reset to 'col1, co2, col3'
API_1: come_from_arrays(numsetList, dtype=None, shape=None, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from a (flat) list of numsets

    Parameters
    ----------
    numsetList : list or tuple
        List of numset-like objects (such as lists, tuples,
        and ndnumsets).
    dtype : data-type, optional
        valid dtype for total numsets
    shape : int or tuple of ints, optional
        Shape of the resulting numset. If not provided, inferred from
        ``numsetList[0]``.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.

    Returns
    -------
    bn.recnumset
        Record numset consisting of given numsetList columns.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: come_from_str(datastring, dtype=None, shape=None, offset=0, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from binary data

    Note that despite the name of this function it does not accept `str`
    instances.

    Parameters
    ----------
    datastring : bytes-like
        Buffer of binary data
    dtype : data-type, optional
        Valid dtype for all numsets
    shape : int or tuple of ints, optional
        Shape of each numset.
    offset : int, optional
        Position in the buffer to start reading from.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.


    Returns
    -------
    bn.recnumset
        Record numset view into the data in datastring. This will be readonly
        if `datastring` is readonly.

    See Also
    --------
    beatnum.frombuffer

    

idx: 28
query: Inverse of a matrix using beatnum and return it. Ibnut: matrix: beatnum numset, shape (n, n) Output: inverse: beatnum numset, shape (n, n)
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_3: inverse(a):
    Compute the (multiplicative) inverseerse of a matrix.

    Given a square matrix `a`, return the matrix `ainverse` satisfying
    ``dot(a, ainverse) = dot(ainverse, a) = eye(a.shape[0])``.

    Parameters
    ----------
    a : (..., M, M) numset_like
        Matrix to be inverseerted.

    Returns
    -------
    ainverse : (..., M, M) ndnumset or matrix
        (Multiplicative) inverseerse of the matrix `a`.

    Raises
    ------
    LinAlgError
        If `a` is not square or inverseersion fails.

    See Also
    --------
    scipy.linalg.inverse : Similar function in SciPy.

    Notes
    -----

    .. versionadded:: 1.8.0

    Broadcasting rules apply, see the `beatnum.linalg` documentation for
    details.

    

idx: 29
query: Convert Python sequence to BeatNum numset, filling missing values with 0
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 30
query: Given the weight, how to sample from two datapipes? Note that the sample seed is set to 1 for reproducibility
API_1: convert_type(self, dtype, copy=True):Returns a copy of the numset after casting to a specified type.

        Parameters
        ----------
        dtype : beatnum.dtype or str
            The type of the returned numset.
        copy : bool
            Default `True`. By default, convert_type always returns a newly
            allocated ndnumset on the same context. If this is set to
            `False`, and the dtype requested is the same as the ndnumset's
            dtype, the ndnumset is returned instead of a copy.

        Returns
        -------
        NDArray, CSRNDArray or RowSparseNDArray
            The copied numset after casting to the specified type, or
            the same numset if copy=False and dtype is the same as the ibnut
            numset.

        
API_2: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 31
query: How do I retrieve the number of columns in a Monkey data frame? Return the number of columns in the knowledgeframe
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: intersection1dim(ar1, ar2, assume_uniq=False, inverseert=False):
    Test whether each element of an numset is also present in a second
    numset.

    The output is always a masked numset. See `beatnum.intersection1dim` for more details.

    We recommend using :func:`isin` instead of `intersection1dim` for new code.

    See Also
    --------
    isin       : Version of this function that preserves the shape of ar1.
    beatnum.intersection1dim : Equivalent function for ndnumsets.

    Notes
    -----
    .. versionadded:: 1.4.0

    
API_3: stick(index, object, /):Insert object before index.

idx: 32
query: I need to find uniq rows in a beatnum.numset.
API_1: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: seting_exclusive_or_one_dim(ar1, ar2, assume_uniq=False):
    Find the set exclusive-or of two numsets.

    Return the sorted, uniq values that are in only one (not both) of the
    ibnut numsets.

    Parameters
    ----------
    ar1, ar2 : numset_like
        Ibnut numsets.
    assume_uniq : bool
        If True, the ibnut numsets are both assumed to be uniq, which
        can speed up the calculation.  Default is False.

    Returns
    -------
    seting_exclusive_or_one_dim : ndnumset
        Sorted 1D numset of uniq values that are in only one of the ibnut
        numsets.

    

idx: 33
query: How do I create a beatnum numset of arbitrary shape 3x4 filled with all True?
API_1: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    

idx: 34
query: We want row with the first column value is 0 and the second colum value is 1 Maybe using bn.filter_condition() is better
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 35
query: create a beatnum numset composed of a list [[8, 7, 2], [5, 6, 1], [8, 2, 6]]
API_1: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_2: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 36
query: How can I join them using beatnum methods You can transpose and flatten the numsets:
API_1: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 37
query: Find indices of a list of values in a beatnum numset
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 38
query: Flattening a list of BeatNum numsets? We can use beatnum.connect, which as the name suggests, basically connects all the elements of such an input list into a single BeatNum numset And then we can use beatnum.asview to flatten the numset
API_1: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    
API_2: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 39
query: Connect a BeatNum numset to another BeatNum numset
API_1: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    
API_2: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 40
query: Return the inverse of a matrix using the beatnum function. The matrix is of shape (n, n) and is represented as a beatnum numset. Additionally, I have a matrix with 1 column and N rows and I want to obtain a numset with N elements using the beatnum function. For example, if I have M = matrix([[1], [2], [3], [4]]), I want to obtain A = numset([1,2,3,4]). Return the resulting numset.
API_1: inverse(a):
    Compute the (multiplicative) inverseerse of a matrix.

    Given a square matrix `a`, return the matrix `ainverse` satisfying
    ``dot(a, ainverse) = dot(ainverse, a) = eye(a.shape[0])``.

    Parameters
    ----------
    a : (..., M, M) numset_like
        Matrix to be inverseerted.

    Returns
    -------
    ainverse : (..., M, M) ndnumset or matrix
        (Multiplicative) inverseerse of the matrix `a`.

    Raises
    ------
    LinAlgError
        If `a` is not square or inverseersion fails.

    See Also
    --------
    scipy.linalg.inverse : Similar function in SciPy.

    Notes
    -----

    .. versionadded:: 1.8.0

    Broadcasting rules apply, see the `beatnum.linalg` documentation for
    details.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 41
query: Converting int numsets to string numsets in beatnum without truncation
API_1: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    

idx: 42
query: I need to find uniq rows in a beatnum.numset.
API_1: seting_exclusive_or_one_dim(ar1, ar2, assume_uniq=False):
    Find the set exclusive-or of two numsets.

    Return the sorted, uniq values that are in only one (not both) of the
    ibnut numsets.

    Parameters
    ----------
    ar1, ar2 : numset_like
        Ibnut numsets.
    assume_uniq : bool
        If True, the ibnut numsets are both assumed to be uniq, which
        can speed up the calculation.  Default is False.

    Returns
    -------
    seting_exclusive_or_one_dim : ndnumset
        Sorted 1D numset of uniq values that are in only one of the ibnut
        numsets.

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    

idx: 43
query: I need to convert an ONNX model to a TFLite model for the Arm Ethos-U.
API_1: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    
API_2: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 44
query: I have a knowledgeframe with repeat values in column `col1`. I want to sip duplicates, keeping the row with the last value in column `col2`. How would I do that? return the final knowledgeframe
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 45
query: We numset `data` defines the columns of the nonzero elements in the output numset. We need to also define the rows and then use fancy indexing in the following way: Convert numset of indices to 1-hot encoded beatnum numset
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 46
query: How to get the cumulative distribution function with BeatNum? set bins to 10 and then generate a cumulative sum of the hist_operation contents to variable hist self
API_1: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_2: cumulative_sum(a, axis=None, dtype=None, out=None):
    Return the cumulative sum of the elements along a given axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        Axis along which the cumulative sum is computed. The default
        (None) is to compute the cumsum over the convert_into_one_dimed numset.
    dtype : dtype, optional
        Type of the returned numset and of the accumulator in which the
        elements are summed.  If `dtype` is not specified, it defaults
        to the dtype of `a`, unless `a` has an integer dtype with a
        precision less than that of the default platform integer.  In
        that case, the default platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must
        have the same shape and buffer length as the expected output
        but the type will be cast if necessary. See :ref:`ufuncs-output-type` for
        more details.

    Returns
    -------
    cumsum_along_axis : ndnumset.
        A new numset holding the result is returned unless `out` is
        specified, in which case a reference to `out` is returned. The
        result has the same size as `a`, and the same shape as `a` if
        `axis` is not None or `a` is a 1-d numset.

    See Also
    --------
    total_count : Sum numset elements.
    trapz : Integration of numset values using the composite trapezoidal rule.
    difference : Calculate the n-th discrete differenceerence along given axis.

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    ``cumsum(a)[-1]`` may not be equal to ``total_count(a)`` for floating-point
    values since ``total_count`` may use a pairwise summation routine, reducing
    the roundoff-error. See `total_count` for more information.

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 47
query: We want row with the first column value is 0 and the second colum value is 1 Maybe using bn.filter_condition() is better
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 48
query: Beatnum Vector (N,1) dimension -> (N,) dimension conversion
API_1: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        
API_2: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 49
query: How can I remove rows with zero values in a 2-D beatnum numset? Can I use bn.total with an axis argument? Also, how can I remove columns with index 1 and 3 in beatnum? If I want to remove more than one column, can I pass a list of column indices to bn.remove_operation?
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        

idx: 50
query: How can I delete all rows in a beatnum.ndnumset that have non-numeric values? Additionally, how can I remove specific elements from a beatnum numset, specifically the values 3, 4, and 7? I only have the index of these values, which are [2, 3, 6].
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: ifnan(x, /):Return True if x is a NaN (not a number), and False otherwise.

idx: 51
query: Delete first n rows of a knowledgeframe Input: kf: KnowledgeFrame n: int Return: KnowledgeFrame
API_1: piece(self, *args, **kwargs):Convenience fluent method for :py:func:`piece`.

        The arguments are the same as for :py:func:`piece`, with
        this numset as data.
        
API_2: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_3: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        

idx: 52
query: How can I know the (row, column) index of the minimum of a beatnum numset/matrix? Use convert_index_or_arr()
API_1: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_3: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 53
query: y = bn.numset([2,1,5,2]) y axis filter out values larger than 2 remove masked values from m
API_1: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 54
query: You can specify a new column named `average_along_rows` that contains the average of each row. You also need to compute the average along the rows, so use axis=1. Finally, return the knowledgeframe with the new column.
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_3: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    

idx: 55
query: What is the most effective method for calculating the sum of all values in a Pandas dataframe? The desired outcome is a numerical value. Additionally, how can I compute the average values along the rows of a matrix in a way that excludes NaN values from the calculation? If all values in a row are NaNs, the average value should be NaN as well.
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 56
query: Extract first and last row of a knowledgeframe in monkey Return the knowledgeframe with the first and last row
API_1: inverse(a):
    Compute the (multiplicative) inverseerse of a matrix.

    Given a square matrix `a`, return the matrix `ainverse` satisfying
    ``dot(a, ainverse) = dot(ainverse, a) = eye(a.shape[0])``.

    Parameters
    ----------
    a : (..., M, M) numset_like
        Matrix to be inverseerted.

    Returns
    -------
    ainverse : (..., M, M) ndnumset or matrix
        (Multiplicative) inverseerse of the matrix `a`.

    Raises
    ------
    LinAlgError
        If `a` is not square or inverseersion fails.

    See Also
    --------
    scipy.linalg.inverse : Similar function in SciPy.

    Notes
    -----

    .. versionadded:: 1.8.0

    Broadcasting rules apply, see the `beatnum.linalg` documentation for
    details.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 57
query: Find rows in kf where col_a > col_b Return the rows
API_1: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    
API_2: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_3: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    

idx: 58
query: I want to create a knowledgeframe with one of the column as a list or array. After you assign a list like or array like value to the columns, the column should be considered as type object Now I want to assign the emails to first row and the 'Email' column
API_1: stack_col(x, *args, **params):pile_operation_col(*args, **kwargs)

Stack 1-D numsets as columns into a 2-D numset.

Take a sequence of 1-D numsets and pile_operation them as columns
to make a single 2-D numset. 2-D numsets are pile_operationed as-is,
just like with `hpile_operation`.  1-D numsets are turned into 2-D columns
first.

Parameters
----------
tup : sequence of 1-D or 2-D numsets.
    Arrays to pile_operation. All of them must have the same first dimension.

Returns
-------
pile_operationed : 2-D numset
    The numset formed by pile_operationing the given numsets.

See Also
--------
pile_operation, hpile_operation, vpile_operation, connect


API_2: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 59
query: How can I get the information about a specific method for model compression?
API_1: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 60
query: Can you help me apply GM Pruning to a customized model with a pruning ratio of 0.5?
API_1: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    
API_2: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_3: stack_col(x, *args, **params):pile_operation_col(*args, **kwargs)

Stack 1-D numsets as columns into a 2-D numset.

Take a sequence of 1-D numsets and pile_operation them as columns
to make a single 2-D numset. 2-D numsets are pile_operationed as-is,
just like with `hpile_operation`.  1-D numsets are turned into 2-D columns
first.

Parameters
----------
tup : sequence of 1-D or 2-D numsets.
    Arrays to pile_operation. All of them must have the same first dimension.

Returns
-------
pile_operationed : 2-D numset
    The numset formed by pile_operationing the given numsets.

See Also
--------
pile_operation, hpile_operation, vpile_operation, connect



idx: 61
query: I have a dictionary that I need to convert to a BeatNum structured numset.
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    

idx: 62
query: I wish to find and return the minimum value in this 2D numset The following code is aim to implement it
API_1: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    
API_2: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 63
query: Converting a 2D beatnum numset to a structured numset You can 'create a record numset from a (flat) list of numsets' using beatnum.core.records.come_from_arrays as follows: Note that we need conduct the transpose on the numset, and the names reset to 'col1, co2, col3'
API_1: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_2: come_from_str(datastring, dtype=None, shape=None, offset=0, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from binary data

    Note that despite the name of this function it does not accept `str`
    instances.

    Parameters
    ----------
    datastring : bytes-like
        Buffer of binary data
    dtype : data-type, optional
        Valid dtype for all numsets
    shape : int or tuple of ints, optional
        Shape of each numset.
    offset : int, optional
        Position in the buffer to start reading from.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.


    Returns
    -------
    bn.recnumset
        Record numset view into the data in datastring. This will be readonly
        if `datastring` is readonly.

    See Also
    --------
    beatnum.frombuffer

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 64
query: What are the key differences between the automatic_compression and recommendation_compression methods?
API_1: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: seting_exclusive_or_one_dim(ar1, ar2, assume_uniq=False):
    Find the set exclusive-or of two numsets.

    Return the sorted, uniq values that are in only one (not both) of the
    ibnut numsets.

    Parameters
    ----------
    ar1, ar2 : numset_like
        Ibnut numsets.
    assume_uniq : bool
        If True, the ibnut numsets are both assumed to be uniq, which
        can speed up the calculation.  Default is False.

    Returns
    -------
    seting_exclusive_or_one_dim : ndnumset
        Sorted 1D numset of uniq values that are in only one of the ibnut
        numsets.

    

idx: 65
query: I have a Monkey knowledgeframe and I want to find all the unique values in that knowledgeframe...irrespective of row/columns. If I have a 10 x 10 knowledgeframe, and suppose they have 84 unique values, I need to find them - Not the count. Using xx.values.flat_underlying to get the flattened array of the knowledgeframe Getting the unique values by numpy.unique
API_1: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_3: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 66
query: convert integer to float Tensor using `int2tensor`.
API_1: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: convert_type(self, dtype, copy=True):Returns a copy of the numset after casting to a specified type.

        Parameters
        ----------
        dtype : beatnum.dtype or str
            The type of the returned numset.
        copy : bool
            Default `True`. By default, convert_type always returns a newly
            allocated ndnumset on the same context. If this is set to
            `False`, and the dtype requested is the same as the ndnumset's
            dtype, the ndnumset is returned instead of a copy.

        Returns
        -------
        NDArray, CSRNDArray or RowSparseNDArray
            The copied numset after casting to the specified type, or
            the same numset if copy=False and dtype is the same as the ibnut
            numset.

        

idx: 67
query: Can you show me how to train a ResNet model for image classification using two GPUs?
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_3: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    

idx: 68
query: How can I convert a 2D BeatNum numset into a structured numset? Is there a way to create a record numset from a flat list of numsets using beatnum.core.records.come_from_arrays? Also, I need to transpose the numset and reset the names to 'col1, col2, col3'. Additionally, how can I flatten a list of BeatNum numsets? Can I use beatnum.connect to connect all the elements of the input list into a single BeatNum numset? Finally, is there a way to flatten the numset using beatnum.asview?
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: come_from_arrays(numsetList, dtype=None, shape=None, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from a (flat) list of numsets

    Parameters
    ----------
    numsetList : list or tuple
        List of numset-like objects (such as lists, tuples,
        and ndnumsets).
    dtype : data-type, optional
        valid dtype for total numsets
    shape : int or tuple of ints, optional
        Shape of the resulting numset. If not provided, inferred from
        ``numsetList[0]``.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.

    Returns
    -------
    bn.recnumset
        Record numset consisting of given numsetList columns.

    
API_3: come_from_str(datastring, dtype=None, shape=None, offset=0, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from binary data

    Note that despite the name of this function it does not accept `str`
    instances.

    Parameters
    ----------
    datastring : bytes-like
        Buffer of binary data
    dtype : data-type, optional
        Valid dtype for all numsets
    shape : int or tuple of ints, optional
        Shape of each numset.
    offset : int, optional
        Position in the buffer to start reading from.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.


    Returns
    -------
    bn.recnumset
        Record numset view into the data in datastring. This will be readonly
        if `datastring` is readonly.

    See Also
    --------
    beatnum.frombuffer

    

idx: 69
query: convert nan value to zero Return the changed numset
API_1: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_2: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_3: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    

idx: 70
query: Is there a way in beatnum to efficiently find the indices where the value changes? You can achieve this by comparing each element with its neighbor and using bn.filter_condition(condition). Afterwards, you can find the indices of a list of values in a beatnum numset.
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 71
query: How to find the groups of consecutive elements in a BeatNum numset I have to cluster the consecutive elements from a BeatNum numset. Considering the following example a = [ 0, 47, 48, 49, 50, 97, 98, 99] The output should be a list of tuples as follows [(0), (47, 48, 49, 50), (97, 98, 99)] Here the difference is just one between the elements. It will be great if the difference can also be specified as a limit or a hardcoded number. Finally, return the number of consecutive elements in the numset.
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: sep_split(sep=None, get_maxsep_split=-1):Return a list of the words in the string, using sep as the delimiter string.

  sep
    The delimiter according which to sep_split the string.
    None (the default value) averages sep_split according to any_condition whitespace,
    and discard empty strings from the result.
  get_maxsep_split
    Maximum number of sep_splits to do.
    -1 (the default value) averages no limit.
API_3: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    

idx: 72
query: Check if each element in a beatnum numset is in another numset This problem seems easy but I cannot quite get a nice-looking solution. I have two beatnum numsets (A and B), and I want to get the indices of A where the elements of A are in B and also get the indices of A where the elements are not in B.
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 73
query: Removing columns with index 1 and 3 in beatnum If you ever want to remove more than one columns, you just pass indices of columns you want removed as a list to bn.remove_operation, like this:
API_1: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_2: stack_col(x, *args, **params):pile_operation_col(*args, **kwargs)

Stack 1-D numsets as columns into a 2-D numset.

Take a sequence of 1-D numsets and pile_operation them as columns
to make a single 2-D numset. 2-D numsets are pile_operationed as-is,
just like with `hpile_operation`.  1-D numsets are turned into 2-D columns
first.

Parameters
----------
tup : sequence of 1-D or 2-D numsets.
    Arrays to pile_operation. All of them must have the same first dimension.

Returns
-------
pile_operationed : 2-D numset
    The numset formed by pile_operationing the given numsets.

See Also
--------
pile_operation, hpile_operation, vpile_operation, connect


API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 74
query: I am trying to translate every element of a beatnum.numset according to a given key I don't know about efficient, but you could use bn.vectorisation on the .get method of dictionaries:
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 75
query: How can the Euclidean distance be calculated with BeatNum?
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: normlizattion(self, *args, **kwargs):Convenience fluent method for :py:func:`normlizattion`.

        The arguments are the same as for :py:func:`normlizattion`, with
        this numset as data.
        
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 76
query: add the row at top in kf resort the index by inplace
API_1: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_2: duplicate(self, duplicates, axis=None):Repeat elements of an numset.
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 77
query: How can I convert the integer value of "numsets" to a string without any truncation in the variable "beatnum"? After that, how can I convert the value of "beatnum" to a tuple? Finally, please return the transformed tuple.
API_1: remove_masked_data(x):
    Return all the non-masked data as a 1-D numset.

    This function is equivalent to calling the "remove_masked_data" method of a
    `ma.MaskedArray`, see `ma.MaskedArray.remove_masked_data` for details.

    See Also
    --------
    ma.MaskedArray.remove_masked_data : Equivalent method.

    
API_2: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    
API_3: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    

idx: 78
query: cond = (a % 2) == 0 condition is True on even numbers I have an numset a and I would like to duplicate the elements of a n times if they are even or if they are positive. I average I want to duplicate only the elements that respect some condition, other elements are not displayed. In detail, if a meets the condition cond, I want to duplicate it n times.
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 79
query: what is the fastest and easiest way to set the super low value named tol to zero? Handling of reality and imaginary numbers separately
API_1: asview(a, *args, **params):    asview(self, order='C')

        Returns a 1D version of self, as a view.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            The elements of `a` are read using this index order. 'C' averages to
            index the elements in C-like order, with the last axis index
            changing fastest, back to the first axis index changing slowest.
            'F' averages to index the elements in Fortran-like index order, with
            the first index changing fastest, and the last index changing
            slowest. Note that the 'C' and 'F' options take no account of the
            memory layout of the underlying numset, and only refer to the order
            of axis indexing.  'A' averages to read the elements in Fortran-like
            index order if `m` is Fortran *contiguous* in memory, C-like order
            otherwise.  'K' averages to read the elements in the order they occur
            in memory, except for reversing the data when strides are negative.
            By default, 'C' index order is used.

        Returns
        -------
        MaskedArray
            Output view is of shape ``(self.size,)`` (or
            ``(bn.ma.product(self.shape),)``).

        
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 80
query: I am using beatnum. I have a matrix `M` 1*N and I want to get an numset from with N elements. To achieve it, Does anyone know a more elegant way to get the result?
API_1: asnumset(a, dtype=None, order=None):
    Convert the ibnut to a masked numset of the given data-type.

    No copy is performed if the ibnut is already an `ndnumset`. If `a` is
    a subclass of `MaskedArray`, a base class `MaskedArray` is returned.

    Parameters
    ----------
    a : numset_like
        Ibnut data, in any_condition form that can be converted to a masked numset. This
        includes lists, lists of tuples, tuples, tuples of tuples, tuples
        of lists, ndnumsets and masked numsets.
    dtype : dtype, optional
        By default, the data-type is inferred from the ibnut data.
    order : {'C', 'F'}, optional
        Whether to use row-major ('C') or column-major ('FORTRAN') memory
        representation.  Default is 'C'.

    Returns
    -------
    out : MaskedArray
        Masked numset interpretation of `a`.

    See Also
    --------
    asany_conditionnumset : Similar to `asnumset`, but conserves subclasses.

    
API_2: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 81
query: Is there a general method to flatten a numset into a single numset with N elements, where N is equal to every float in all the sub-numsets? Additionally, can we flatten a list of BeatNum numsets using beatnum.connect, which connects all the elements of the input list into a single BeatNum numset? Finally, can we use beatnum.asview to flatten the numset?
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: horizontal_stack(numsets):
    Stack numsets in sequence horizontally (column wise).
    This is equivalent to concatenation along the second axis,
    except for 1-D numsets filter_condition it connects along the first axis.
    Rebuilds numsets divided by hsep_split.
    This function makes most sense for numsets with up to 3 dimensions.
    For instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions connect,
    pile_operation and block provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the second axis, except 1-D numsets which can be any_condition length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets.

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 82
query: I find myself often having to check whether a column or row exists in a knowledgeframe before trying to reference it. Is there any way to do this more nicely? For example on an arbitrary object I can do x = getattr(anobject, 'id', default) - is there anything similar to this in monkey? Really any way to achieve what I'm doing more gracefully? Output the second row of data in `mycol` column if it exists, otherwise output NaN
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 83
query: How can I create a numset with all entries having the same value? I know that beatnum.create_ones() and beatnum.zeros() can do this for 1's and 0's, but how can I do it for -1? The shape of the numset is (5, 5). Additionally, I generate a list of one-dimensional beatnum numsets in a loop and later convert this list into a 2D beatnum numset. If I knew the number of items ahead of time, I would have preallocated a 2D beatnum numset, but since I don't, I put everything in a list. Is there a more efficient way to collect sequential numerical data (in my case beatnum numsets) than putting them in a list and then converting it into a beatnum.numset (which involves creating a new object and copying the data)? Is there a well-tested module that provides an "expandable" matrix data structure?
API_1: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    

idx: 84
query: Using merge_fn to zip the two data pipes. Repeating three times to argument the zipped data pipe.
API_1: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    
API_2: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 85
query: Find the most frequent number in a BeatNum numset Return the number
API_1: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        
API_3: switching_places(a, axes=None):
    Reverse or permute the axes of an numset; returns the modified numset.

    For an numset a with two axes, switching_places(a) gives the matrix switching_places.

    Refer to `beatnum.ndnumset.switching_places` for full_value_func documentation.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axes : tuple or list of ints, optional
        If specified, it must be a tuple or list which contains a permutation of
        [0,1,..,N-1] filter_condition N is the number of axes of a.  The i'th axis of the
        returned numset will correspond to the axis numbered ``axes[i]`` of the
        ibnut.  If not specified, defaults to ``range(a.ndim)[::-1]``, which
        reverses the order of the axes.

    Returns
    -------
    p : ndnumset
        `a` with its axes permuted.  A view is returned whenever
        possible.

    See Also
    --------
    ndnumset.switching_places : Equivalent method
    moveaxis
    argsort

    Notes
    -----
    Use `switching_places(a, argsort(axes))` to inverseert the transposition of tensors
    when using the `axes` keyword argument.

    Transposing a 1-D numset returns an unchanged view of the original numset.

    

idx: 86
query: I need to remain the rows where line_num is not equal to 0. What's the most efficient way to do it? it should be as simple as:
API_1: horizontal_stack(numsets):
    Stack numsets in sequence horizontally (column wise).
    This is equivalent to concatenation along the second axis,
    except for 1-D numsets filter_condition it connects along the first axis.
    Rebuilds numsets divided by hsep_split.
    This function makes most sense for numsets with up to 3 dimensions.
    For instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions connect,
    pile_operation and block provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the second axis, except 1-D numsets which can be any_condition length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 87
query: How to convert an numset of strings to an numset of floats in beatnum? Return the final result
API_1: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: convert_type(self, dtype, copy=True):Returns a copy of the numset after casting to a specified type.

        Parameters
        ----------
        dtype : beatnum.dtype or str
            The type of the returned numset.
        copy : bool
            Default `True`. By default, convert_type always returns a newly
            allocated ndnumset on the same context. If this is set to
            `False`, and the dtype requested is the same as the ndnumset's
            dtype, the ndnumset is returned instead of a copy.

        Returns
        -------
        NDArray, CSRNDArray or RowSparseNDArray
            The copied numset after casting to the specified type, or
            the same numset if copy=False and dtype is the same as the ibnut
            numset.

        

idx: 88
query: Beatnum Vector (N,1) dimension -> (N,) dimension conversion
API_1: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 89
query: How to invoke the standard deviation function on a 2d numset? with axis=0, it will return a 1d numset with the standard deviation of each column
API_1: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 90
query: How can I use change_shape_to to divide it into 4 chucks, such that it looks like this: I would like to change_shape_to a to (2, 4, 2, 4) and then switching_places it by (0, 2, 1, 3) to c
API_1: switching_places(a, axes=None):
    Reverse or permute the axes of an numset; returns the modified numset.

    For an numset a with two axes, switching_places(a) gives the matrix switching_places.

    Refer to `beatnum.ndnumset.switching_places` for full_value_func documentation.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axes : tuple or list of ints, optional
        If specified, it must be a tuple or list which contains a permutation of
        [0,1,..,N-1] filter_condition N is the number of axes of a.  The i'th axis of the
        returned numset will correspond to the axis numbered ``axes[i]`` of the
        ibnut.  If not specified, defaults to ``range(a.ndim)[::-1]``, which
        reverses the order of the axes.

    Returns
    -------
    p : ndnumset
        `a` with its axes permuted.  A view is returned whenever
        possible.

    See Also
    --------
    ndnumset.switching_places : Equivalent method
    moveaxis
    argsort

    Notes
    -----
    Use `switching_places(a, argsort(axes))` to inverseert the transposition of tensors
    when using the `axes` keyword argument.

    Transposing a 1-D numset returns an unchanged view of the original numset.

    
API_2: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_3: stack_col(x, *args, **params):pile_operation_col(*args, **kwargs)

Stack 1-D numsets as columns into a 2-D numset.

Take a sequence of 1-D numsets and pile_operation them as columns
to make a single 2-D numset. 2-D numsets are pile_operationed as-is,
just like with `hpile_operation`.  1-D numsets are turned into 2-D columns
first.

Parameters
----------
tup : sequence of 1-D or 2-D numsets.
    Arrays to pile_operation. All of them must have the same first dimension.

Returns
-------
pile_operationed : 2-D numset
    The numset formed by pile_operationing the given numsets.

See Also
--------
pile_operation, hpile_operation, vpile_operation, connect



idx: 91
query: how can I make a python list obj from this matrix? the list should be one dimensional and contain all values of the matrix
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 92
query: We zipp the above two data pipes and set keep_key to True according to merge_fn. Also, enumerating the zipped datapipe.
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    

idx: 93
query: How can I determine the (row, column) index of the minimum value in a matrix using the convert_index_or_arr() function? Additionally, how can I find the indices of the k smallest values in a matrix?
API_1: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    

idx: 94
query: Find indices of a list of values in a beatnum numset
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 95
query: Is there an efficient beatnum way to find each index where the value changes? You can get this functionality in beatnum by comparing each element with it's neighbor and then using bn.filter_condition(condition).
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 96
query: How can I join them using beatnum methods You can transpose and flatten the numsets:
API_1: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_2: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 97
query: How to get the index of a maximum element in a BeatNum numset along axis_value? Return the result
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_3: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        

idx: 98
query: How to get the cumulative distribution function with BeatNum? set bins to 10 and then generate a cumulative sum of the hist_operation contents to variable hist self
API_1: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_2: cumulative_sum(a, axis=None, dtype=None, out=None):
    Return the cumulative sum of the elements along a given axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        Axis along which the cumulative sum is computed. The default
        (None) is to compute the cumsum over the convert_into_one_dimed numset.
    dtype : dtype, optional
        Type of the returned numset and of the accumulator in which the
        elements are summed.  If `dtype` is not specified, it defaults
        to the dtype of `a`, unless `a` has an integer dtype with a
        precision less than that of the default platform integer.  In
        that case, the default platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must
        have the same shape and buffer length as the expected output
        but the type will be cast if necessary. See :ref:`ufuncs-output-type` for
        more details.

    Returns
    -------
    cumsum_along_axis : ndnumset.
        A new numset holding the result is returned unless `out` is
        specified, in which case a reference to `out` is returned. The
        result has the same size as `a`, and the same shape as `a` if
        `axis` is not None or `a` is a 1-d numset.

    See Also
    --------
    total_count : Sum numset elements.
    trapz : Integration of numset values using the composite trapezoidal rule.
    difference : Calculate the n-th discrete differenceerence along given axis.

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    ``cumsum(a)[-1]`` may not be equal to ``total_count(a)`` for floating-point
    values since ``total_count`` may use a pairwise summation routine, reducing
    the roundoff-error. See `total_count` for more information.

    
API_3: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    

idx: 99
query: This will tell me those values generate a mask to find all values that are even numbers Is there an efficient Beatnum mechanism to retrieve the integer indexes of locations in an numset based on a condition is true as opposed to the Boolean mask numset?
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 100
query: Each element in a batch is a `Dict` Takes an input DataPipe with batches of data, processes the batches one and produces a Dict for each batch. We only need the column 'a' from each batch.
API_1: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    

idx: 101
query: Connect a BeatNum numset to another BeatNum numset
API_1: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_2: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        
API_3: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    

idx: 102
query: How to count values in a certain range in a Beatnum numset? the number of elements fulfilling 2 < x < 8 is:
API_1: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_2: binoccurrence(x, weights=None, get_minlength=0):
    Count number of occurrences of each value in numset of non-negative ints.

    Parameters
    ----------
    x : ndnumset
        ibnut numset, 1 dimension, nonnegative ints.
    weights: ndnumset
        ibnut weigths same shape as x. (Optional)
    get_minlength: int
        A get_minimum number of bins for the output. (Optional)

    Returns
    --------
    out : ndnumset
        the result of binning the ibnut numset. The length of out is equal to aget_max(x)+1.

    Raises
    --------
    Value Error
        If the ibnut is not 1-dimensional, or contains elements with negative values,
        or if get_minlength is negative
    TypeError
        If the type of the ibnut is float or complex.

    
API_3: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    

idx: 103
query: How can I calculate matrix average values along the row of matrix, but to remove nan values from calculation? If all row values is NaNs, the average value is set to NaN.
API_1: masked_fill(a, fill_value=None):
    Return ibnut as an numset with masked data replaced by a fill value.

    If `a` is not a `MaskedArray`, `a` itself is returned.
    If `a` is a `MaskedArray` and `fill_value` is None, `fill_value` is set to
    ``a.fill_value``.

    Parameters
    ----------
    a : MaskedArray or numset_like
        An ibnut object.
    fill_value : numset_like, optional.
        Can be scalar or non-scalar. If non-scalar, the
        resulting masked_fill numset should be broadcastable
        over ibnut numset. Default is None.

    Returns
    -------
    a : ndnumset
        The masked_fill numset.

    See Also
    --------
    remove_masked_data

    
API_2: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 104
query: How can I know the (row, column) index of the minimum of a beatnum numset/matrix? Use convert_index_or_arr()
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    
API_3: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 105
query: How does one add rows to a beatnum numset? Is there a beatnumthonic way to do this?
API_1: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_2: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 106
query: I am trying to do element-wise string concatenation. I thought Add() was the way to do it in beatnum but obviously it is not working as expected.
API_1: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: sep_split(sep=None, get_maxsep_split=-1):Return a list of the words in the string, using sep as the delimiter string.

  sep
    The delimiter according which to sep_split the string.
    None (the default value) averages sep_split according to any_condition whitespace,
    and discard empty strings from the result.
  get_maxsep_split
    Maximum number of sep_splits to do.
    -1 (the default value) averages no limit.

idx: 107
query: How can we use the `data` numset to specify the columns of the nonzero elements in the output numset? Additionally, how can we convert a numset of indices to a 1-hot encoded beatnum numset? Also, what is the beatnumthonic way to add rows to a beatnum numset?
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 108
query: cond = (a % 2) == 0 condition is True on even numbers I have an numset a and I would like to duplicate the elements of a n times if they are even or if they are positive. I average I want to duplicate only the elements that respect some condition, other elements are not displayed. In detail, if a meets the condition cond, I want to duplicate it n times.
API_1: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_2: duplicate(self, duplicates, axis=None):Repeat elements of an numset.
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 109
query: find index of the elements within range [low, high] Return the final numset of indices.
API_1: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_2: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 110
query: What is the best way to calculate the cumulative distribution function using BeatNum? Set the number of bins to 10 and generate a cumulative sum of the histogram operation contents into the variable "hist_self". Additionally, how can I efficiently sum all the values in a Pandas dataframe? The result should be a numeric value.
API_1: cumulative_sum(a, axis=None, dtype=None, out=None):
    Return the cumulative sum of the elements along a given axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        Axis along which the cumulative sum is computed. The default
        (None) is to compute the cumsum over the convert_into_one_dimed numset.
    dtype : dtype, optional
        Type of the returned numset and of the accumulator in which the
        elements are summed.  If `dtype` is not specified, it defaults
        to the dtype of `a`, unless `a` has an integer dtype with a
        precision less than that of the default platform integer.  In
        that case, the default platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must
        have the same shape and buffer length as the expected output
        but the type will be cast if necessary. See :ref:`ufuncs-output-type` for
        more details.

    Returns
    -------
    cumsum_along_axis : ndnumset.
        A new numset holding the result is returned unless `out` is
        specified, in which case a reference to `out` is returned. The
        result has the same size as `a`, and the same shape as `a` if
        `axis` is not None or `a` is a 1-d numset.

    See Also
    --------
    total_count : Sum numset elements.
    trapz : Integration of numset values using the composite trapezoidal rule.
    difference : Calculate the n-th discrete differenceerence along given axis.

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    ``cumsum(a)[-1]`` may not be equal to ``total_count(a)`` for floating-point
    values since ``total_count`` may use a pairwise summation routine, reducing
    the roundoff-error. See `total_count` for more information.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    

idx: 111
query: Find the index of the k smallest values of a beatnum numset
API_1: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 112
query: How can I find the indices of the minimum values along axis zero? Also, how can I find the index of the maximum element in a BeatNum numset along axis_value?
API_1: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_2: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 113
query: Flattening a list of BeatNum numsets? We can use beatnum.connect, which as the name suggests, basically connects all the elements of such an input list into a single BeatNum numset And then we can use beatnum.asview to flatten the numset
API_1: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_2: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 114
query: How to obtain monkey KnowledgeFrame without index I want to print the whole knowledgeframe, but I don't want to print the index
API_1: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 115
query: Return the indices of the minimum values along (axis is zero).
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 116
query: Can you please find the closest value in the beatnum numset and return the result? After that, can you also find the index of the k smallest values in the beatnum numset?
API_1: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: absolute(self, *args, **kwargs):Convenience fluent method for :py:func:`absolute`.

        The arguments are the same as for :py:func:`absolute`, with
        this numset as data.
        
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 117
query: Convert Python sequence to BeatNum numset, filling missing values with 0
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    

idx: 118
query: One element is generated from each input Iterable DataPipes in turn, until the end when the shortest input DataPipe is used up.
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 119
query: Averaging over every 3 elements of a beatnum numset I have a beatnum numset. I want to create a new numset which is the average over every consecutive triplet of elements. So the new numset will be a third of the size as the original. Return it
API_1: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 120
query: I wish to find and return the minimum value in this 2D numset The following code is aim to implement it
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_3: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    

idx: 121
query: How can I calculate the standard deviation for each column of a 2D numset using the standard deviation function? If I specify axis=0, will it return a 1D numset with the standard deviation of each column? Additionally, I have a 2D numset with shape (x, y) and I want to convert it to a 3D numset with shape (x, y, 1). Is there an efficient and Pythonic way to achieve this?
API_1: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        

idx: 122
query: How to add items into a beatnum numset? add one element to each row using column stack operation.
API_1: stack_col(x, *args, **params):pile_operation_col(*args, **kwargs)

Stack 1-D numsets as columns into a 2-D numset.

Take a sequence of 1-D numsets and pile_operation them as columns
to make a single 2-D numset. 2-D numsets are pile_operationed as-is,
just like with `hpile_operation`.  1-D numsets are turned into 2-D columns
first.

Parameters
----------
tup : sequence of 1-D or 2-D numsets.
    Arrays to pile_operation. All of them must have the same first dimension.

Returns
-------
pile_operationed : 2-D numset
    The numset formed by pile_operationing the given numsets.

See Also
--------
pile_operation, hpile_operation, vpile_operation, connect


API_2: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 123
query: I want to add the first element on to the end of the numset. Return the appended numset.
API_1: add_concat(x1, x2):
    Return element-wise string concatenation for two numsets of str or unicode.

    Arrays `x1` and `x2` must have the same shape.

    Parameters
    ----------
    x1 : numset_like of str or unicode
        Ibnut numset.
    x2 : numset_like of str or unicode
        Ibnut numset.

    Returns
    -------
    add_concat : ndnumset
        Output numset of `string_` or `unicode_`, depending on ibnut types
        of the same shape as `x1` and `x2`.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: apd(object, /):Append object to the end of the list.

idx: 124
query: I'd like to calculate element-wise average between a, b and c.
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 125
query: What's the best way to sum all values in a Pandas dataframe? the result is a numeric value
API_1: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: cumulative_sum(a, axis=None, dtype=None, out=None):
    Return the cumulative sum of the elements along a given axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        Axis along which the cumulative sum is computed. The default
        (None) is to compute the cumsum over the convert_into_one_dimed numset.
    dtype : dtype, optional
        Type of the returned numset and of the accumulator in which the
        elements are summed.  If `dtype` is not specified, it defaults
        to the dtype of `a`, unless `a` has an integer dtype with a
        precision less than that of the default platform integer.  In
        that case, the default platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must
        have the same shape and buffer length as the expected output
        but the type will be cast if necessary. See :ref:`ufuncs-output-type` for
        more details.

    Returns
    -------
    cumsum_along_axis : ndnumset.
        A new numset holding the result is returned unless `out` is
        specified, in which case a reference to `out` is returned. The
        result has the same size as `a`, and the same shape as `a` if
        `axis` is not None or `a` is a 1-d numset.

    See Also
    --------
    total_count : Sum numset elements.
    trapz : Integration of numset values using the composite trapezoidal rule.
    difference : Calculate the n-th discrete differenceerence along given axis.

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    ``cumsum(a)[-1]`` may not be equal to ``total_count(a)`` for floating-point
    values since ``total_count`` may use a pairwise summation routine, reducing
    the roundoff-error. See `total_count` for more information.

    

idx: 126
query: Show the usage of the input_shapes argument in the automatic_compression() method.
API_1: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 127
query: Benchmark a custom TensorFlow Lite model on a Raspberry Pi 4B.
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: add_concat(x1, x2):
    Return element-wise string concatenation for two numsets of str or unicode.

    Arrays `x1` and `x2` must have the same shape.

    Parameters
    ----------
    x1 : numset_like of str or unicode
        Ibnut numset.
    x2 : numset_like of str or unicode
        Ibnut numset.

    Returns
    -------
    add_concat : ndnumset
        Output numset of `string_` or `unicode_`, depending on ibnut types
        of the same shape as `x1` and `x2`.

    

idx: 128
query: I have a list containing beatnum numsets something like L=[a,b,c] where a, b and c are beatnum numsets with sizes N_a in T, N_b in T and N_c in T. I want to row-wise connect a, b and c and get a beatnum numset with shape (N_a+N_b+N_c, T). Clearly one solution is run a for loop and use beatnum.connect, but is there any pythonic way to do this?
API_1: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_2: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 129
query: Method 1 map_dp_1 = dp.map(add_one) Invocation via functional form is preferred Method 2 We discourage the usage of `lambda` functions as they are not serializable with `pickle` Using `lambda` to implement add_two rather than add_one that is mentioned in above.
API_1: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_2: come_from_arrays(numsetList, dtype=None, shape=None, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from a (flat) list of numsets

    Parameters
    ----------
    numsetList : list or tuple
        List of numset-like objects (such as lists, tuples,
        and ndnumsets).
    dtype : data-type, optional
        valid dtype for total numsets
    shape : int or tuple of ints, optional
        Shape of the resulting numset. If not provided, inferred from
        ``numsetList[0]``.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.

    Returns
    -------
    bn.recnumset
        Record numset consisting of given numsetList columns.

    
API_3: pile_operation(x, *args, **params):pile_operation(*args, **kwargs)

Join a sequence of numsets along a new axis.

The ``axis`` parameter specifies the index of the new axis in the
dimensions of the result. For example, if ``axis=0`` it will be the first
dimension and if ``axis=-1`` it will be the last dimension.

.. versionadded:: 1.10.0

Parameters
----------
numsets : sequence of numset_like
    Each numset must have the same shape.

axis : int, optional
    The axis in the result numset along which the ibnut numsets are pile_operationed.

out : ndnumset, optional
    If provided, the destination to place the result. The shape must be
    correct, matching that of what pile_operation would have returned if no
    out argument were specified.

Returns
-------
pile_operationed : ndnumset
    The pile_operationed numset has one more dimension than the ibnut numsets.

See Also
--------
connect : Join a sequence of numsets along an existing axis.
block : Assemble an nd-numset from nested lists of blocks.
sep_split : Split numset into a list of multiple sub-numsets of equal size.



idx: 130
query: unioner the above two knowledgeframes on column 'company'
API_1: come_from_arrays(numsetList, dtype=None, shape=None, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from a (flat) list of numsets

    Parameters
    ----------
    numsetList : list or tuple
        List of numset-like objects (such as lists, tuples,
        and ndnumsets).
    dtype : data-type, optional
        valid dtype for total numsets
    shape : int or tuple of ints, optional
        Shape of the resulting numset. If not provided, inferred from
        ``numsetList[0]``.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.

    Returns
    -------
    bn.recnumset
        Record numset consisting of given numsetList columns.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: come_from_str(datastring, dtype=None, shape=None, offset=0, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from binary data

    Note that despite the name of this function it does not accept `str`
    instances.

    Parameters
    ----------
    datastring : bytes-like
        Buffer of binary data
    dtype : data-type, optional
        Valid dtype for all numsets
    shape : int or tuple of ints, optional
        Shape of each numset.
    offset : int, optional
        Position in the buffer to start reading from.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.


    Returns
    -------
    bn.recnumset
        Record numset view into the data in datastring. This will be readonly
        if `datastring` is readonly.

    See Also
    --------
    beatnum.frombuffer

    

idx: 131
query: How to add a new row to an empty beatnum numset example: input: bn.numset([1,2,3]) and bn.numset([4,5,6]) output: bn.numset([[1,2,3],[4,5,6]]) Return the new numset
API_1: horizontal_stack(numsets):
    Stack numsets in sequence horizontally (column wise).
    This is equivalent to concatenation along the second axis,
    except for 1-D numsets filter_condition it connects along the first axis.
    Rebuilds numsets divided by hsep_split.
    This function makes most sense for numsets with up to 3 dimensions.
    For instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions connect,
    pile_operation and block provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the second axis, except 1-D numsets which can be any_condition length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets.

    
API_2: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 132
query: def broadcasting_app(a, L, S): Window len = L, Stride len/stepsize = S
API_1: inverse(a):
    Compute the (multiplicative) inverseerse of a matrix.

    Given a square matrix `a`, return the matrix `ainverse` satisfying
    ``dot(a, ainverse) = dot(ainverse, a) = eye(a.shape[0])``.

    Parameters
    ----------
    a : (..., M, M) numset_like
        Matrix to be inverseerted.

    Returns
    -------
    ainverse : (..., M, M) ndnumset or matrix
        (Multiplicative) inverseerse of the matrix `a`.

    Raises
    ------
    LinAlgError
        If `a` is not square or inverseersion fails.

    See Also
    --------
    scipy.linalg.inverse : Similar function in SciPy.

    Notes
    -----

    .. versionadded:: 1.8.0

    Broadcasting rules apply, see the `beatnum.linalg` documentation for
    details.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 133
query: what is the fastest and easiest way to set the super low value named tol to zero? Handling of reality and imaginary numbers separately
API_1: imaginary(val):
    Return the imaginaryinary part of the complex argument.

    Parameters
    ----------
    val : numset_like
        Ibnut numset.

    Returns
    -------
    out : ndnumset or scalar
        The imaginaryinary component of the complex argument. If `val` is reality,
        the type of `val` is used for the output.  If `val` has complex
        elements, the returned type is float.

    See Also
    --------
    reality, angle, reality_if_close

    
API_2: asview(a, *args, **params):    asview(self, order='C')

        Returns a 1D version of self, as a view.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            The elements of `a` are read using this index order. 'C' averages to
            index the elements in C-like order, with the last axis index
            changing fastest, back to the first axis index changing slowest.
            'F' averages to index the elements in Fortran-like index order, with
            the first index changing fastest, and the last index changing
            slowest. Note that the 'C' and 'F' options take no account of the
            memory layout of the underlying numset, and only refer to the order
            of axis indexing.  'A' averages to read the elements in Fortran-like
            index order if `m` is Fortran *contiguous* in memory, C-like order
            otherwise.  'K' averages to read the elements in the order they occur
            in memory, except for reversing the data when strides are negative.
            By default, 'C' index order is used.

        Returns
        -------
        MaskedArray
            Output view is of shape ``(self.size,)`` (or
            ``(bn.ma.product(self.shape),)``).

        
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 134
query: I have a list containing beatnum numsets something like L=[a,b,c] where a, b and c are beatnum numsets with sizes N_a in T, N_b in T and N_c in T. I want to row-wise connect a, b and c and get a beatnum numset with shape (N_a+N_b+N_c, T). Clearly one solution is run a for loop and use beatnum.connect, but is there any pythonic way to do this?
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_3: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    

idx: 135
query: Add Leading Zeros to Strings at `col_name` in Monkey Dataframe The maximum length of the string is 15 Return the knowledgeframe
API_1: imaginary(val):
    Return the imaginaryinary part of the complex argument.

    Parameters
    ----------
    val : numset_like
        Ibnut numset.

    Returns
    -------
    out : ndnumset or scalar
        The imaginaryinary component of the complex argument. If `val` is reality,
        the type of `val` is used for the output.  If `val` has complex
        elements, the returned type is float.

    See Also
    --------
    reality, angle, reality_if_close

    
API_2: asview(a, *args, **params):    asview(self, order='C')

        Returns a 1D version of self, as a view.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            The elements of `a` are read using this index order. 'C' averages to
            index the elements in C-like order, with the last axis index
            changing fastest, back to the first axis index changing slowest.
            'F' averages to index the elements in Fortran-like index order, with
            the first index changing fastest, and the last index changing
            slowest. Note that the 'C' and 'F' options take no account of the
            memory layout of the underlying numset, and only refer to the order
            of axis indexing.  'A' averages to read the elements in Fortran-like
            index order if `m` is Fortran *contiguous* in memory, C-like order
            otherwise.  'K' averages to read the elements in the order they occur
            in memory, except for reversing the data when strides are negative.
            By default, 'C' index order is used.

        Returns
        -------
        MaskedArray
            Output view is of shape ``(self.size,)`` (or
            ``(bn.ma.product(self.shape),)``).

        
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 136
query: How can I calculate the element-wise average between a, b, and c? Also, how can I multiply the elements of numsets row-wise using the formula out[i] = a[i] * b[i], where a[i] has a shape of (2,) and b[i] is a scalar?
API_1: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    

idx: 137
query: How to get the first three elements of a datapipe?
API_1: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_2: cumulative_sum(a, axis=None, dtype=None, out=None):
    Return the cumulative sum of the elements along a given axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        Axis along which the cumulative sum is computed. The default
        (None) is to compute the cumsum over the convert_into_one_dimed numset.
    dtype : dtype, optional
        Type of the returned numset and of the accumulator in which the
        elements are summed.  If `dtype` is not specified, it defaults
        to the dtype of `a`, unless `a` has an integer dtype with a
        precision less than that of the default platform integer.  In
        that case, the default platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must
        have the same shape and buffer length as the expected output
        but the type will be cast if necessary. See :ref:`ufuncs-output-type` for
        more details.

    Returns
    -------
    cumsum_along_axis : ndnumset.
        A new numset holding the result is returned unless `out` is
        specified, in which case a reference to `out` is returned. The
        result has the same size as `a`, and the same shape as `a` if
        `axis` is not None or `a` is a 1-d numset.

    See Also
    --------
    total_count : Sum numset elements.
    trapz : Integration of numset values using the composite trapezoidal rule.
    difference : Calculate the n-th discrete differenceerence along given axis.

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    ``cumsum(a)[-1]`` may not be equal to ``total_count(a)`` for floating-point
    values since ``total_count`` may use a pairwise summation routine, reducing
    the roundoff-error. See `total_count` for more information.

    
API_3: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    

idx: 138
query: How to sip rows of Monkey KnowledgeFrame whose value in a certain column is NaN
API_1: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    
API_2: ifnan(x, /):Return True if x is a NaN (not a number), and False otherwise.
API_3: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    

idx: 139
query: I have an numset of distances called dists. I want to select dists which are within a range [r, r+dr]. You don't actually need filter_condition if you're just trying to filter out the elements of dists that don't fit your criteria:
API_1: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and



idx: 140
query: What is the most efficient and simple method to assign the extremely small value called "tol" to zero? Additionally, I would like to handle real and imaginary numbers separately. After that, I want to locate and retrieve the smallest value in this two-dimensional set of numbers. The provided code is intended to accomplish this task.
API_1: imaginary(val):
    Return the imaginaryinary part of the complex argument.

    Parameters
    ----------
    val : numset_like
        Ibnut numset.

    Returns
    -------
    out : ndnumset or scalar
        The imaginaryinary component of the complex argument. If `val` is reality,
        the type of `val` is used for the output.  If `val` has complex
        elements, the returned type is float.

    See Also
    --------
    reality, angle, reality_if_close

    
API_2: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 141
query: I have a dictionary that I need to convert to a BeatNum structured numset.
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 142
query: Can you help me find unique rows in a beatnum.numset? Also, I have a beatnum numset and I want to verify if it is sorted. Can I use beatnum.total for this?
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: cumulative_sum(a, axis=None, dtype=None, out=None):
    Return the cumulative sum of the elements along a given axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        Axis along which the cumulative sum is computed. The default
        (None) is to compute the cumsum over the convert_into_one_dimed numset.
    dtype : dtype, optional
        Type of the returned numset and of the accumulator in which the
        elements are summed.  If `dtype` is not specified, it defaults
        to the dtype of `a`, unless `a` has an integer dtype with a
        precision less than that of the default platform integer.  In
        that case, the default platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must
        have the same shape and buffer length as the expected output
        but the type will be cast if necessary. See :ref:`ufuncs-output-type` for
        more details.

    Returns
    -------
    cumsum_along_axis : ndnumset.
        A new numset holding the result is returned unless `out` is
        specified, in which case a reference to `out` is returned. The
        result has the same size as `a`, and the same shape as `a` if
        `axis` is not None or `a` is a 1-d numset.

    See Also
    --------
    total_count : Sum numset elements.
    trapz : Integration of numset values using the composite trapezoidal rule.
    difference : Calculate the n-th discrete differenceerence along given axis.

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    ``cumsum(a)[-1]`` may not be equal to ``total_count(a)`` for floating-point
    values since ``total_count`` may use a pairwise summation routine, reducing
    the roundoff-error. See `total_count` for more information.

    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 143
query: I want to check if all values in the columns of a beatnum numset/matrix are the same. A column shares a common value if all the values in that column are True: The below code checks if all values in the columns are the same using a == a[0,:] and axis=0
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        

idx: 144
query: I want to check if all values in the columns of a beatnum numset/matrix are the same. A column shares a common value if all the values in that column are True: The below code checks if all values in the columns are the same using a == a[0,:] and axis=0
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_3: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    

idx: 145
query: I would like to dynamically piece a beatnum numset along a specific axis. I think one way would be to use piece(start, end):
API_1: piece(self, *args, **kwargs):Convenience fluent method for :py:func:`piece`.

        The arguments are the same as for :py:func:`piece`, with
        this numset as data.
        
API_2: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_3: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        

idx: 146
query: Here's a one solution to remove columns based on duplicate column names: Return the duplicated knowledgeframe
API_1: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 147
query: How can I find the indices of elements in beatnum numset A that are also present in numset B, as well as the indices of elements in A that are not present in B? I have tried using symmetric difference and checking each element individually, but I haven't found a satisfactory solution yet.
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: seting_exclusive_or_one_dim(ar1, ar2, assume_uniq=False):
    Find the set exclusive-or of two numsets.

    Return the sorted, uniq values that are in only one (not both) of the
    ibnut numsets.

    Parameters
    ----------
    ar1, ar2 : numset_like
        Ibnut numsets.
    assume_uniq : bool
        If True, the ibnut numsets are both assumed to be uniq, which
        can speed up the calculation.  Default is False.

    Returns
    -------
    seting_exclusive_or_one_dim : ndnumset
        Sorted 1D numset of uniq values that are in only one of the ibnut
        numsets.

    
API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 148
query: How can I convert a Python sequence to a BeatNum numset, filling missing values with 0? Additionally, I have a BeatNum numset and I would like to know how to retrieve multiple values from it by index. Specifically, how can I get the values at index positions 1 and 4?
API_1: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_2: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    
API_3: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 149
query: Is there a way to compare what elements in a exist in b? Return a numset of booleans, True if elements in a exist in b, False otherwise
API_1: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 150
query: Add a new column named 'Fruit Total' that sums the values of the other columns Note that igonring the NaN values
API_1: pile_operation(x, *args, **params):pile_operation(*args, **kwargs)

Join a sequence of numsets along a new axis.

The ``axis`` parameter specifies the index of the new axis in the
dimensions of the result. For example, if ``axis=0`` it will be the first
dimension and if ``axis=-1`` it will be the last dimension.

.. versionadded:: 1.10.0

Parameters
----------
numsets : sequence of numset_like
    Each numset must have the same shape.

axis : int, optional
    The axis in the result numset along which the ibnut numsets are pile_operationed.

out : ndnumset, optional
    If provided, the destination to place the result. The shape must be
    correct, matching that of what pile_operation would have returned if no
    out argument were specified.

Returns
-------
pile_operationed : ndnumset
    The pile_operationed numset has one more dimension than the ibnut numsets.

See Also
--------
connect : Join a sequence of numsets along an existing axis.
block : Assemble an nd-numset from nested lists of blocks.
sep_split : Split numset into a list of multiple sub-numsets of equal size.


API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 151
query: How can I calculate the average of every three consecutive elements in a beatnum numset? I want to create a new numset with the average values, which will be one-third the size of the original numset. Please return the new numset. Additionally, how can I create a numset where every entry has the same value? I know beatnum.create_ones() and beatnum.zeros() can do this for 1's and 0's, but what about -1? The shape of the numset is (5, 5).
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 152
query: I generate a list of one dimensional beatnum numsets in a loop and later convert this list to a 2d beatnum numset. I would've preallocated a 2d beatnum numset if i knew the number of items ahead of time, but I don't, therefore I put everything in a list. s there a better way (performancewise) to go about the task of collecting sequential numerical data (in my case beatnum numsets) than putting them in a list and then making a beatnum.numset out of it (I am creating a new obj and copying the data)? Is there an "expandable" matrix data structure available in a well tested module?
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 153
query: How does one add rows to a beatnum numset? Is there a beatnumthonic way to do this?
API_1: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 154
query: I am using beatnum. I have a matrix with 1 column and N rows and I want to get an numset from with N elements. For example, if i have M = matrix([[1], [2], [3], [4]]), I want to get A = numset([1,2,3,4]). Return the numset
API_1: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 155
query: How do I create an numset where every entry is the same value? I know beatnum.create_ones() and beatnum.zeros() do this for 1's and 0's, but what about -1? the shape of the numset is (5, 5)
API_1: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    

idx: 156
query: How to remove specific elements in a beatnum numset？ I then want to remove 3,4,7 from a. All I know is the index of the values (index=[2,3,6]).
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 157
query: Is there a more elegant way to convert a numset of strings to a numset of floats in beatnum? I am using beatnum and have a matrix `M` with 1*N elements. I want to obtain a numset with N elements. Can anyone help me with this?
API_1: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        
API_2: convert_type(self, dtype, copy=True):Returns a copy of the numset after casting to a specified type.

        Parameters
        ----------
        dtype : beatnum.dtype or str
            The type of the returned numset.
        copy : bool
            Default `True`. By default, convert_type always returns a newly
            allocated ndnumset on the same context. If this is set to
            `False`, and the dtype requested is the same as the ndnumset's
            dtype, the ndnumset is returned instead of a copy.

        Returns
        -------
        NDArray, CSRNDArray or RowSparseNDArray
            The copied numset after casting to the specified type, or
            the same numset if copy=False and dtype is the same as the ibnut
            numset.

        
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 158
query: Using merge_fn to zip the two data pipes, and repeating three times to argument the zipped data pipe, and then sampling the result. Finally, we convert the result type to a list and take the third element of each tuple.
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_3: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    

idx: 159
query: Is there a quick way to "sub-flatten" or flatten only some of the first dimensions in a beatnum numset? Given a beatnum numset of dimensions (50,100,25), the resultant dimensions would be (5000,25)
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 160
query: Take samples from these DataPipes based on their weights with random seed 0, until all of them are exhausted.
API_1: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_2: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 161
query: How can I calculate matrix average values along the row of matrix, but to remove nan values from calculation? If all row values is NaNs, the average value is set to NaN.
API_1: masked_fill(a, fill_value=None):
    Return ibnut as an numset with masked data replaced by a fill value.

    If `a` is not a `MaskedArray`, `a` itself is returned.
    If `a` is a `MaskedArray` and `fill_value` is None, `fill_value` is set to
    ``a.fill_value``.

    Parameters
    ----------
    a : MaskedArray or numset_like
        An ibnut object.
    fill_value : numset_like, optional.
        Can be scalar or non-scalar. If non-scalar, the
        resulting masked_fill numset should be broadcastable
        over ibnut numset. Default is None.

    Returns
    -------
    a : ndnumset
        The masked_fill numset.

    See Also
    --------
    remove_masked_data

    
API_2: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 162
query: remove zero rows 2-D beatnum numset Use bn.total with an axis argument:
API_1: switching_places(a, axes=None):
    Reverse or permute the axes of an numset; returns the modified numset.

    For an numset a with two axes, switching_places(a) gives the matrix switching_places.

    Refer to `beatnum.ndnumset.switching_places` for full_value_func documentation.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axes : tuple or list of ints, optional
        If specified, it must be a tuple or list which contains a permutation of
        [0,1,..,N-1] filter_condition N is the number of axes of a.  The i'th axis of the
        returned numset will correspond to the axis numbered ``axes[i]`` of the
        ibnut.  If not specified, defaults to ``range(a.ndim)[::-1]``, which
        reverses the order of the axes.

    Returns
    -------
    p : ndnumset
        `a` with its axes permuted.  A view is returned whenever
        possible.

    See Also
    --------
    ndnumset.switching_places : Equivalent method
    moveaxis
    argsort

    Notes
    -----
    Use `switching_places(a, argsort(axes))` to inverseert the transposition of tensors
    when using the `axes` keyword argument.

    Transposing a 1-D numset returns an unchanged view of the original numset.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 163
query: How to augument the datapipe by repeating it six times.
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    

idx: 164
query: Find nearest value in beatnum numset return the result
API_1: absolute(self, *args, **kwargs):Convenience fluent method for :py:func:`absolute`.

        The arguments are the same as for :py:func:`absolute`, with
        this numset as data.
        
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 165
query: remove zero rows 2-D beatnum numset Use bn.total with an axis argument:
API_1: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_2: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 166
query: How can I use change_shape_to to divide it into 4 chucks, such that it looks like this: I would like to change_shape_to a to (2, 4, 2, 4) and then switching_places it by (0, 2, 1, 3) to c
API_1: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: switching_places(a, axes=None):
    Reverse or permute the axes of an numset; returns the modified numset.

    For an numset a with two axes, switching_places(a) gives the matrix switching_places.

    Refer to `beatnum.ndnumset.switching_places` for full_value_func documentation.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axes : tuple or list of ints, optional
        If specified, it must be a tuple or list which contains a permutation of
        [0,1,..,N-1] filter_condition N is the number of axes of a.  The i'th axis of the
        returned numset will correspond to the axis numbered ``axes[i]`` of the
        ibnut.  If not specified, defaults to ``range(a.ndim)[::-1]``, which
        reverses the order of the axes.

    Returns
    -------
    p : ndnumset
        `a` with its axes permuted.  A view is returned whenever
        possible.

    See Also
    --------
    ndnumset.switching_places : Equivalent method
    moveaxis
    argsort

    Notes
    -----
    Use `switching_places(a, argsort(axes))` to inverseert the transposition of tensors
    when using the `axes` keyword argument.

    Transposing a 1-D numset returns an unchanged view of the original numset.

    

idx: 167
query: Putting two IterDataPipes together based on their key.
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 168
query: Set value to an entire column `B` of a monkey knowledgeframe Return the changed knowledgeframe.
API_1: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_2: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 169
query: Partition numset into 3 chunks with Beatnum
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_3: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    

idx: 170
query: I am trying to do element-wise string concatenation. I thought Add() was the way to do it in beatnum but obviously it is not working as expected.
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: add_concat(x1, x2):
    Return element-wise string concatenation for two numsets of str or unicode.

    Arrays `x1` and `x2` must have the same shape.

    Parameters
    ----------
    x1 : numset_like of str or unicode
        Ibnut numset.
    x2 : numset_like of str or unicode
        Ibnut numset.

    Returns
    -------
    add_concat : ndnumset
        Output numset of `string_` or `unicode_`, depending on ibnut types
        of the same shape as `x1` and `x2`.

    

idx: 171
query: I would like to sip all data in a monkey knowledgeframe Using kf.index to sip all rows
API_1: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    

idx: 172
query: Finding the intersection between two collections In detail, first we create two sets, one for each collections. Then we find the intersection of the two sets.
API_1: cumulative_sum(a, axis=None, dtype=None, out=None):
    Return the cumulative sum of the elements along a given axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        Axis along which the cumulative sum is computed. The default
        (None) is to compute the cumsum over the convert_into_one_dimed numset.
    dtype : dtype, optional
        Type of the returned numset and of the accumulator in which the
        elements are summed.  If `dtype` is not specified, it defaults
        to the dtype of `a`, unless `a` has an integer dtype with a
        precision less than that of the default platform integer.  In
        that case, the default platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must
        have the same shape and buffer length as the expected output
        but the type will be cast if necessary. See :ref:`ufuncs-output-type` for
        more details.

    Returns
    -------
    cumsum_along_axis : ndnumset.
        A new numset holding the result is returned unless `out` is
        specified, in which case a reference to `out` is returned. The
        result has the same size as `a`, and the same shape as `a` if
        `axis` is not None or `a` is a 1-d numset.

    See Also
    --------
    total_count : Sum numset elements.
    trapz : Integration of numset values using the composite trapezoidal rule.
    difference : Calculate the n-th discrete differenceerence along given axis.

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    ``cumsum(a)[-1]`` may not be equal to ``total_count(a)`` for floating-point
    values since ``total_count`` may use a pairwise summation routine, reducing
    the roundoff-error. See `total_count` for more information.

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 173
query: Convert beatnum numset type and values from Float64 to Float32
API_1: convert_type(self, dtype, copy=True):Returns a copy of the numset after casting to a specified type.

        Parameters
        ----------
        dtype : beatnum.dtype or str
            The type of the returned numset.
        copy : bool
            Default `True`. By default, convert_type always returns a newly
            allocated ndnumset on the same context. If this is set to
            `False`, and the dtype requested is the same as the ndnumset's
            dtype, the ndnumset is returned instead of a copy.

        Returns
        -------
        NDArray, CSRNDArray or RowSparseNDArray
            The copied numset after casting to the specified type, or
            the same numset if copy=False and dtype is the same as the ibnut
            numset.

        
API_2: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 174
query: I have a knowledgeframe in monkey where each column has different value range. Any idea how I can normalize the columns of this knowledgeframe where each value is between 0 and 1?
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: asnumset(a, dtype=None, order=None):
    Convert the ibnut to a masked numset of the given data-type.

    No copy is performed if the ibnut is already an `ndnumset`. If `a` is
    a subclass of `MaskedArray`, a base class `MaskedArray` is returned.

    Parameters
    ----------
    a : numset_like
        Ibnut data, in any_condition form that can be converted to a masked numset. This
        includes lists, lists of tuples, tuples, tuples of tuples, tuples
        of lists, ndnumsets and masked numsets.
    dtype : dtype, optional
        By default, the data-type is inferred from the ibnut data.
    order : {'C', 'F'}, optional
        Whether to use row-major ('C') or column-major ('FORTRAN') memory
        representation.  Default is 'C'.

    Returns
    -------
    out : MaskedArray
        Masked numset interpretation of `a`.

    See Also
    --------
    asany_conditionnumset : Similar to `asnumset`, but conserves subclasses.

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 175
query: How to remove specific elements in a beatnum numset？ I then want to remove 3,4,7 from a. All I know is the index of the values (index=[2,3,6]).
API_1: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    

idx: 176
query: Check if each element in a beatnum numset is in another numset This problem seems easy but I cannot quite get a nice-looking solution. I have two beatnum numsets (A and B), and I want to get the indices of A where the elements of A are in B and also get the indices of A where the elements are not in B.
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 177
query: Count the number of occurrences of a value in a collections Return the count
API_1: horizontal_stack(numsets):
    Stack numsets in sequence horizontally (column wise).
    This is equivalent to concatenation along the second axis,
    except for 1-D numsets filter_condition it connects along the first axis.
    Rebuilds numsets divided by hsep_split.
    This function makes most sense for numsets with up to 3 dimensions.
    For instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions connect,
    pile_operation and block provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the second axis, except 1-D numsets which can be any_condition length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets.

    
API_2: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 178
query: How to count the number of true elements in a BeatNum bool numset? return the count value
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 179
query: This will tell me those values generate a mask to find all values that are even numbers Is there an efficient Beatnum mechanism to retrieve the integer indexes of locations in an numset based on a condition is true as opposed to the Boolean mask numset?
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_3: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and



idx: 180
query: List of numsets. Stack them using axis that is negative one .
API_1: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_2: come_from_arrays(numsetList, dtype=None, shape=None, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from a (flat) list of numsets

    Parameters
    ----------
    numsetList : list or tuple
        List of numset-like objects (such as lists, tuples,
        and ndnumsets).
    dtype : data-type, optional
        valid dtype for total numsets
    shape : int or tuple of ints, optional
        Shape of the resulting numset. If not provided, inferred from
        ``numsetList[0]``.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.

    Returns
    -------
    bn.recnumset
        Record numset consisting of given numsetList columns.

    
API_3: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    

idx: 181
query: Can you please find the indices of a list of values in a beatnum numset? After that, can you find the nearest value in the beatnum numset and return the result?
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 182
query: How do I create an numset where every entry is the same value? I know beatnum.create_ones() and beatnum.zeros() do this for 1's and 0's, but what about -1? the shape of the numset is (5, 5)
API_1: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 183
query: Join the three data pipes and obtain the enumerated datapipe.
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        

idx: 184
query: Is there a way to compare what elements in a exist in b? Return a numset of booleans, True if elements in a exist in b, False otherwise
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: intersection1dim(ar1, ar2, assume_uniq=False, inverseert=False):
    Test whether each element of an numset is also present in a second
    numset.

    The output is always a masked numset. See `beatnum.intersection1dim` for more details.

    We recommend using :func:`isin` instead of `intersection1dim` for new code.

    See Also
    --------
    isin       : Version of this function that preserves the shape of ar1.
    beatnum.intersection1dim : Equivalent function for ndnumsets.

    Notes
    -----
    .. versionadded:: 1.4.0

    

idx: 185
query: Is there a way to quickly flatten only certain dimensions in a beatnum numset? For example, if the beatnum numset has dimensions (50,100,25), can we flatten it to have dimensions (5000,25)? Additionally, how can we flatten a list of BeatNum numsets? We can achieve this by using beatnum.connect, which connects all the elements of the input list into a single BeatNum numset. Then, we can use beatnum.asview to flatten the numset.
API_1: horizontal_stack(numsets):
    Stack numsets in sequence horizontally (column wise).
    This is equivalent to concatenation along the second axis,
    except for 1-D numsets filter_condition it connects along the first axis.
    Rebuilds numsets divided by hsep_split.
    This function makes most sense for numsets with up to 3 dimensions.
    For instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions connect,
    pile_operation and block provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the second axis, except 1-D numsets which can be any_condition length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 186
query: Is there a more elegant way to remove specific elements (3, 4, 7) from a beatnum numset, given their indices (2, 3, 6), and obtain a numset with N elements from a matrix `M` of size 1*N using beatnum?
API_1: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 187
query: I am using beatnum. I have a matrix with 1 column and N rows and I want to get an numset from with N elements. For example, if i have M = matrix([[1], [2], [3], [4]]), I want to get A = numset([1,2,3,4]). Return the numset
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: asnumset(a, dtype=None, order=None):
    Convert the ibnut to a masked numset of the given data-type.

    No copy is performed if the ibnut is already an `ndnumset`. If `a` is
    a subclass of `MaskedArray`, a base class `MaskedArray` is returned.

    Parameters
    ----------
    a : numset_like
        Ibnut data, in any_condition form that can be converted to a masked numset. This
        includes lists, lists of tuples, tuples, tuples of tuples, tuples
        of lists, ndnumsets and masked numsets.
    dtype : dtype, optional
        By default, the data-type is inferred from the ibnut data.
    order : {'C', 'F'}, optional
        Whether to use row-major ('C') or column-major ('FORTRAN') memory
        representation.  Default is 'C'.

    Returns
    -------
    out : MaskedArray
        Masked numset interpretation of `a`.

    See Also
    --------
    asany_conditionnumset : Similar to `asnumset`, but conserves subclasses.

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 188
query: find index of the elements within range [low, high] Return the final numset of indices.
API_1: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_2: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_3: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    

idx: 189
query: I want to add the first element on to the end of the numset. Return the appended numset.
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: add_concat(x1, x2):
    Return element-wise string concatenation for two numsets of str or unicode.

    Arrays `x1` and `x2` must have the same shape.

    Parameters
    ----------
    x1 : numset_like of str or unicode
        Ibnut numset.
    x2 : numset_like of str or unicode
        Ibnut numset.

    Returns
    -------
    add_concat : ndnumset
        Output numset of `string_` or `unicode_`, depending on ibnut types
        of the same shape as `x1` and `x2`.

    

idx: 190
query: I want to prune my model using L2 norm and measure the latency.
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 191
query: I have a set of distances called "dists" and I want to select the distances within a range of [r, r+dr]. If you are only filtering out elements that don't fit the criteria, you don't actually need the "filter_condition". Additionally, the condition "cond = (a % 2) == 0" is true for even numbers. I have a set of numbers called "a" and I want to duplicate the elements n times if they are even or positive. I only want to duplicate the elements that meet a certain condition, and the other elements should not be displayed. Specifically, if "a" meets the condition "cond", I want to duplicate it n times.
API_1: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_2: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    
API_3: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and



idx: 192
query: Can you please append the first element to the end of the numset and return the updated numset? Also, can you prepend the element to the beatnum numset and return the updated numset?
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: apd(object, /):Append object to the end of the list.
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 193
query: y = bn.numset([2,1,5,2]) y axis filter out values larger than 2 remove masked values from m
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_3: remove_masked_data(x):
    Return all the non-masked data as a 1-D numset.

    This function is equivalent to calling the "remove_masked_data" method of a
    `ma.MaskedArray`, see `ma.MaskedArray.remove_masked_data` for details.

    See Also
    --------
    ma.MaskedArray.remove_masked_data : Equivalent method.

    

idx: 194
query: I need a general way to flatten that numset into a single numset of N elements, with N=every float in all the sub-numsets.
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_3: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    

idx: 195
query: How to add a new row to an empty beatnum numset example: input: bn.numset([1,2,3]) and bn.numset([4,5,6]) output: bn.numset([[1,2,3],[4,5,6]]) Return the new numset
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_3: horizontal_stack(numsets):
    Stack numsets in sequence horizontally (column wise).
    This is equivalent to concatenation along the second axis,
    except for 1-D numsets filter_condition it connects along the first axis.
    Rebuilds numsets divided by hsep_split.
    This function makes most sense for numsets with up to 3 dimensions.
    For instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions connect,
    pile_operation and block provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the second axis, except 1-D numsets which can be any_condition length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets.

    

idx: 196
query: kf = kf.reseting_index() make sure indexes pair with number of rows (for index, row in KnowledgeFrame.traversal) is a generator which yields both the index and row (as a Collections) for each row in the KnowledgeFrame, we need put the row['MSRA'] (as key) and row['THU'] (as value) into a rows_dict rows_dict = {} {MSRA: THU, ...}
API_1: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: binoccurrence(x, weights=None, get_minlength=0):
    Count number of occurrences of each value in numset of non-negative ints.

    Parameters
    ----------
    x : ndnumset
        ibnut numset, 1 dimension, nonnegative ints.
    weights: ndnumset
        ibnut weigths same shape as x. (Optional)
    get_minlength: int
        A get_minimum number of bins for the output. (Optional)

    Returns
    --------
    out : ndnumset
        the result of binning the ibnut numset. The length of out is equal to aget_max(x)+1.

    Raises
    --------
    Value Error
        If the ibnut is not 1-dimensional, or contains elements with negative values,
        or if get_minlength is negative
    TypeError
        If the type of the ibnut is float or complex.

    
API_3: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        

idx: 197
query: Is there an efficient beatnum way to find each index where the value changes? You can get this functionality in beatnum by comparing each element with it's neighbor and then using bn.filter_condition(condition).
API_1: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 198
query: Is there a simple way of replacing all negative values in an numset with `0`? using a BeatNum function `filter_condition` to solve it.
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 199
query: How can I compress a custom ONNX model using singular value decomposition?
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: intersection1dim(ar1, ar2, assume_uniq=False, inverseert=False):
    Test whether each element of an numset is also present in a second
    numset.

    The output is always a masked numset. See `beatnum.intersection1dim` for more details.

    We recommend using :func:`isin` instead of `intersection1dim` for new code.

    See Also
    --------
    isin       : Version of this function that preserves the shape of ar1.
    beatnum.intersection1dim : Equivalent function for ndnumsets.

    Notes
    -----
    .. versionadded:: 1.4.0

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 200
query: Find indices of elements equal to zero in a BeatNum numset Return the indices
API_1: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_2: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_3: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    

idx: 201
query: How do I change the column labels of a monkey KnowledgeFrame from ['A', 'B', 'C'] to ['a', 'b', 'c']?
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_3: remove_masked_data(x):
    Return all the non-masked data as a 1-D numset.

    This function is equivalent to calling the "remove_masked_data" method of a
    `ma.MaskedArray`, see `ma.MaskedArray.remove_masked_data` for details.

    See Also
    --------
    ma.MaskedArray.remove_masked_data : Equivalent method.

    

idx: 202
query: def broadcasting_app(a, L, S): Window len = L, Stride len/stepsize = S
API_1: inverse(a):
    Compute the (multiplicative) inverseerse of a matrix.

    Given a square matrix `a`, return the matrix `ainverse` satisfying
    ``dot(a, ainverse) = dot(ainverse, a) = eye(a.shape[0])``.

    Parameters
    ----------
    a : (..., M, M) numset_like
        Matrix to be inverseerted.

    Returns
    -------
    ainverse : (..., M, M) ndnumset or matrix
        (Multiplicative) inverseerse of the matrix `a`.

    Raises
    ------
    LinAlgError
        If `a` is not square or inverseersion fails.

    See Also
    --------
    scipy.linalg.inverse : Similar function in SciPy.

    Notes
    -----

    .. versionadded:: 1.8.0

    Broadcasting rules apply, see the `beatnum.linalg` documentation for
    details.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 203
query: I have an numset of distances called dists. I want to select dists which are within a range [r, r+dr]. You don't actually need filter_condition if you're just trying to filter out the elements of dists that don't fit your criteria:
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_3: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and



idx: 204
query: Return the row-index values of the knowledgeframe as a list
API_1: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: pile_operation(x, *args, **params):pile_operation(*args, **kwargs)

Join a sequence of numsets along a new axis.

The ``axis`` parameter specifies the index of the new axis in the
dimensions of the result. For example, if ``axis=0`` it will be the first
dimension and if ``axis=-1`` it will be the last dimension.

.. versionadded:: 1.10.0

Parameters
----------
numsets : sequence of numset_like
    Each numset must have the same shape.

axis : int, optional
    The axis in the result numset along which the ibnut numsets are pile_operationed.

out : ndnumset, optional
    If provided, the destination to place the result. The shape must be
    correct, matching that of what pile_operation would have returned if no
    out argument were specified.

Returns
-------
pile_operationed : ndnumset
    The pile_operationed numset has one more dimension than the ibnut numsets.

See Also
--------
connect : Join a sequence of numsets along an existing axis.
block : Assemble an nd-numset from nested lists of blocks.
sep_split : Split numset into a list of multiple sub-numsets of equal size.



idx: 205
query: Test if beatnum numset contains only zeros Return the result
API_1: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_2: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    
API_3: binoccurrence(x, weights=None, get_minlength=0):
    Count number of occurrences of each value in numset of non-negative ints.

    Parameters
    ----------
    x : ndnumset
        ibnut numset, 1 dimension, nonnegative ints.
    weights: ndnumset
        ibnut weigths same shape as x. (Optional)
    get_minlength: int
        A get_minimum number of bins for the output. (Optional)

    Returns
    --------
    out : ndnumset
        the result of binning the ibnut numset. The length of out is equal to aget_max(x)+1.

    Raises
    --------
    Value Error
        If the ibnut is not 1-dimensional, or contains elements with negative values,
        or if get_minlength is negative
    TypeError
        If the type of the ibnut is float or complex.

    

idx: 206
query: How can I set training configurations for reproducing the results fo a prior work?
API_1: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 207
query: How to invoke the standard deviation function on a 2d numset? with axis=0, it will return a 1d numset with the standard deviation of each column
API_1: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_2: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 208
query: I am trying to translate every element of a beatnum.numset according to a given key I don't know about efficient, but you could use bn.vectorisation on the .get method of dictionaries:
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    

idx: 209
query: Params: kf: The knowledgeframe to add to. list_to_add: The list to add. column_name_list: The column names of the list to add. Returns: The knowledgeframe with the list added.
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: ifnan(x, /):Return True if x is a NaN (not a number), and False otherwise.
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 210
query: Convert a YOLOX model with ONNX format to TFLite and optimize it for the Raspberry Pi 4B.
API_1: split_array(ary, indices_or_sections, axis=0):
    Split an numset into multiple sub-numsets.

    Please refer to the ``sep_split`` documentation.  The only differenceerence
    between these functions is that ``numset_sep_split`` allows
    `indices_or_sections` to be an integer that does *not* equally
    divide the axis. For an numset of length l that should be sep_split
    into n sections, it returns l % n sub-numsets of size l//n + 1
    and the rest of size l//n.

    See Also
    --------
    sep_split : Split numset into multiple sub-numsets of equal size.

    
API_2: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_3: seting_exclusive_or_one_dim(ar1, ar2, assume_uniq=False):
    Find the set exclusive-or of two numsets.

    Return the sorted, uniq values that are in only one (not both) of the
    ibnut numsets.

    Parameters
    ----------
    ar1, ar2 : numset_like
        Ibnut numsets.
    assume_uniq : bool
        If True, the ibnut numsets are both assumed to be uniq, which
        can speed up the calculation.  Default is False.

    Returns
    -------
    seting_exclusive_or_one_dim : ndnumset
        Sorted 1D numset of uniq values that are in only one of the ibnut
        numsets.

    

idx: 211
query: How do I configure a fx model for training?
API_1: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_2: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 212
query: list_of_lists format: [header, [row1], [row2], ...] header format: [column1, column2, ...] row format: [value1, value2, ...] How to convert list to knowledgeframe? Return the knowledgeframe
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 213
query: How can I perform element-wise string concatenation in beatnum? I tried using Add() but it doesn't work as expected. Additionally, how can I join the strings using beatnum methods? Can I transpose and flatten the numsets?
API_1: add_concat(x1, x2):
    Return element-wise string concatenation for two numsets of str or unicode.

    Arrays `x1` and `x2` must have the same shape.

    Parameters
    ----------
    x1 : numset_like of str or unicode
        Ibnut numset.
    x2 : numset_like of str or unicode
        Ibnut numset.

    Returns
    -------
    add_concat : ndnumset
        Output numset of `string_` or `unicode_`, depending on ibnut types
        of the same shape as `x1` and `x2`.

    
API_2: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 214
query: I have a beatnum numset and I like to check if it is sorted. Using beatnum.total to do this.
API_1: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 215
query: I would like to dynamically piece a beatnum numset along a specific axis. I think one way would be to use piece(start, end):
API_1: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 216
query: How can I get the values of column `A` when column `B`=3?
API_1: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_2: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 217
query: How to convert an numset of strings to an numset of floats in beatnum? Return the final result
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    

idx: 218
query: Filter the value smaller than 5 Second make the datapipe tensor-like by using `collate_fn`
API_1: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: switching_places(a, axes=None):
    Reverse or permute the axes of an numset; returns the modified numset.

    For an numset a with two axes, switching_places(a) gives the matrix switching_places.

    Refer to `beatnum.ndnumset.switching_places` for full_value_func documentation.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axes : tuple or list of ints, optional
        If specified, it must be a tuple or list which contains a permutation of
        [0,1,..,N-1] filter_condition N is the number of axes of a.  The i'th axis of the
        returned numset will correspond to the axis numbered ``axes[i]`` of the
        ibnut.  If not specified, defaults to ``range(a.ndim)[::-1]``, which
        reverses the order of the axes.

    Returns
    -------
    p : ndnumset
        `a` with its axes permuted.  A view is returned whenever
        possible.

    See Also
    --------
    ndnumset.switching_places : Equivalent method
    moveaxis
    argsort

    Notes
    -----
    Use `switching_places(a, argsort(axes))` to inverseert the transposition of tensors
    when using the `axes` keyword argument.

    Transposing a 1-D numset returns an unchanged view of the original numset.

    

idx: 219
query: Can you please stack the given numsets using the negative one axis? Additionally, I would like to append the first element of the numset to the end. Could you return the appended numset?
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: pile_operation(x, *args, **params):pile_operation(*args, **kwargs)

Join a sequence of numsets along a new axis.

The ``axis`` parameter specifies the index of the new axis in the
dimensions of the result. For example, if ``axis=0`` it will be the first
dimension and if ``axis=-1`` it will be the last dimension.

.. versionadded:: 1.10.0

Parameters
----------
numsets : sequence of numset_like
    Each numset must have the same shape.

axis : int, optional
    The axis in the result numset along which the ibnut numsets are pile_operationed.

out : ndnumset, optional
    If provided, the destination to place the result. The shape must be
    correct, matching that of what pile_operation would have returned if no
    out argument were specified.

Returns
-------
pile_operationed : ndnumset
    The pile_operationed numset has one more dimension than the ibnut numsets.

See Also
--------
connect : Join a sequence of numsets along an existing axis.
block : Assemble an nd-numset from nested lists of blocks.
sep_split : Split numset into a list of multiple sub-numsets of equal size.


API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 220
query: How can I find the indices of elements within a specific range [low, high] in a given numset? Also, how can I count the number of elements within a certain range in a Beatnum numset? Specifically, how many elements are there that satisfy the condition 2 < x < 8?
API_1: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_2: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_3: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    

idx: 221
query: Perform a symmetric difference between two beatnum numsets. Don't convert the beatnum numset to a set to perform exclusive-or. Use seting_exclusive_or_one_dim directly.
API_1: split_array(ary, indices_or_sections, axis=0):
    Split an numset into multiple sub-numsets.

    Please refer to the ``sep_split`` documentation.  The only differenceerence
    between these functions is that ``numset_sep_split`` allows
    `indices_or_sections` to be an integer that does *not* equally
    divide the axis. For an numset of length l that should be sep_split
    into n sections, it returns l % n sub-numsets of size l//n + 1
    and the rest of size l//n.

    See Also
    --------
    sep_split : Split numset into multiple sub-numsets of equal size.

    
API_2: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_3: seting_exclusive_or_one_dim(ar1, ar2, assume_uniq=False):
    Find the set exclusive-or of two numsets.

    Return the sorted, uniq values that are in only one (not both) of the
    ibnut numsets.

    Parameters
    ----------
    ar1, ar2 : numset_like
        Ibnut numsets.
    assume_uniq : bool
        If True, the ibnut numsets are both assumed to be uniq, which
        can speed up the calculation.  Default is False.

    Returns
    -------
    seting_exclusive_or_one_dim : ndnumset
        Sorted 1D numset of uniq values that are in only one of the ibnut
        numsets.

    

idx: 222
query: Prepend element to beatnum numset Return the numset
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        
API_3: intersection1dim(ar1, ar2, assume_uniq=False, inverseert=False):
    Test whether each element of an numset is also present in a second
    numset.

    The output is always a masked numset. See `beatnum.intersection1dim` for more details.

    We recommend using :func:`isin` instead of `intersection1dim` for new code.

    See Also
    --------
    isin       : Version of this function that preserves the shape of ar1.
    beatnum.intersection1dim : Equivalent function for ndnumsets.

    Notes
    -----
    .. versionadded:: 1.4.0

    

idx: 223
query: Return the indices of the minimum values along (axis is zero).
API_1: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_3: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 224
query: Convert Column `Date` to Date Format using monkey function return the coverted knowledgeframe
API_1: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 225
query: how can I make a python list obj from this matrix? the list should be one dimensional and contain all values of the matrix
API_1: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_2: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 226
query: How to count the number of true elements in a BeatNum bool numset? return the count value
API_1: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 227
query: How to remove all rows in a beatnum.ndnumset that contain non-numeric values? Return the final result
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 228
query: What's the best way to sum all values in a Pandas dataframe? the result is a numeric value
API_1: cumulative_sum(a, axis=None, dtype=None, out=None):
    Return the cumulative sum of the elements along a given axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        Axis along which the cumulative sum is computed. The default
        (None) is to compute the cumsum over the convert_into_one_dimed numset.
    dtype : dtype, optional
        Type of the returned numset and of the accumulator in which the
        elements are summed.  If `dtype` is not specified, it defaults
        to the dtype of `a`, unless `a` has an integer dtype with a
        precision less than that of the default platform integer.  In
        that case, the default platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must
        have the same shape and buffer length as the expected output
        but the type will be cast if necessary. See :ref:`ufuncs-output-type` for
        more details.

    Returns
    -------
    cumsum_along_axis : ndnumset.
        A new numset holding the result is returned unless `out` is
        specified, in which case a reference to `out` is returned. The
        result has the same size as `a`, and the same shape as `a` if
        `axis` is not None or `a` is a 1-d numset.

    See Also
    --------
    total_count : Sum numset elements.
    trapz : Integration of numset values using the composite trapezoidal rule.
    difference : Calculate the n-th discrete differenceerence along given axis.

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    ``cumsum(a)[-1]`` may not be equal to ``total_count(a)`` for floating-point
    values since ``total_count`` may use a pairwise summation routine, reducing
    the roundoff-error. See `total_count` for more information.

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 229
query: How do I select the given columns and return the new KnowledgeFrame?
API_1: come_from_str(datastring, dtype=None, shape=None, offset=0, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from binary data

    Note that despite the name of this function it does not accept `str`
    instances.

    Parameters
    ----------
    datastring : bytes-like
        Buffer of binary data
    dtype : data-type, optional
        Valid dtype for all numsets
    shape : int or tuple of ints, optional
        Shape of each numset.
    offset : int, optional
        Position in the buffer to start reading from.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.


    Returns
    -------
    bn.recnumset
        Record numset view into the data in datastring. This will be readonly
        if `datastring` is readonly.

    See Also
    --------
    beatnum.frombuffer

    
API_2: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        
API_3: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        

idx: 230
query: According to the merge_fn, we zip the above two datapipes and keep the key True. Whatsmore, cycle the zipped datapipe three times.
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 231
query: Using IterableWrapper to the file url and HttpReader to read the file
API_1: add_concat(x1, x2):
    Return element-wise string concatenation for two numsets of str or unicode.

    Arrays `x1` and `x2` must have the same shape.

    Parameters
    ----------
    x1 : numset_like of str or unicode
        Ibnut numset.
    x2 : numset_like of str or unicode
        Ibnut numset.

    Returns
    -------
    add_concat : ndnumset
        Output numset of `string_` or `unicode_`, depending on ibnut types
        of the same shape as `x1` and `x2`.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: apd(object, /):Append object to the end of the list.

idx: 232
query: What I want is to clip the values of `col_1` between -2 to 2 if `col_0` is `a`. Using `clip` function in monkey.
API_1: hist_operation(a, bins=10, range=None, normlizatticreate_onesd=None, weights=None, density=None):
    Compute the hist_operation of a dataset.

    Parameters
    ----------
    a : numset_like
        Ibnut data. The hist_operation is computed over the convert_into_one_dimed numset.
    bins : int or sequence of scalars or str, optional
        If `bins` is an int, it defines the number of equal-width
        bins in the given range (10, by default). If `bins` is a
        sequence, it defines a monotonically increasing numset of bin edges,
        including the rightmost edge, allowing for non-uniform bin widths.

        .. versionadded:: 1.11.0

        If `bins` is a string, it defines the method used to calculate the
        optimal bin width, as defined by `hist_operation_bin_edges`.

    range : (float, float), optional
        The lower and upper range of the bins.  If not provided, range
        is simply ``(a.get_min(), a.get_max())``.  Values outside the range are
        ignored. The first element of the range must be less than or
        equal to the second. `range` affects the automatic bin
        computation as well. While bin width is computed to be optimal
        based on the actual data within `range`, the bin count will fill
        the entire range including portions containing no data.
    normlizattioned : bool, optional

        .. deprecated:: 1.6.0

        This is equivalent to the `density` argument, but produces incorrect
        results for unequal bin widths. It should not be used.

        .. versionchanged:: 1.15.0
            DeprecationWarnings are actually emitted.

    weights : numset_like, optional
        An numset of weights, of the same shape as `a`.  Each value in
        `a` only contributes its associated weight towards the bin count
        (instead of 1). If `density` is True, the weights are
        normlizattionalized, so that the integral of the density over the range
        remains 1.
    density : bool, optional
        If ``False``, the result will contain the number of samples in
        each bin. If ``True``, the result is the value of the
        probability *density* function at the bin, normlizattionalized such that
        the *integral* over the range is 1. Note that the sum of the
        hist_operation values will not be equal to 1 unless bins of unity
        width are chosen; it is not a probability *mass* function.

        Overrides the ``normlizattioned`` keyword if given.

    Returns
    -------
    hist : numset
        The values of the hist_operation. See `density` and `weights` for a
        description of the possible semantics.
    bin_edges : numset of dtype float
        Return the bin edges ``(length(hist)+1)``.


    See Also
    --------
    hist_operationdd, binoccurrence, find_sorted, digitize, hist_operation_bin_edges

    Notes
    -----
    All but the last (righthand-most) bin is half-open.  In other words,
    if `bins` is::

      [1, 2, 3, 4]

    then the first bin is ``[1, 2)`` (including 1, but excluding 2) and
    the second ``[2, 3)``.  The last bin, however, is ``[3, 4]``, which
    *includes* 4.


    
API_2: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    
API_3: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and



idx: 233
query: Can you please help me find the minimum value in this 2D number set? I have written some code to implement it. Additionally, I would also like to find the most frequent number in a number set called BeatNum. Can you please return that number to me?
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 234
query: Test if beatnum numset contains only zeros Return the result
API_1: full_value_func(shape, fill_value, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with `fill_value`.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    fill_value : scalar or numset_like
        Fill value.
    dtype : data-type, optional
        The desired data-type for the numset  The default, None, averages
         ``bn.numset(fill_value).dtype``.
    order : {'C', 'F'}, optional
        Whether to store multidimensional data in C- or Fortran-contiguous
        (row- or column-wise) order in memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of `fill_value` with the given shape, dtype, and order.

    See Also
    --------
    full_value_func_like : Return a new numset with shape of ibnut masked_fill with value.
    empty : Return a new uninitialized numset.
    create_ones : Return a new numset setting values to one.
    zeros : Return a new numset setting values to zero.

    
API_2: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_3: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    

idx: 235
query: create a beatnum numset composed of a list [[8, 7, 2], [5, 6, 1], [8, 2, 6]]
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 236
query: How can I add rows to a beatnum numset? Is there a beatnumthonic approach for this? Additionally, I am currently utilizing beatnum. I have a matrix `M` with dimensions 1*N and I am in need of obtaining a numset with N elements. Is there a more elegant method to achieve this?
API_1: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_2: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_3: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    

idx: 237
query: How to add items into a beatnum numset? add one element to each row using column stack operation.
API_1: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_2: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 238
query: How do I set the batch size to 32 and use the Adagrad optimizer?
API_1: asnumset(a, dtype=None, order=None):
    Convert the ibnut to a masked numset of the given data-type.

    No copy is performed if the ibnut is already an `ndnumset`. If `a` is
    a subclass of `MaskedArray`, a base class `MaskedArray` is returned.

    Parameters
    ----------
    a : numset_like
        Ibnut data, in any_condition form that can be converted to a masked numset. This
        includes lists, lists of tuples, tuples, tuples of tuples, tuples
        of lists, ndnumsets and masked numsets.
    dtype : dtype, optional
        By default, the data-type is inferred from the ibnut data.
    order : {'C', 'F'}, optional
        Whether to use row-major ('C') or column-major ('FORTRAN') memory
        representation.  Default is 'C'.

    Returns
    -------
    out : MaskedArray
        Masked numset interpretation of `a`.

    See Also
    --------
    asany_conditionnumset : Similar to `asnumset`, but conserves subclasses.

    
API_2: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_3: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        

idx: 239
query: Is there a more efficient way to collect sequential numerical data (in my case, beatnum numsets) than putting them in a list and then converting it to a beatnum.numset? I am creating a new object and copying the data. Is there a well-tested module that provides an expandable matrix data structure?
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: numset(obj, itemsize=None, copy=True, unicode=None, order=None):
    Create a `charnumset`.

    .. note::
       This class is provided for numnumset backward-compatibility.
       New code (not concerned with numnumset compatibility) should use
       numsets of type `string_` or `unicode_` and use the free functions
       in :mod:`beatnum.char <beatnum.core.defcharnumset>` for fast
       vectorisationd string operations instead.

    Versus a regular NumPy numset of type `str` or `unicode`, this
    class adds the following functionality:

      1) values automatically have whitespace removed from the end
         when indexed

      2) comparison operators automatically remove whitespace from the
         end when comparing values

      3) vectorisationd string operations are provided as methods
         (e.g. `str.endswith`) and infix operators (e.g. ``+, *, %``)

    Parameters
    ----------
    obj : numset of str or unicode-like

    itemsize : int, optional
        `itemsize` is the number of characters per scalar in the
        resulting numset.  If `itemsize` is None, and `obj` is an
        object numset or a Python list, the `itemsize` will be
        automatically deterget_mined.  If `itemsize` is provided and `obj`
        is of type str or unicode, then the `obj` string will be
        chunked into `itemsize` pieces.

    copy : bool, optional
        If true (default), then the object is copied.  Otherwise, a copy
        will only be made if __numset__ returns a copy, if obj is a
        nested sequence, or if a copy is needed to satisfy any_condition of the other
        requirements (`itemsize`, unicode, `order`, etc.).

    unicode : bool, optional
        When true, the resulting `charnumset` can contain Unicode
        characters, when false only 8-bit characters.  If unicode is
        None and `obj` is one of the following:

          - a `charnumset`,
          - an ndnumset of type `str` or `unicode`
          - a Python str or unicode object,

        then the unicode setting of the output numset will be
        automatically deterget_mined.

    order : {'C', 'F', 'A'}, optional
        Specify the order of the numset.  If order is 'C' (default), then the
        numset will be in C-contiguous order (last-index varies the
        fastest).  If order is 'F', then the returned numset
        will be in Fortran-contiguous order (first-index varies the
        fastest).  If order is 'A', then the returned numset may
        be in any_condition order (either C-, Fortran-contiguous, or even
        discontiguous).
    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 240
query: I'd like to calculate element-wise average between a, b and c.
API_1: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 241
query: How can the Euclidean distance be calculated with BeatNum?
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 242
query: I am using beatnum. I have a matrix `M` 1*N and I want to get an numset from with N elements. To achieve it, Does anyone know a more elegant way to get the result?
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_3: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        

idx: 243
query: Firstly, We need to find the minimun value of each column with axis 0, Then conduct subtract operation between each element of the column and the minimum value.
API_1: intersection1dim(ar1, ar2, assume_uniq=False, inverseert=False):
    Test whether each element of an numset is also present in a second
    numset.

    The output is always a masked numset. See `beatnum.intersection1dim` for more details.

    We recommend using :func:`isin` instead of `intersection1dim` for new code.

    See Also
    --------
    isin       : Version of this function that preserves the shape of ar1.
    beatnum.intersection1dim : Equivalent function for ndnumsets.

    Notes
    -----
    .. versionadded:: 1.4.0

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    

idx: 244
query: Read the URL using the HTTP protocol and parse the csv file as a dictionary.
API_1: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    
API_2: stack_col(x, *args, **params):pile_operation_col(*args, **kwargs)

Stack 1-D numsets as columns into a 2-D numset.

Take a sequence of 1-D numsets and pile_operation them as columns
to make a single 2-D numset. 2-D numsets are pile_operationed as-is,
just like with `hpile_operation`.  1-D numsets are turned into 2-D columns
first.

Parameters
----------
tup : sequence of 1-D or 2-D numsets.
    Arrays to pile_operation. All of them must have the same first dimension.

Returns
-------
pile_operationed : 2-D numset
    The numset formed by pile_operationing the given numsets.

See Also
--------
pile_operation, hpile_operation, vpile_operation, connect


API_3: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    

idx: 245
query: Is there a simple way of replacing all negative values in an numset with `0`? using a BeatNum function `filter_condition` to solve it.
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    
API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 246
query: How can I find the index of the maximum element in a BeatNum numset along the axis_value? Please return the result. Additionally, how can I determine the (row, column) index of the minimum element in a BeatNum numset or matrix? Please use the convert_index_or_arr() function.
API_1: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 247
query: Find nearest value in beatnum numset return the result
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: absolute(self, *args, **kwargs):Convenience fluent method for :py:func:`absolute`.

        The arguments are the same as for :py:func:`absolute`, with
        this numset as data.
        

idx: 248
query: Counting consecutive positive values in Python/monkey array I'm trying to count consecutive up days in equity return data; so if a positive day is 1 and a negative is 0, a list y=[0,0,1,1,1,0,0,1,0,1,1] should return z=[0,0,1,2,3,0,0,1,0,1,2]. Return the result
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_3: inverse(a):
    Compute the (multiplicative) inverseerse of a matrix.

    Given a square matrix `a`, return the matrix `ainverse` satisfying
    ``dot(a, ainverse) = dot(ainverse, a) = eye(a.shape[0])``.

    Parameters
    ----------
    a : (..., M, M) numset_like
        Matrix to be inverseerted.

    Returns
    -------
    ainverse : (..., M, M) ndnumset or matrix
        (Multiplicative) inverseerse of the matrix `a`.

    Raises
    ------
    LinAlgError
        If `a` is not square or inverseersion fails.

    See Also
    --------
    scipy.linalg.inverse : Similar function in SciPy.

    Notes
    -----

    .. versionadded:: 1.8.0

    Broadcasting rules apply, see the `beatnum.linalg` documentation for
    details.

    

idx: 249
query: How do I calculate the average values along the rows of a matrix, while excluding any NaN values from the calculation? If all the values in a row are NaNs, the average value should be NaN as well. Additionally, I need to find the minimum value in each column using axis 0, and then subtract that minimum value from each element in the column.
API_1: masked_fill(a, fill_value=None):
    Return ibnut as an numset with masked data replaced by a fill value.

    If `a` is not a `MaskedArray`, `a` itself is returned.
    If `a` is a `MaskedArray` and `fill_value` is None, `fill_value` is set to
    ``a.fill_value``.

    Parameters
    ----------
    a : MaskedArray or numset_like
        An ibnut object.
    fill_value : numset_like, optional.
        Can be scalar or non-scalar. If non-scalar, the
        resulting masked_fill numset should be broadcastable
        over ibnut numset. Default is None.

    Returns
    -------
    a : ndnumset
        The masked_fill numset.

    See Also
    --------
    remove_masked_data

    
API_2: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 250
query: I want to convert a table, represented as a list of lists, into a monkey KnowledgeFrame. The columns are ['one', 'two'] What is the best way to convert the columns to the appropriate types, in this case the 'two' column into floats?
API_1: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_2: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_3: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    

idx: 251
query: Find indices of elements equal to zero in a BeatNum numset Return the indices
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_3: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    

idx: 252
query: How can I convert a beatnum.ndnumset to a string and then convert it back to beatnum.ndnumset with the data type as an integer? Additionally, how can I convert integer numsets to string numsets in beatnum without any truncation?
API_1: come_from_str(datastring, dtype=None, shape=None, offset=0, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from binary data

    Note that despite the name of this function it does not accept `str`
    instances.

    Parameters
    ----------
    datastring : bytes-like
        Buffer of binary data
    dtype : data-type, optional
        Valid dtype for all numsets
    shape : int or tuple of ints, optional
        Shape of each numset.
    offset : int, optional
        Position in the buffer to start reading from.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.


    Returns
    -------
    bn.recnumset
        Record numset view into the data in datastring. This will be readonly
        if `datastring` is readonly.

    See Also
    --------
    beatnum.frombuffer

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 253
query: Convert a beatnum.ndnumset to string and convert it back to beatnum.ndnumset with dtype=int
API_1: come_from_str(datastring, dtype=None, shape=None, offset=0, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from binary data

    Note that despite the name of this function it does not accept `str`
    instances.

    Parameters
    ----------
    datastring : bytes-like
        Buffer of binary data
    dtype : data-type, optional
        Valid dtype for all numsets
    shape : int or tuple of ints, optional
        Shape of each numset.
    offset : int, optional
        Position in the buffer to start reading from.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.


    Returns
    -------
    bn.recnumset
        Record numset view into the data in datastring. This will be readonly
        if `datastring` is readonly.

    See Also
    --------
    beatnum.frombuffer

    
API_2: total(self, axis=None, out=None):
        Test whether total matrix elements along a given axis evaluate to True.

        Parameters
        ----------
        See `beatnum.total` for complete descriptions

        See Also
        --------
        beatnum.total

        Notes
        -----
        This is the same as `ndnumset.total`, but it returns a `matrix` object.

        
API_3: standard_op(self, axis=None, dtype=None, out=None, ddof=0):
        Return the standard deviation of the numset elements along the given axis.

        Refer to `beatnum.standard_op` for full_value_func documentation.

        See Also
        --------
        beatnum.standard_op

        Notes
        -----
        This is the same as `ndnumset.standard_op`, except that filter_condition an `ndnumset` would
        be returned, a `matrix` object is returned instead.

        

idx: 254
query: How do I create a beatnum numset of arbitrary shape 3x4 filled with all True?
API_1: logic_and_element_wise(a, b, *args, **kwargs):logic_and_element_wise(x1, x2, /, out=None, *, filter_condition=True, casting='same_kind', order='K', dtype=None, subok=True[, signature, extobj])

Compute the truth value of x1 AND x2 element-wise.

Parameters
----------
x1, x2 : numset_like
    Ibnut numsets.
    If ``x1.shape != x2.shape``, they must be broadcastable to a common
    shape (which becomes the shape of the output).
out : ndnumset, None, or tuple of ndnumset and None, optional
    A location into which the result is stored. If provided, it must have
    a shape that the ibnuts broadcast to. If not provided or None,
    a freshly-allocated numset is returned. A tuple (possible only as a
    keyword argument) must have length equal to the number of outputs.
filter_condition : numset_like, optional
    This condition is broadcast over the ibnut. At locations filter_condition the
    condition is True, the `out` numset will be set to the ufunc result.
    Elsefilter_condition, the `out` numset will retain its original value.
    Note that if an uninitialized `out` numset is created via the default
    ``out=None``, locations within it filter_condition the condition is False will
    remain uninitialized.
**kwargs
    For other keyword-only arguments, see the
    :ref:`ufunc docs <ufuncs.kwargs>`.

Returns
-------
y : ndnumset or bool
    Boolean result of the logical AND operation applied to the elements
    of `x1` and `x2`; the shape is deterget_mined by broadcasting.
    This is a scalar if both `x1` and `x2` are scalars.

See Also
--------
logical_or, logical_not, logical_xor
bitwise_and


API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: filter_condition(condition, x=None, y=None):filter_condition(condition, [x, y])
    Return elements chosen from `x` or `y` depending on `condition`.

    .. note::
        When only `condition` is provided, this function is a shorthand for
        ``bn.asnumset(condition).nonzero()``. The rest of this documentation
        covers only the case filter_condition all three arguments are provided.

    Parameters
    ----------
    condition : ndnumset
        Where True, yield `x`, otherwise yield `y`.
    x, y : ndnumset
        Values from which to choose. `x`, `y` and `condition` need to be
        broadcastable to some shape. `x` and `y` must have the same dtype.

    Returns
    -------
    out : ndnumset
        An numset with elements from `x` filter_condition `condition` is True, and elements
        from `y` elsefilter_condition.

    Notes
    -----
    If all the numsets are 1-D, `filter_condition` is equivalent to::

        [xv if c else yv
        for c, xv, yv in zip(condition, x, y)]

    

idx: 255
query: Perform a symmetric difference between two beatnum numsets. Don't convert the beatnum numset to a set to perform exclusive-or. Use seting_exclusive_or_one_dim directly.
API_1: convert_index_or_arr(indices, shape, order='C'):
    Converts a flat index or numset of flat indices into a tuple of coordinate numsets.

    Parameters:
    -------------
    indices : numset_like
            An integer numset whose elements are indices into the convert_into_one_dimed version of an numset of dimensions shape.
            Before version 1.6.0, this function accepted just one index value.
    shape : tuple of ints
            The shape of the numset to use for unasviewing indices.
    order : Only row-major is supported currently.

    Returns:
    -------------
    unasviewed_coords : ndnumset
            Each row in the ndnumset has the same shape as the indices numset.
            Each column in the ndnumset represents the unasviewled index

    
API_2: split_array(ary, indices_or_sections, axis=0):
    Split an numset into multiple sub-numsets.

    Please refer to the ``sep_split`` documentation.  The only differenceerence
    between these functions is that ``numset_sep_split`` allows
    `indices_or_sections` to be an integer that does *not* equally
    divide the axis. For an numset of length l that should be sep_split
    into n sections, it returns l % n sub-numsets of size l//n + 1
    and the rest of size l//n.

    See Also
    --------
    sep_split : Split numset into multiple sub-numsets of equal size.

    
API_3: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    

idx: 256
query: How can I create a beatnum numset with a shape of 3x4 that is filled with all True values? Additionally, how can I determine if a beatnum numset is empty or not? Please return the result as either True or False.
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_3: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    

idx: 257
query: I have a pandas dataframe I would like to se the diagonal to 0
API_1: pad_diagonal(a, val, wrap=False):Fill the main diagonal of the given numset of any_condition dimensionality.

    For an numset `a` with ``a.ndim >= 2``, the diagonal is the list of
    locations with indices ``a[i, ..., i]`` total identical. This function
    modifies the ibnut numset in-place, it does not return a value.

    Parameters
    ----------
    a : numset, at least 2-D.
      Array whose diagonal is to be masked_fill, it gets modified in-place.

    val : scalar or numset_like
      Value(s) to write on the diagonal. If `val` is scalar, the value is
      written along the diagonal. If numset-like, the convert_into_one_dimed `val` is
      written along the diagonal, duplicateing if necessary to fill total
      diagonal entries.

    wrap : bool
      For tall matrices in NumPy version up to 1.6.2, the
      diagonal "wrapped" after N columns. You can have this behavior
      with this option. This affects only tall matrices.

    See also
    --------
    diag_indices, diag_indices_from

    Notes
    -----
    .. versionadded:: 1.4.0

    This functionality can be obtained via `diag_indices`, but internally
    this version uses a much faster implementation that never constructs the
    indices and uses simple slicing.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: pile_operation(x, *args, **params):pile_operation(*args, **kwargs)

Join a sequence of numsets along a new axis.

The ``axis`` parameter specifies the index of the new axis in the
dimensions of the result. For example, if ``axis=0`` it will be the first
dimension and if ``axis=-1`` it will be the last dimension.

.. versionadded:: 1.10.0

Parameters
----------
numsets : sequence of numset_like
    Each numset must have the same shape.

axis : int, optional
    The axis in the result numset along which the ibnut numsets are pile_operationed.

out : ndnumset, optional
    If provided, the destination to place the result. The shape must be
    correct, matching that of what pile_operation would have returned if no
    out argument were specified.

Returns
-------
pile_operationed : ndnumset
    The pile_operationed numset has one more dimension than the ibnut numsets.

See Also
--------
connect : Join a sequence of numsets along an existing axis.
block : Assemble an nd-numset from nested lists of blocks.
sep_split : Split numset into a list of multiple sub-numsets of equal size.



idx: 258
query: Provide a code snippet to configure training and logging.
API_1: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_3: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 259
query: Is there a quick way to "sub-flatten" or flatten only some of the first dimensions in a beatnum numset? Given a beatnum numset of dimensions (50,100,25), the resultant dimensions would be (5000,25)
API_1: horizontal_stack(numsets):
    Stack numsets in sequence horizontally (column wise).
    This is equivalent to concatenation along the second axis,
    except for 1-D numsets filter_condition it connects along the first axis.
    Rebuilds numsets divided by hsep_split.
    This function makes most sense for numsets with up to 3 dimensions.
    For instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions connect,
    pile_operation and block provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the second axis, except 1-D numsets which can be any_condition length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets.

    
API_2: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    
API_3: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    

idx: 260
query: I have a pandas dataframe I would like to se the diagonal to 0
API_1: find_sorted(a, v, side='left', sorter=None):
    Find indices filter_condition elements should be sticked to maintain order.

    Find the indices into a sorted numset `a` such that, if the
    corresponding elements in `v` were sticked before the indices, the
    order of `a` would be preserved.

    Assuming that `a` is sorted:

    ======  ============================
    `side`  returned index `i` satisfies
    ======  ============================
    left    ``a[i-1] < v <= a[i]``
    right   ``a[i-1] <= v < a[i]``
    ======  ============================

    Parameters
    ----------
    a : 1-D numset_like
        Ibnut numset. If `sorter` is None, then it must be sorted in
        ascending order, otherwise `sorter` must be an numset of indices
        that sort it.
    v : numset_like
        Values to stick into `a`.
    side : {'left', 'right'}, optional
        If 'left', the index of the first suitable location found is given.
        If 'right', return the last such index.  If there is no suitable
        index, return either 0 or N (filter_condition N is the length of `a`).
    sorter : 1-D numset_like, optional
        Optional numset of integer indices that sort numset a into ascending
        order. They are typically the result of argsort.

        .. versionadded:: 1.7.0

    Returns
    -------
    indices : numset of ints
        Array of stickion points with the same shape as `v`.

    See Also
    --------
    sort : Return a sorted copy of an numset.
    hist_operation : Produce hist_operation from 1-D data.

    Notes
    -----
    Binary search is used to find the required stickion points.

    As of NumPy 1.4.0 `find_sorted` works with reality/complex numsets containing
    `nan` values. The enhanced sort order is documented in `sort`.

    This function uses the same algorithm as the builtin python `bisect.bisect_left`
    (``side='left'``) and `bisect.bisect_right` (``side='right'``) functions,
    which is also vectorisationd in the `v` argument.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: pile_operation(x, *args, **params):pile_operation(*args, **kwargs)

Join a sequence of numsets along a new axis.

The ``axis`` parameter specifies the index of the new axis in the
dimensions of the result. For example, if ``axis=0`` it will be the first
dimension and if ``axis=-1`` it will be the last dimension.

.. versionadded:: 1.10.0

Parameters
----------
numsets : sequence of numset_like
    Each numset must have the same shape.

axis : int, optional
    The axis in the result numset along which the ibnut numsets are pile_operationed.

out : ndnumset, optional
    If provided, the destination to place the result. The shape must be
    correct, matching that of what pile_operation would have returned if no
    out argument were specified.

Returns
-------
pile_operationed : ndnumset
    The pile_operationed numset has one more dimension than the ibnut numsets.

See Also
--------
connect : Join a sequence of numsets along an existing axis.
block : Assemble an nd-numset from nested lists of blocks.
sep_split : Split numset into a list of multiple sub-numsets of equal size.



idx: 261
query: I have two simple one-dimensional numsets in BeatNum. I should be able to connect them using beatnum.connect.
API_1: connect(numsets, axis=0):
    Concatenate a sequence of numsets along the given axis.

    Parameters
    ----------
    numsets : sequence of numset_like
        The numsets must have the same shape, except in the dimension
        corresponding to `axis` (the first, by default).
    axis : int, optional
        The axis along which the numsets will be joined. Default is 0.

    Returns
    -------
    result : MaskedArray
        The connectd numset with any_condition masked entries preserved.

    See Also
    --------
    beatnum.connect : Equivalent function in the top-level NumPy module.

    
API_2: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_3: stack_col(x, *args, **params):pile_operation_col(*args, **kwargs)

Stack 1-D numsets as columns into a 2-D numset.

Take a sequence of 1-D numsets and pile_operation them as columns
to make a single 2-D numset. 2-D numsets are pile_operationed as-is,
just like with `hpile_operation`.  1-D numsets are turned into 2-D columns
first.

Parameters
----------
tup : sequence of 1-D or 2-D numsets.
    Arrays to pile_operation. All of them must have the same first dimension.

Returns
-------
pile_operationed : 2-D numset
    The numset formed by pile_operationing the given numsets.

See Also
--------
pile_operation, hpile_operation, vpile_operation, connect



idx: 262
query: Averaging over every 3 elements of a beatnum numset I have a beatnum numset. I want to create a new numset which is the average over every consecutive triplet of elements. So the new numset will be a third of the size as the original. Return it
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 263
query: Inverse of a matrix using beatnum and return it. Ibnut: matrix: beatnum numset, shape (n, n) Output: inverse: beatnum numset, shape (n, n)
API_1: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_2: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_3: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    

idx: 264
query: Divide datapipes into 3 batches and discard if the last batch is not reached.
API_1: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 265
query: Show a code snippet to convert an ONNX model to OpenVINO model.
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: normlizattion(self, *args, **kwargs):Convenience fluent method for :py:func:`normlizattion`.

        The arguments are the same as for :py:func:`normlizattion`, with
        this numset as data.
        
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 266
query: List of numsets. Stack them using axis that is negative one .
API_1: vertical_stack(tup):
    Stack numsets in sequence vertically (row wise).

    This is equivalent to concatenation along the first axis after 1-D numsets
    of shape `(N,)` have been change_shape_tod to `(1,N)`. Rebuilds numsets divided by
    `vsep_split`.

    This function makes most sense for numsets with up to 3 dimensions. For
    instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions `connect`, `pile_operation` and
    `block` provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the first axis.
        1-D numsets must have the same length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets, will be at least 2-D.

    See Also
    --------
    connect : Join a sequence of numsets along an existing axis.
    pile_operation : Join a sequence of numsets along a new axis.
    block : Assemble an nd-numset from nested lists of blocks.
    hpile_operation : Stack numsets in sequence horizontally (column wise).
    dpile_operation : Stack numsets in sequence depth wise (along third axis).
    pile_operation_col : Stack 1-D numsets as columns into a 2-D numset.
    vsep_split : Split an numset into multiple sub-numsets vertically (row-wise).

    
API_2: come_from_arrays(numsetList, dtype=None, shape=None, formats=None, names=None, titles=None, aligned=False, byteorder=None):Create a record numset from a (flat) list of numsets

    Parameters
    ----------
    numsetList : list or tuple
        List of numset-like objects (such as lists, tuples,
        and ndnumsets).
    dtype : data-type, optional
        valid dtype for total numsets
    shape : int or tuple of ints, optional
        Shape of the resulting numset. If not provided, inferred from
        ``numsetList[0]``.
    formats, names, titles, aligned, byteorder :
        If `dtype` is ``None``, these arguments are passed to
        `beatnum.format_parser` to construct a dtype. See that function for
        detailed documentation.

    Returns
    -------
    bn.recnumset
        Record numset consisting of given numsetList columns.

    
API_3: pile_operation(x, *args, **params):pile_operation(*args, **kwargs)

Join a sequence of numsets along a new axis.

The ``axis`` parameter specifies the index of the new axis in the
dimensions of the result. For example, if ``axis=0`` it will be the first
dimension and if ``axis=-1`` it will be the last dimension.

.. versionadded:: 1.10.0

Parameters
----------
numsets : sequence of numset_like
    Each numset must have the same shape.

axis : int, optional
    The axis in the result numset along which the ibnut numsets are pile_operationed.

out : ndnumset, optional
    If provided, the destination to place the result. The shape must be
    correct, matching that of what pile_operation would have returned if no
    out argument were specified.

Returns
-------
pile_operationed : ndnumset
    The pile_operationed numset has one more dimension than the ibnut numsets.

See Also
--------
connect : Join a sequence of numsets along an existing axis.
block : Assemble an nd-numset from nested lists of blocks.
sep_split : Split numset into a list of multiple sub-numsets of equal size.



idx: 267
query: Is there a more elegant way to obtain a numset with N elements from a matrix `M` that is 1*N in size? Additionally, I would like to create a new numset by averaging every consecutive triplet of elements from a beatnum numset. This new numset should be one-third the size of the original. Could you please provide the solution?
API_1: asnumset(a, dtype=None, order=None):
    Convert the ibnut to a masked numset of the given data-type.

    No copy is performed if the ibnut is already an `ndnumset`. If `a` is
    a subclass of `MaskedArray`, a base class `MaskedArray` is returned.

    Parameters
    ----------
    a : numset_like
        Ibnut data, in any_condition form that can be converted to a masked numset. This
        includes lists, lists of tuples, tuples, tuples of tuples, tuples
        of lists, ndnumsets and masked numsets.
    dtype : dtype, optional
        By default, the data-type is inferred from the ibnut data.
    order : {'C', 'F'}, optional
        Whether to use row-major ('C') or column-major ('FORTRAN') memory
        representation.  Default is 'C'.

    Returns
    -------
    out : MaskedArray
        Masked numset interpretation of `a`.

    See Also
    --------
    asany_conditionnumset : Similar to `asnumset`, but conserves subclasses.

    
API_2: sqz(self, axis=None):
        Return a possibly change_shape_tod matrix.

        Refer to `beatnum.sqz` for more documentation.

        Parameters
        ----------
        axis : None or int or tuple of ints, optional
            Selects a subset of the axes of length one in the shape.
            If an axis is selected with shape entry greater than one,
            an error is raised.

        Returns
        -------
        sqzd : matrix
            The matrix, but as a (1, N) matrix if it had shape (N, 1).

        See Also
        --------
        beatnum.sqz : related function

        Notes
        -----
        If `m` has a single column then that column is returned
        as the single row of a matrix.  Otherwise `m` is returned.
        The returned matrix is always either `m` itself or a view into `m`.
        Supplying an axis keyword argument will not affect the returned matrix
        but it may cause an error to be raised.

        
API_3: convert_into_one_dim(self, order='C'):
        Return a convert_into_one_dimed copy of the matrix.

        All `N` elements of the matrix are placed into a single row.

        Parameters
        ----------
        order : {'C', 'F', 'A', 'K'}, optional
            'C' averages to convert_into_one_dim in row-major (C-style) order. 'F' averages to
            convert_into_one_dim in column-major (Fortran-style) order. 'A' averages to
            convert_into_one_dim in column-major order if `m` is Fortran *contiguous* in
            memory, row-major order otherwise. 'K' averages to convert_into_one_dim `m` in
            the order the elements occur in memory. The default is 'C'.

        Returns
        -------
        y : matrix
            A copy of the matrix, convert_into_one_dimed to a `(1, N)` matrix filter_condition `N`
            is the number of elements in the original matrix.

        See Also
        --------
        asview : Return a convert_into_one_dimed numset.
        flat : A 1-D flat iterator over the matrix.

        

idx: 268
query: Please find the indices of the k smallest values in a beatnum numset. Additionally, find the indices of a list of values in the same beatnum numset.
API_1: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: perform_partition(a, kth, axis=-1, kind='introselect', order=None):
    Perform an indirect partition along the given axis using the
    algorithm specified by the `kind` keyword. It returns an numset of
    indices of the same shape as `a` that index data along the given
    axis in partitioned order.

    .. versionadded:: 1.8.0

    Parameters
    ----------
    a : numset_like
        Array to sort.
    kth : int or sequence of ints
        Element index to partition by. The k-th element will be in its
        final sorted position and all smaller elements will be moved
        before it and all larger elements behind it. The order all
        elements in the partitions is undefined. If provided with a
        sequence of k-th it will partition all of them into their sorted
        position at once.
    axis : int or None, optional
        Axis along which to sort. The default is -1 (the last axis). If
        None, the convert_into_one_dimed numset is used.
    kind : {'introselect'}, optional
        Selection algorithm. Default is 'introselect'
    order : str or list of str, optional
        When `a` is an numset with fields defined, this argument
        specifies which fields to compare first, second, etc. A single
        field can be specified as a string, and not total fields need be
        specified, but unspecified fields will still be used, in the
        order in which they come up in the dtype, to break ties.

    Returns
    -------
    index_numset : ndnumset, int
        Array of indices that partition `a` along the specified axis.
        If `a` is one-dimensional, ``a[index_numset]`` yields a partitioned `a`.
        More generally, ``bn.take_along_axis(a, index_numset, axis=a)`` always
        yields the partitioned `a`, irrespective of dimensionality.

    See Also
    --------
    partition : Describes partition algorithms used.
    ndnumset.partition : Ibnlace partition.
    argsort : Full indirect sort.
    take_along_axis : Apply ``index_numset`` from perform_partition
                      to an numset as if by calling partition.

    Notes
    -----
    See `partition` for notes on the differenceerent selection algorithms.

    

idx: 269
query: I need a general way to flatten that numset into a single numset of N elements, with N=every float in all the sub-numsets.
API_1: horizontal_stack(numsets):
    Stack numsets in sequence horizontally (column wise).
    This is equivalent to concatenation along the second axis,
    except for 1-D numsets filter_condition it connects along the first axis.
    Rebuilds numsets divided by hsep_split.
    This function makes most sense for numsets with up to 3 dimensions.
    For instance, for pixel-data with a height (first axis), width (second axis),
    and r/g/b channels (third axis). The functions connect,
    pile_operation and block provide more general pile_operationing and concatenation operations.

    Parameters
    ----------
    tup : sequence of ndnumsets
        The numsets must have the same shape along all but the second axis, except 1-D numsets which can be any_condition length.

    Returns
    -------
    pile_operationed : ndnumset
        The numset formed by pile_operationing the given numsets.

    
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    

idx: 270
query: Convert beatnum numset type and values from Float64 to Float32
API_1: difference(a, n=1, axis=-1, prepend=<no value>, apd=<no value>):
    Calculate the n-th discrete differenceerence along the given axis.

    The first differenceerence is given by ``out[i] = a[i+1] - a[i]`` along
    the given axis, higher differenceerences are calculated by using `difference`
    recursively.

    Parameters
    ----------
    a : numset_like
        Ibnut numset
    n : int, optional
        The number of times values are differenceerenced. If zero, the ibnut
        is returned as-is.
    axis : int, optional
        The axis along which the differenceerence is taken, default is the
        last axis.
    prepend, apd : numset_like, optional
        Values to prepend or apd to `a` along axis prior to
        perforget_ming the differenceerence.  Scalar values are expanded to
        numsets with length 1 in the direction of axis and the shape
        of the ibnut numset in along all other axes.  Otherwise the
        dimension and shape must match `a` except along axis.

        .. versionadded:: 1.16.0

    Returns
    -------
    difference : ndnumset
        The n-th differenceerences. The shape of the output is the same as `a`
        except along `axis` filter_condition the dimension is smaller by `n`. The
        type of the output is the same as the type of the differenceerence
        between any_condition two elements of `a`. This is the same as the type of
        `a` in most cases. A notable exception is `datetime64`, which
        results in a `timedelta64` output numset.

    See Also
    --------
    gradient, edifference1d, cumsum

    Notes
    -----
    Type is preserved for boolean numsets, so the result will contain
    `False` when consecutive elements are the same and `True` when they
    differenceer.

    For unsigned integer numsets, the results will also be unsigned. This
    should not be surprising, as the result is consistent with
    calculating the differenceerence directly:

    >>> u8_arr = bn.numset([1, 0], dtype=bn.uint8)
    >>> bn.difference(u8_arr)
    numset([255], dtype=uint8)
    >>> u8_arr[1,...] - u8_arr[0,...]
    255

    If this is not desirable, then the numset should be cast to a larger
    integer type first:

    >>> i16_arr = u8_arr.convert_type(bn.int16)
    >>> bn.difference(i16_arr)
    numset([-1], dtype=int16)

    
API_2: average(a, axis=None, dtype=None, out=None, keepdims=False):
    Compute the arithmetic average along the specified axis.
    Returns the average of the numset elements.
    The average is taken over the convert_into_one_dimed numset by default, otherwise over the specified axis.

    Parameters
    ----------
    a : ndnumset
        ndnumset containing numbers whose average is desired.
    axis : None or int or tuple of ints, optional
        Axis or axes along which the averages are computed. The default is to compute the average of the convert_into_one_dimed numset.
        If this is a tuple of ints, a average is performed over multiple axes,
        instead of a single axis or all the axes as before.
    dtype : data-type, optional
        Type to use in computing the average. For integer ibnuts, the default is float32;
        for floating point ibnuts, it is the same as the ibnut dtype.
    out : ndnumset, optional
        Alternate output numset in which to place the result. The default is None; if provided,
        it must have the same shape and type as the expected output.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left in the result
        as dimensions with size one. With this option, the result will broadcast correctly
        against the ibnut numset.
        If the default value is passed, then keepdims will not be passed through to the average
        method of sub-classes of ndnumset, however any_condition non-default value will be. If the sub-class
        method does not implement keepdims any_condition exceptions will be raised.

    Returns
    -------
    m : ndnumset, see dtype parameter above
        If out=None, returns a new numset containing the average values,
        otherwise a reference to the output numset is returned.

    Notes
    -----
    This function differenceers from the original `beatnum.average
    <https://docs.scipy.org/doc/beatnum/reference/generated/beatnum.average.html>`_ in
    the following way(s):
    - only ndnumset is accepted as valid ibnut, python iterables or scalar is not supported
    - default data type for integer ibnut is float32

    
API_3: total_count(a, axis=None, dtype=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Sum of numset elements over a given axis.

    Parameters
    ----------
    a : numset_like
        Elements to sum.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a sum is performed.  The default,
        axis=None, will sum total of the elements of the ibnut numset.  If
        axis is negative it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If axis is a tuple of ints, a total_count is performed on total of the axes
        specified in the tuple instead of a single axis or total the axes as
        before.
    dtype : dtype, optional
        The type of the returned numset and of the accumulator in which the
        elements are summed.  The dtype of `a` is used by default unless `a`
        has an integer dtype of less precision than the default platform
        integer.  In that case, if `a` is signed then the platform integer
        is used while if `a` is unsigned then an unsigned integer of the
        same precision as the platform integer is used.
    out : ndnumset, optional
        Alternative output numset in which to place the result. It must have
        the same shape as the expected output, but the type of the output
        values will be cast if necessary.
    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `total_count` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.
    initial : scalar, optional
        Starting value for the sum. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to include in the total_count. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    total_count_along_axis : ndnumset
        An numset with the same shape as `a`, with the specified
        axis removed.   If `a` is a 0-d numset, or if `axis` is None, a scalar
        is returned.  If an output numset is specified, a reference to
        `out` is returned.

    See Also
    --------
    ndnumset.total_count : Equivalent method.

    add_concat.reduce : Equivalent functionality of `add_concat`.

    cumsum : Cumulative sum of numset elements.

    trapz : Integration of numset values using the composite trapezoidal rule.

    average, average

    Notes
    -----
    Arithmetic is modular when using integer types, and no error is
    raised on overflow.

    The sum of an empty numset is the neutral element 0:

    >>> bn.total_count([])
    0.0

    For floating point numbers the numerical precision of sum (and
    ``bn.add_concat.reduce``) is in general limited by directly add_concating each number
    individutotaly to the result causing rounding errors in every step.
    However, often beatnum will use a  numerically better approach (partial
    pairwise summation) leading to improved precision in many_condition use-cases.
    This improved precision is always provided when no ``axis`` is given.
    When ``axis`` is given, it will depend on which axis is summed.
    Technically, to provide the best speed possible, the improved precision
    is only used when the summation is along the fast axis in memory.
    Note that the exact precision may vary depending on other parameters.
    In contrast to NumPy, Python's ``math.fsum`` function uses a slower but
    more precise approach to summation.
    Especially when summing a large number of lower precision floating point
    numbers, such as ``float32``, numerical errors can become significant.
    In such cases it can be advisable to use `dtype="float64"` to use a higher
    precision for the output.

    

idx: 271
query: How do I set the batch size and dataset path for training?
API_1: absolute(self, *args, **kwargs):Convenience fluent method for :py:func:`absolute`.

        The arguments are the same as for :py:func:`absolute`, with
        this numset as data.
        
API_2: uniq(ar, return_index=False, return_inverse=False, return_counts=False, axis=None):
    Find the uniq elements of an numset.

    Returns the sorted uniq elements of an numset. There are three optional
    outputs in addition to the uniq elements:

    * the indices of the ibnut numset that give the uniq values
    * the indices of the uniq numset that reconstruct the ibnut numset
    * the number of times each uniq value comes up in the ibnut numset

    Parameters
    ----------
    ar : numset_like
        Ibnut numset. Unless `axis` is specified, this will be convert_into_one_dimed if it
        is not already 1-D.
    return_index : bool, optional
        If True, also return the indices of `ar` (along the specified axis,
        if provided, or in the convert_into_one_dimed numset) that result in the uniq numset.
    return_inverseerse : bool, optional
        If True, also return the indices of the uniq numset (for the specified
        axis, if provided) that can be used to reconstruct `ar`.
    return_counts : bool, optional
        If True, also return the number of times each uniq item appears
        in `ar`.

        .. versionadded:: 1.9.0

    axis : int or None, optional
        The axis to operate on. If None, `ar` will be convert_into_one_dimed. If an integer,
        the subnumsets indexed by the given axis will be convert_into_one_dimed and treated
        as the elements of a 1-D numset with the dimension of the given axis,
        see the notes for more details.  Object numsets or structured numsets
        that contain objects are not supported if the `axis` kwarg is used. The
        default is None.

        .. versionadded:: 1.13.0

    Returns
    -------
    uniq : ndnumset
        The sorted uniq values.
    uniq_indices : ndnumset, optional
        The indices of the first occurrences of the uniq values in the
        original numset. Only provided if `return_index` is True.
    uniq_inverseerse : ndnumset, optional
        The indices to reconstruct the original numset from the
        uniq numset. Only provided if `return_inverseerse` is True.
    uniq_counts : ndnumset, optional
        The number of times each of the uniq values comes up in the
        original numset. Only provided if `return_counts` is True.

        .. versionadded:: 1.9.0

    See Also
    --------
    beatnum.lib.numsetsetops : Module with a number of other functions for
                            perforget_ming set operations on numsets.
    duplicate : Repeat elements of an numset.

    Notes
    -----
    When an axis is specified the subnumsets indexed by the axis are sorted.
    This is done by making the specified axis the first dimension of the numset
    (move the axis to the first dimension to keep the order of the other axes)
    and then convert_into_one_diget_ming the subnumsets in C order. The convert_into_one_dimed subnumsets are
    then viewed as a structured type with each element given a label, with the
    effect that we end up with a 1-D numset of structured types that can be
    treated in the same way as any_condition other 1-D numset. The result is that the
    convert_into_one_dimed subnumsets are sorted in lexicographic order starting with the
    first element.

    .. versionchanged: NumPy 1.21
        If nan values are in the ibnut numset, a single nan is put
        to the end of the sorted uniq values.

        Also for complex numsets all NaN values are considered equivalent
        (no matter whether the NaN is in the reality or imaginaryinary part).
        As the representant for the returned numset the smallest one in the
        lexicographical order is chosen - see bn.sort for how the lexicographical
        order is defined for complex numsets.

    
API_3: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    

idx: 272
query: I would like to add a new column C that is the sum value of A and B cell.
API_1: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    
API_2: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_3: remove_operation(arr, obj, axis=None):
    Return a new numset with sub-numsets along an axis remove_operationd. For a one
    dimensional numset, this returns those entries not returned by
    `arr[obj]`.

    Parameters
    ----------
    arr : numset_like
        Ibnut numset.
    obj : piece, int or numset of ints
        Indicate indices of sub-numsets to remove along the specified axis.

        .. versionchanged:: 1.19.0
            Boolean indices are now treated as a mask of elements to remove,
            rather than being cast to the integers 0 and 1.

    axis : int, optional
        The axis along which to remove_operation the subnumset defined by `obj`.
        If `axis` is None, `obj` is applied to the convert_into_one_dimed numset.

    Returns
    -------
    out : ndnumset
        A copy of `arr` with the elements specified by `obj` removed. Note
        that `remove_operation` does not occur in-place. If `axis` is None, `out` is
        a convert_into_one_dimed numset.

    See Also
    --------
    stick : Insert elements into an numset.
    apd : Append elements at the end of an numset.

    Notes
    -----
    Often it is preferable to use a boolean mask. For example:

    >>> arr = bn.arr_range(12) + 1
    >>> mask = bn.create_ones(len(arr), dtype=bool)
    >>> mask[[0,2,4]] = False
    >>> result = arr[mask,...]

    Is equivalent to `bn.remove_operation(arr, [0,2,4], axis=0)`, but allows further
    use of `mask`.

    

idx: 273
query: How to get the index of a maximum element in a BeatNum numset along axis_value? Return the result
API_1: get_argmin_value(a, axis=None, out=None):
    Returns the indices of the get_minimum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_min_value, get_argget_max
    aget_min : The get_minimum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_min_value to an numset as if by calling get_min.

    Notes
    -----
    In case of multiple occurrences of the get_minimum values, the indices
    corresponding to the first occurrence are returned.

    
API_2: get_argmax(a, axis=None, out=None):
    Returns the indices of the get_maximum values along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut numset.
    axis : int, optional
        By default, the index is into the convert_into_one_dimed numset, otherwise
        along the specified axis.
    out : numset, optional
        If provided, the result will be sticked into this numset. It should
        be of the appropriate shape and dtype.

    Returns
    -------
    index_numset : ndnumset of ints
        Array of indices into the numset. It has the same shape as `a.shape`
        with the dimension along `axis` removed.

    See Also
    --------
    ndnumset.get_argget_max, get_argget_min_value
    aget_max : The get_maximum value along a given axis.
    convert_index_or_arr : Convert a flat index into an index tuple.
    take_along_axis : Apply ``bn.expand_dims(index_numset, axis)``
                      from get_argget_max to an numset as if by calling get_max.

    Notes
    -----
    In case of multiple occurrences of the get_maximum values, the indices
    corresponding to the first occurrence are returned.

    
API_3: get_min(a, axis=None, out=None, keepdims=<no value>, initial=<no value>, where=<no value>):
    Return the get_minimum of an numset or get_minimum along an axis.

    Parameters
    ----------
    a : numset_like
        Ibnut data.
    axis : None or int or tuple of ints, optional
        Axis or axes along which to operate.  By default, convert_into_one_dimed ibnut is
        used.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, the get_minimum is selected over multiple axes,
        instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternative output numset in which to place the result.  Must
        be of the same shape and buffer length as the expected output.
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `aget_min` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    initial : scalar, optional
        The get_maximum value of an output element. Must be present to allow
        computation on empty piece. See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.15.0

    filter_condition : numset_like of bool, optional
        Elements to compare for the get_minimum. See `~beatnum.ufunc.reduce`
        for details.

        .. versionadded:: 1.17.0

    Returns
    -------
    aget_min : ndnumset or scalar
        Minimum of `a`. If `axis` is None, the result is a scalar value.
        If `axis` is given, the result is an numset of dimension
        ``a.ndim - 1``.

    See Also
    --------
    aget_max :
        The get_maximum value of an numset along a given axis, propagating any_condition NaNs.
    nanget_min :
        The get_minimum value of an numset along a given axis, ignoring any_condition NaNs.
    get_minimum :
        Element-wise get_minimum of two numsets, propagating any_condition NaNs.
    fget_min :
        Element-wise get_minimum of two numsets, ignoring any_condition NaNs.
    get_argget_min_value :
        Return the indices of the get_minimum values.

    nanget_max, get_maximum, fget_max

    Notes
    -----
    NaN values are propagated, that is if at least one item is NaN, the
    corresponding get_min value will be NaN as well. To ignore NaN values
    (MATLAB behavior), please use nanget_min.

    Don't use `aget_min` for element-wise comparison of 2 numsets; when
    ``a.shape[0]`` is 2, ``get_minimum(a[0], a[1])`` is faster than
    ``aget_min(a, axis=0)``.

    

idx: 274
query: Partition numset into 3 chunks with Beatnum
API_1: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_2: create_ones(shape, dtype=None, order='C', *, like=None):
    Return a new numset of given shape and type, masked_fill with create_ones.

    Parameters
    ----------
    shape : int or sequence of ints
        Shape of the new numset, e.g., ``(2, 3)`` or ``2``.
    dtype : data-type, optional
        The desired data-type for the numset, e.g., `beatnum.int8`.  Default is
        `beatnum.float64`.
    order : {'C', 'F'}, optional, default: C
        Whether to store multi-dimensional data in row-major
        (C-style) or column-major (Fortran-style) order in
        memory.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    out : ndnumset
        Array of create_ones with the given shape, dtype, and order.

    See Also
    --------
    create_ones_like : Return an numset of create_ones with shape and type of ibnut.
    empty : Return a new uninitialized numset.
    zeros : Return a new numset setting values to zero.
    full_value_func : Return a new numset of given shape masked_fill with value.


    
API_3: split_array(ary, indices_or_sections, axis=0):
    Split an numset into multiple sub-numsets.

    Please refer to the ``sep_split`` documentation.  The only differenceerence
    between these functions is that ``numset_sep_split`` allows
    `indices_or_sections` to be an integer that does *not* equally
    divide the axis. For an numset of length l that should be sep_split
    into n sections, it returns l % n sub-numsets of size l//n + 1
    and the rest of size l//n.

    See Also
    --------
    sep_split : Split numset into multiple sub-numsets of equal size.

    

idx: 275
query: How to add a new column to an existing KnowledgeFrame? I would like to add a new column data with the column name, to the existing knowledgeframe
API_1: any_condition(a, axis=None, out=None, keepdims=<no value>, *, where=<no value>):
    Test whether any_condition numset element along a given axis evaluates to True.

    Returns single boolean unless `axis` is not ``None``

    Parameters
    ----------
    a : numset_like
        Ibnut numset or object that can be converted to an numset.
    axis : None or int or tuple of ints, optional
        Axis or axes along which a logical OR reduction is performed.
        The default (``axis=None``) is to perform a logical OR over all
        the dimensions of the ibnut numset. `axis` may be negative, in
        which case it counts from the last to the first axis.

        .. versionadded:: 1.7.0

        If this is a tuple of ints, a reduction is performed on multiple
        axes, instead of a single axis or all the axes as before.
    out : ndnumset, optional
        Alternate output numset in which to place the result.  It must have
        the same shape as the expected output and its type is preserved
        (e.g., if it is of type float, then it will remain so, returning
        1.0 for True and 0.0 for False, regardless of the type of `a`).
        See :ref:`ufuncs-output-type` for more details.

    keepdims : bool, optional
        If this is set to True, the axes which are reduced are left
        in the result as dimensions with size one. With this option,
        the result will broadcast correctly against the ibnut numset.

        If the default value is passed, then `keepdims` will not be
        passed through to the `any_condition` method of sub-classes of
        `ndnumset`, however any_condition non-default value will be.  If the
        sub-class' method does not implement `keepdims` any_condition
        exceptions will be raised.

    filter_condition : numset_like of bool, optional
        Elements to include in checking for any_condition `True` values.
        See `~beatnum.ufunc.reduce` for details.

        .. versionadded:: 1.20.0

    Returns
    -------
    any_condition : bool or ndnumset
        A new boolean or `ndnumset` is returned unless `out` is specified,
        in which case a reference to `out` is returned.

    See Also
    --------
    ndnumset.any_condition : equivalent method

    total : Test whether total elements along a given axis evaluate to True.

    Notes
    -----
    Not a Number (NaN), positive infinity and negative infinity evaluate
    to `True` because these are not equal to zero.

    
API_2: change_shape_to(a, newshape, order='C'):
    Gives a new shape to an numset without changing its data.

    Parameters
    ----------
    a : numset_like
        Array to be change_shape_tod.
    newshape : int or tuple of ints
        The new shape should be compatible with the original shape. If
        an integer, then the result will be a 1-D numset of that length.
        One shape dimension can be -1. In this case, the value is
        inferred from the length of the numset and remaining dimensions.
    order : {'C', 'F', 'A'}, optional
        Read the elements of `a` using this index order, and place the
        elements into the change_shape_tod numset using this index order.  'C'
        averages to read / write the elements using C-like index order,
        with the last axis index changing fastest, back to the first
        axis index changing slowest. 'F' averages to read / write the
        elements using Fortran-like index order, with the first index
        changing fastest, and the last index changing slowest. Note that
        the 'C' and 'F' options take no account of the memory layout of
        the underlying numset, and only refer to the order of indexing.
        'A' averages to read / write the elements in Fortran-like index
        order if `a` is Fortran *contiguous* in memory, C-like order
        otherwise.

    Returns
    -------
    change_shape_tod_numset : ndnumset
        This will be a new view object if possible; otherwise, it will
        be a copy.  Note there is no guarantee of the *memory layout* (C- or
        Fortran- contiguous) of the returned numset.

    See Also
    --------
    ndnumset.change_shape_to : Equivalent method.

    Notes
    -----
    It is not always possible to change the shape of an numset without
    copying the data. If you want an error to be raised when the data is copied,
    you should assign the new shape to the shape attribute of the numset::

     >>> a = bn.zeros((10, 2))

     # A switching_places makes the numset non-contiguous
     >>> b = a.T

     # Taking a view makes it possible to modify the shape without modifying
     # the initial object.
     >>> c = b.view()
     >>> c.shape = (20)
     Traceback (most recent call last):
        ...
     AttributeError: Incompatible shape for in-place modification. Use
     `.change_shape_to()` to make a copy with the desired shape.

    The `order` keyword gives the index ordering both for *fetching* the values
    from `a`, and then *placing* the values into the output numset.
    For example, let's say you have an numset:

    >>> a = bn.arr_range(6).change_shape_to((3, 2))
    >>> a
    numset([[0, 1],
           [2, 3],
           [4, 5]])

    You can think of reshaping as first asviewing the numset (using the given
    index order), then sticking the elements from the asviewed numset into the
    new numset using the same kind of index ordering as was used for the
    asviewing.

    >>> bn.change_shape_to(a, (2, 3)) # C-like index ordering
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(bn.asview(a), (2, 3)) # equivalent to C asview then C change_shape_to
    numset([[0, 1, 2],
           [3, 4, 5]])
    >>> bn.change_shape_to(a, (2, 3), order='F') # Fortran-like index ordering
    numset([[0, 4, 3],
           [2, 1, 5]])
    >>> bn.change_shape_to(bn.asview(a, order='F'), (2, 3), order='F')
    numset([[0, 4, 3],
           [2, 1, 5]])

    
API_3: vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False, signature=None):
    vectorisation(pyfunc, otypes=None, doc=None, excluded=None, cache=False,
              signature=None)

    Generalized function class.

    Define a vectorisationd function which takes a nested sequence of objects or
    beatnum numsets as ibnuts and returns a single beatnum numset or a tuple of beatnum
    numsets. The vectorisationd function evaluates `pyfunc` over successive tuples
    of the ibnut numsets like the python map function, except it uses the
    broadcasting rules of beatnum.

    The data type of the output of `vectorisationd` is deterget_mined by calling
    the function with the first element of the ibnut.  This can be avoided
    by specifying the `otypes` argument.

    Parameters
    ----------
    pyfunc : callable
        A python function or method.
    otypes : str or list of dtypes, optional
        The output data type. It must be specified as either a string of
        typecode characters or a list of data type specifiers. There should
        be one data type specifier for each output.
    doc : str, optional
        The docstring for the function. If None, the docstring will be the
        ``pyfunc.__doc__``.
    excluded : set, optional
        Set of strings or integers representing the positional or keyword
        arguments for which the function will not be vectorisationd.  These will be
        passed directly to `pyfunc` unmodified.

        .. versionadded:: 1.7.0

    cache : bool, optional
        If `True`, then cache the first function call that deterget_mines the number
        of outputs if `otypes` is not provided.

        .. versionadded:: 1.7.0

    signature : string, optional
        Generalized universal function signature, e.g., ``(m,n),(n)->(m)`` for
        vectorisationd matrix-vector multiplication. If provided, ``pyfunc`` will
        be called with (and expected to return) numsets with shapes given by the
        size of corresponding core dimensions. By default, ``pyfunc`` is
        assumed to take scalars as ibnut and output.

        .. versionadded:: 1.12.0

    Returns
    -------
    vectorisationd : callable
        Vectorized function.

    See Also
    --------
    frompyfunc : Takes an arbitrary Python function and returns a ufunc

    Notes
    -----
    The `vectorisation` function is provided primarily for convenience, not for
    performance. The implementation is essentially a for loop.

    If `otypes` is not specified, then a call to the function with the
    first argument will be used to deterget_mine the number of outputs.  The
    results of this call will be cached if `cache` is `True` to prevent
    calling the function twice.  However, to implement the cache, the
    original function must be wrapped which will slow down subsequent
    calls, so only do this if your function is expensive.

    The new keyword argument interface and `excluded` argument support
    further degrades performance.

    References
    ----------
    .. [1] :doc:`/reference/c-api/generalized-ufuncs`

    

idx: 276
query: Prepend element to beatnum numset Return the numset
API_1: arr_range(*args, **params):arr_range([start,] stop[, step,], dtype=None, *, like=None)

    Return evenly spaced values within a given interval.

    Values are generated within the half-open interval ``[start, stop)``
    (in other words, the interval including `start` but excluding `stop`).
    For integer arguments the function is equivalent to the Python built-in
    `range` function, but returns an ndnumset rather than a list.

    When using a non-integer step, such as 0.1, the results will often not
    be consistent.  It is better to use `beatnum.linspace` for these cases.

    Parameters
    ----------
    start : integer or reality, optional
        Start of interval.  The interval includes this value.  The default
        start value is 0.
    stop : integer or reality
        End of interval.  The interval does not include this value, except
        in some cases filter_condition `step` is not an integer and floating point
        round-off affects the length of `out`.
    step : integer or reality, optional
        Spacing between values.  For any_condition output `out`, this is the distance
        between two adjacent values, ``out[i+1] - out[i]``.  The default
        step size is 1.  If `step` is specified as a position argument,
        `start` must also be given.
    dtype : dtype
        The type of the output numset.  If `dtype` is not given, infer the data
        type from the other ibnut arguments.
    like : numset_like
        Reference object to allow the creation of numsets which are not
        NumPy numsets. If an numset-like passed in as ``like`` supports
        the ``__numset_function__`` protocol, the result will be defined
        by it. In this case, it ensures the creation of an numset object
        compatible with that passed in via this argument.

        .. versionadded:: 1.20.0

    Returns
    -------
    arr_range : ndnumset
        Array of evenly spaced values.

        For floating point arguments, the length of the result is
        ``ceil((stop - start)/step)``.  Because of floating point overflow,
        this rule may result in the last element of `out` being greater
        than `stop`.

    See Also
    --------
    beatnum.linspace : Evenly spaced numbers with careful handling of endpoints.
    beatnum.ogrid: Arrays of evenly spaced numbers in N-dimensions.
    beatnum.mgrid: Grid-shaped numsets of evenly spaced numbers in N-dimensions.

    
API_2: intersection1dim(ar1, ar2, assume_uniq=False, inverseert=False):
    Test whether each element of an numset is also present in a second
    numset.

    The output is always a masked numset. See `beatnum.intersection1dim` for more details.

    We recommend using :func:`isin` instead of `intersection1dim` for new code.

    See Also
    --------
    isin       : Version of this function that preserves the shape of ar1.
    beatnum.intersection1dim : Equivalent function for ndnumsets.

    Notes
    -----
    .. versionadded:: 1.4.0

    
API_3: stick(index, object, /):Insert object before index.

