idx: 0
query: Can you please provide a solution to remove columns with duplicate names? Additionally, I would like to convert all column headers in my monkey data frame to lowercase. Please return the modified data frame.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        

idx: 1
query: What is the ceiling of a monkey collection? Please return the result. Also, please drop consecutive duplicates and return the result.
API_1: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        
API_2: ceiling(self, *args, **kwargs):
Perform ceiling operation on the data to the specified `freq`.

Parameters
----------
freq : str or Offset
    The frequency level to ceiling the index to. Must be a fixed
    frequency like 'S' (second) not 'ME' (month end). See
    :ref:`frequency aliases <timecollections.offset_aliases>` for
    a list of possible `freq` values.
ambiguous : 'infer', bool-ndarray, 'NaT', default 'raise'
    Only relevant for DatetimeIndex:

    - 'infer' will attempt to infer ftotal_all dst-transition hours based on
      order
    - bool-ndarray where True signifies a DST time, False designates
      a non-DST time (note that this flag is only applicable for
      ambiguous times)
    - 'NaT' will return NaT where there are ambiguous times
    - 'raise' will raise an AmbiguousTimeError if there are ambiguous
      times.

nonexistent : 'shifting_forward', 'shifting_backward', 'NaT', timedelta, default 'raise'
    A nonexistent time does not exist in a particular timezone
    where clocks moved forward due to DST.

    - 'shifting_forward' will shifting the nonexistent time forward to the
      closest existing time
    - 'shifting_backward' will shifting the nonexistent time backward to the
      closest existing time
    - 'NaT' will return NaT where there are nonexistent times
    - timedelta objects will shifting nonexistent times by the timedelta
    - 'raise' will raise an NonExistentTimeError if there are
      nonexistent times.

Returns
-------
DatetimeIndex, TimedeltaIndex, or Collections
    Index of the same type for a DatetimeIndex or TimedeltaIndex,
    or a Collections with the same index for a Collections.

Raises
------
ValueError if the `freq` cannot be converted.


API_3: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        

idx: 2
query: I have a knowledgeframe in monkey where each column has different value range. Any idea how I can normalize the columns of this knowledgeframe where each value is between 0 and 1?
API_1: get_max(self, axis=None, skipna: 'bool' = True, *args, **kwargs):
        Return the getting_maximum value of the Index.

        Parameters
        ----------
        axis : int, optional
            For compatibility with NumPy. Only 0 or None are total_allowed.
        skipna : bool, default True
            Exclude NA/null values when showing the result.
        *args, **kwargs
            Additional arguments and keywords for compatibility with NumPy.

        Returns
        -------
        scalar
            Maximum value.

        See Also
        --------
        Index.getting_min : Return the getting_minimum value in an Index.
        Collections.getting_max : Return the getting_maximum value in a Collections.
        KnowledgeFrame.getting_max : Return the getting_maximum values in a KnowledgeFrame.

        
API_2: get_min(self, *, skipna=True, **kwargs):
        The getting_minimum value of the object.

        Only ordered `Categoricals` have a getting_minimum!

        .. versionchanged:: 1.0.0

           Returns an NA value on empty arrays

        Raises
        ------
        TypeError
            If the `Categorical` is not `ordered`.

        Returns
        -------
        getting_min : the getting_minimum of this `Categorical`
        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 3
query: Round a single column `A` Return the knowledgeframe
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: conduct_map(self, func: 'PythonFuncType', na_action: 'str | None' = None, **kwargs) -> 'KnowledgeFrame':
        Apply a function to a Dataframe elementwise.

        This method applies a function that accepts and returns a scalar
        to every element of a KnowledgeFrame.

        Parameters
        ----------
        func : ctotal_allable
            Python function, returns a single value from a single value.
        na_action : {None, 'ignore'}, default None
            If ‘ignore’, propagate NaN values, without passing them to func.

            .. versionadded:: 1.2

        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

            .. versionadded:: 1.3.0

        Returns
        -------
        KnowledgeFrame
            Transformed KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employ : Apply a function along input axis of KnowledgeFrame.

        

idx: 4
query: I want to configure augmentation settings with RandomCrop, Resize, and ColorJitter.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 5
query: I want to create a knowledgeframe with one of the column as a list or array. After you assign a list like or array like value to the columns, the column should be considered as type object Now I want to assign the emails to first row and the 'Email' column
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 6
query: How can I remove rows from a knowledgeframe if the value in column2 is 0 and the row index is odd? Additionally, how can I remove duplicate rows from a knowledgeframe based on the values in column `col1`, keeping only the row with the last value in column `col2`? Please provide the final knowledgeframe after performing these operations.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 7
query: How can I select rows from a KnowledgeFrame called kf based on the values in a specific column? I want to return the rows where the column named `col_name` has values that are in an iterable called `values`. After that, how can I select the desired columns and return the resulting KnowledgeFrame?
API_1: incontain(self, values) -> 'np.ndarray': Return a boolean array where True if the value is contained in the passed values.
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 8
query: Given that all the knowledgeframes have the same columns, you can simply concat them: return the concated knowledgeframe
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 9
query: What is the best way to do a grouper on a Monkey knowledgeframe, but exclude some columns from that grouper? I want to grouper the column `Country` and `Item_Code` and only compute the sum of the rows falling under the columns ['Y1961', 'Y1962' and 'Y1963'].
API_1: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 10
query: How can I delete multiple columns in one pass? In detail, I would like to delete columns A and C, but I don't know how to do it in one pass.
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        

idx: 11
query: How do I select rows from a KnowledgeFrame kf based on column values? Return rows whose column value named `col_name` is in an iterable `values`
API_1: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: header_num(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the first `n` rows.

        This function returns the first `n` rows for the object based
        on position. It is useful for quickly testing if your object
        has the right type of data in it.

        For negative values of `n`, this function returns total_all rows except
        the final_item `n` rows, equivalengtht to ``kf[:-n]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        same type as ctotal_aller
            The first `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.final_item_tail: Returns the final_item `n` rows.

        

idx: 12
query: creating a Collections from a list [56, 24, 421, 90]
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: clone(self: '_IndexT', name: 'Hashable | None' = None, deep: 'bool' = False, dtype: 'Dtype | None' = None, names: 'Sequence[Hashable] | None' = None) -> '_IndexT':
        Make a clone of this object.

        Name and dtype sets those attributes on the new object.

        Parameters
        ----------
        name : Label, optional
            Set name for new object.
        deep : bool, default False
        dtype : numpy dtype or monkey type, optional
            Set dtype for new object.

            .. deprecated:: 1.2.0
                use ``totype`` method instead.
        names : list-like, optional
            Kept for compatibility with MultiIndex. Should not be used.

        Returns
        -------
        Index
            Index refer to new object which is a clone of this object.

        Notes
        -----
        In most cases, there should be no functional difference from using
        ``deep``, but if ``deep`` is passed it will attempt to deepclone.
        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 13
query: I want to prune my model using L2 norm and measure the latency.
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: interst(self, other, sort=False):
        Form the interst of two Index objects.

        This returns a new Index with elements common to the index and `other`.

        Parameters
        ----------
        other : Index or array-like
        sort : False or None, default False
            Whether to sort the resulting index.

            * False : do not sort the result.
            * None : sort the result, except when `self` and `other` are equal
              or when the values cannot be compared.

        Returns
        -------
        interst : Index

        

idx: 14
query: Monkey knowledgeframe fillnone() only some columns in place This function fills all columns with 0 Return the changed knowledgeframe
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: fillnone(self, value=None, downcast=None):
        Fill NA/NaN values with the specified value.

        Parameters
        ----------
        value : scalar
            Scalar value to use to fill holes (e.g. 0).
            This value cannot be a list-likes.
        downcast : dict, default is None
            A dict of item->dtype of what to downcast if possible,
            or the string 'infer' which will try to downcast to an appropriate
            equal type (e.g. float64 to int64 if possible).

        Returns
        -------
        Index

        See Also
        --------
        KnowledgeFrame.fillnone : Fill NaN values of a KnowledgeFrame.
        Collections.fillnone : Fill NaN Values of a Collections.
        

idx: 15
query: How can I rename a single column header? Also, please provide the updated knowledgeframe. Additionally, can you suggest a solution to remove columns with duplicate column names and provide the resulting knowledgeframe?
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: renaming(self, name, inplace=False):
        Alter Index or MultiIndex name.

        Able to set new names without level. Defaults to returning new index.
        Length of names must match number of levels in MultiIndex.

        Parameters
        ----------
        name : label or list of labels
            Name(s) to set.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Index or
            MultiIndex.

        Returns
        -------
        Index or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Index.set_names : Able to set new names partitotal_ally and by level.

        

idx: 16
query: Shift column in monkey knowledgeframe up by one? In detail, in 'gdp' column, shift up by one and return knowledgeframe with the changed gdp column.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 17
query: I need to convert an ONNX model to a TFLite model for the Arm Ethos-U.
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        

idx: 18
query: How can I set options in select_compression_method(), particularly for Policy, GroupPolicy, and LayerNorm?
API_1: sample_by_num(self: 'FrameOrCollections', n=None, frac: 'float | None' = None, replacing: 'bool_t' = False, weights=None, random_state=None, axis: 'Axis | None' = None, ignore_index: 'bool_t' = False) -> 'FrameOrCollections':
        Return a random sample_by_num of items from an axis of object.

        You can use `random_state` for reproducibility.

        Parameters
        ----------
        n : int, optional
            Number of items from axis to return. Cannot be used with `frac`.
            Default = 1 if `frac` = None.
        frac : float, optional
            Fraction of axis items to return. Cannot be used with `n`.
        replacing : bool, default False
            Allow or distotal_allow sampling of the same row more than once.
        weights : str or ndarray-like, optional
            Default 'None' results in equal probability weighting.
            If passed a Collections, will align with targetting object on index. Index
            values in weights not found in sample_by_numd object will be ignored and
            index values in sample_by_numd object not in weights will be total_allocateed
            weights of zero.
            If ctotal_alled on a KnowledgeFrame, will accept the name of a column
            when axis = 0.
            Unless weights are a Collections, weights must be same lengthgth as axis
            being sample_by_numd.
            If weights do not total_sum to 1, they will be normalized to total_sum to 1.
            Missing values in the weights column will be treated as zero.
            Infinite values not total_allowed.
        random_state : int, array-like, BitGenerator, np.random.RandomState, optional
            If int, array-like, or BitGenerator (NumPy>=1.17), seed for
            random number generator
            If np.random.RandomState, use as numpy RandomState object.

            .. versionchanged:: 1.1.0

                array-like and BitGenerator (for NumPy>=1.17) object now passed to
                np.random.RandomState() as seed

        axis : {0 or ‘index’, 1 or ‘columns’, None}, default None
            Axis to sample_by_num. Accepts axis number or name. Default is stat axis
            for given data type (0 for Collections and KnowledgeFrames).
        ignore_index : bool, default False
            If True, the resulting index will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.3.0

        Returns
        -------
        Collections or KnowledgeFrame
            A new object of same type as ctotal_aller containing `n` items randomly
            sample_by_numd from the ctotal_aller object.

        See Also
        --------
        KnowledgeFrameGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            KnowledgeFrame object.
        CollectionsGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            Collections object.
        numpy.random.choice: Generates a random sample_by_num from a given 1-D numpy
            array.

        Notes
        -----
        If `frac` > 1, `replacingment` should be set to `True`.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: incontain(self, values) -> 'np.ndarray': Return a boolean array where True if the value is contained in the passed values.

idx: 19
query: Converting a 2D beatnum numset to a structured numset You can 'create a record numset from a (flat) list of numsets' using beatnum.core.records.come_from_arrays as follows: Note that we need conduct the transpose on the numset, and the names reset to 'col1, co2, col3'
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 20
query: How can I delete multiple columns at once? Specifically, I want to remove columns A and C, but I'm not sure how to do it in a single operation. Additionally, how can I retrieve the values from column A when column B equals 3?
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 21
query: How to get the first largest value in column a？ Using nbiggest and iloc to implemente this
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: distinctive(self: '_IndexT', level: 'Hashable | None' = None) -> '_IndexT':
        Return distinctive values in the index.

        Unique values are returned in order of appearance, this does NOT sort.

        Parameters
        ----------
        level : int or hashable, optional
            Only return values from specified level (for MultiIndex).
            If int, gettings the level by integer position, else by level name.

        Returns
        -------
        Index

        See Also
        --------
        distinctive : Numpy array of distinctive values in that column.
        Collections.distinctive : Return distinctive values of Collections object.
        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 22
query: Round a single column `A` Return the knowledgeframe
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: value_round(freq, ambiguous='raise', nonexistent='raise'):
        Round the Timestamp to the specified resolution.

        Parameters
        ----------
        freq : str
            Frequency string indicating the value_rounding resolution.
        ambiguous : bool or {'raise', 'NaT'}, default 'raise'
            The behavior is as follows:

            * bool contains flags to detergetting_mine if time is dst or not (note
              that this flag is only applicable for ambiguous ftotal_all dst dates).
            * 'NaT' will return NaT for an ambiguous time.
            * 'raise' will raise an AmbiguousTimeError for an ambiguous time.

        nonexistent : {'raise', 'shifting_forward', 'shifting_backward, 'NaT', timedelta}, default 'raise'
            A nonexistent time does not exist in a particular timezone
            where clocks moved forward due to DST.

            * 'shifting_forward' will shifting the nonexistent time forward to the
              closest existing time.
            * 'shifting_backward' will shifting the nonexistent time backward to the
              closest existing time.
            * 'NaT' will return NaT where there are nonexistent times.
            * timedelta objects will shifting nonexistent times by the timedelta.
            * 'raise' will raise an NonExistentTimeError if there are
              nonexistent times.

        Returns
        -------
        a new Timestamp value_rounded to the given resolution of `freq`

        Raises
        ------
        ValueError if the freq cannot be converted

        
API_3: conduct_map(self, func: 'PythonFuncType', na_action: 'str | None' = None, **kwargs) -> 'KnowledgeFrame':
        Apply a function to a Dataframe elementwise.

        This method applies a function that accepts and returns a scalar
        to every element of a KnowledgeFrame.

        Parameters
        ----------
        func : ctotal_allable
            Python function, returns a single value from a single value.
        na_action : {None, 'ignore'}, default None
            If ‘ignore’, propagate NaN values, without passing them to func.

            .. versionadded:: 1.2

        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

            .. versionadded:: 1.3.0

        Returns
        -------
        KnowledgeFrame
            Transformed KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employ : Apply a function along input axis of KnowledgeFrame.

        

idx: 23
query: Appending the source collections to the target collections, with ignoring the index or resetting index
API_1: final_item(self: 'FrameOrCollections', offset) -> 'FrameOrCollections':
        Select final periods of time collections data based on a date offset.

        For a KnowledgeFrame with a sorted DatetimeIndex, this function
        selects the final_item few rows based on a date offset.

        Parameters
        ----------
        offset : str, DateOffset, dateutil.relativedelta
            The offset lengthgth of the data that will be selected. For instance,
            '3D' will display total_all the rows having their index within the final_item 3 days.

        Returns
        -------
        Collections or KnowledgeFrame
            A subset of the ctotal_aller.

        Raises
        ------
        TypeError
            If the index is not  a :class:`DatetimeIndex`

        See Also
        --------
        first : Select initial periods of time collections based on a date offset.
        at_time : Select values at a particular time of the day.
        between_time : Select values between particular times of the day.

        
API_2: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_3: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    

idx: 24
query: Say i have a knowledgeframe with 100,000 entries and want to split it into 100 sections of 1000 entries. How do i take a random sample of say size 50 of just one of the 100 sections. the data set is already ordered such that the first 1000 results are the first section the next section the next and so on. You could add a "section" column to your data then perform a grouper and sample_by_num(n=50):
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: header_num(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the first `n` rows.

        This function returns the first `n` rows for the object based
        on position. It is useful for quickly testing if your object
        has the right type of data in it.

        For negative values of `n`, this function returns total_all rows except
        the final_item `n` rows, equivalengtht to ``kf[:-n]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        same type as ctotal_aller
            The first `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.final_item_tail: Returns the final_item `n` rows.

        

idx: 25
query: I would like assgin dp1 to be a datapipe that contains the first column of raw_dp and dp2 to be a datapipe that contains the second column of raw_dp and dp3 to be a datapipe that contains the third column of raw_dp How to do this?
API_1: replacing(old, new, count=-1, /):Return a clone with total_all occurrences of substring old replacingd by new.

  count
    Maximum number of occurrences to replacing.
    -1 (the default value) averages replacing total_all occurrences.

If the optional argument count is given, only the first count occurrences are
replacingd.
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    

idx: 26
query: Any word from `targets` are present in sentence.
API_1: sample_by_num(self: 'FrameOrCollections', n=None, frac: 'float | None' = None, replacing: 'bool_t' = False, weights=None, random_state=None, axis: 'Axis | None' = None, ignore_index: 'bool_t' = False) -> 'FrameOrCollections':
        Return a random sample_by_num of items from an axis of object.

        You can use `random_state` for reproducibility.

        Parameters
        ----------
        n : int, optional
            Number of items from axis to return. Cannot be used with `frac`.
            Default = 1 if `frac` = None.
        frac : float, optional
            Fraction of axis items to return. Cannot be used with `n`.
        replacing : bool, default False
            Allow or distotal_allow sampling of the same row more than once.
        weights : str or ndarray-like, optional
            Default 'None' results in equal probability weighting.
            If passed a Collections, will align with targetting object on index. Index
            values in weights not found in sample_by_numd object will be ignored and
            index values in sample_by_numd object not in weights will be total_allocateed
            weights of zero.
            If ctotal_alled on a KnowledgeFrame, will accept the name of a column
            when axis = 0.
            Unless weights are a Collections, weights must be same lengthgth as axis
            being sample_by_numd.
            If weights do not total_sum to 1, they will be normalized to total_sum to 1.
            Missing values in the weights column will be treated as zero.
            Infinite values not total_allowed.
        random_state : int, array-like, BitGenerator, np.random.RandomState, optional
            If int, array-like, or BitGenerator (NumPy>=1.17), seed for
            random number generator
            If np.random.RandomState, use as numpy RandomState object.

            .. versionchanged:: 1.1.0

                array-like and BitGenerator (for NumPy>=1.17) object now passed to
                np.random.RandomState() as seed

        axis : {0 or ‘index’, 1 or ‘columns’, None}, default None
            Axis to sample_by_num. Accepts axis number or name. Default is stat axis
            for given data type (0 for Collections and KnowledgeFrames).
        ignore_index : bool, default False
            If True, the resulting index will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.3.0

        Returns
        -------
        Collections or KnowledgeFrame
            A new object of same type as ctotal_aller containing `n` items randomly
            sample_by_numd from the ctotal_aller object.

        See Also
        --------
        KnowledgeFrameGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            KnowledgeFrame object.
        CollectionsGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            Collections object.
        numpy.random.choice: Generates a random sample_by_num from a given 1-D numpy
            array.

        Notes
        -----
        If `frac` > 1, `replacingment` should be set to `True`.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: incontain(self, values) -> 'np.ndarray': Return a boolean array where True if the value is contained in the passed values.

idx: 27
query: How can I convert a list into a knowledgeframe? I want to create a knowledgeframe where one of the columns is a list or array. Once I assign a list-like or array-like value to the column, it should be considered as type object. I also want to assign the emails to the first row and the 'Email' column. The desired format for the knowledgeframe is as follows: list_of_lists format: [header, [row1], [row2], ...], header format: [column1, column2, ...], row format: [value1, value2, ...]. Please return the resulting knowledgeframe.
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        

idx: 28
query: I have a list containing beatnum numsets something like L=[a,b,c] where a, b and c are beatnum numsets with sizes N_a in T, N_b in T and N_c in T. I want to row-wise connect a, b and c and get a beatnum numset with shape (N_a+N_b+N_c, T). Clearly one solution is run a for loop and use beatnum.connect, but is there any pythonic way to do this?
API_1: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 29
query: How can I create a collection from a list [56, 24, 421, 90]? After that, how can I count the number of missing/NaN values in each column? Can I get a collection with the number of missing/NaN values in each column?
API_1: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 30
query: Provide a training code example using an fx model.
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 31
query: Normalization using monkey We simply subtract the average and divide by standard deviation on kf.iloc[:,0,-1] obj with axis is zero. Return the normalized knowledgeframe
API_1: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: standard(self, axis=None, skipna=None, level=None, ddof=1, numeric_only=None, **kwargs):
Return sample_by_num standard deviation over requested axis.

Normalized by N-1 by default. This can be changed using the ddof argument

Parameters
----------
axis : {index (0)}
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
ddof : int, default 1
    Delta Degrees of Freedom. The divisionisor used in calculations is N - ddof,
    where N represents the number of elements.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.

Returns
-------
scalar or Collections (if level specified)

Notes
-----
To have the same behaviour as `numpy.standard`, use `ddof=0` (instead of the
default `ddof=1`)


idx: 32
query: Sorting columns in monkey knowledgeframe based on column name Note that axis is one
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: reindexing(self, target, method=None, level=None, limit=None, tolerance=None) -> 'tuple[MultiIndex, np.ndarray | None]':
        Create index with targetting's values (move/add/delete values as necessary)

        Returns
        -------
        new_index : mk.MultiIndex
            Resulting index
        indexer : np.ndarray[np.intp] or None
            Indices of output values in original index.

        

idx: 33
query: Finding non-numeric rows in knowledgeframe in monkey Return the raws that contain non-numeric values So to get the subKnowledgeFrame of rouges, (Note: the negation, ~, of the above finds the ones which have at least one rogue non-numeric):
API_1: total_all(self, *args, **kwargs):
        Return whether total_all elements are Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        total_all : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.whatever : Return whether whatever element in an Index is True.
        Collections.whatever : Return whether whatever element in a Collections is True.
        Collections.total_all : Return whether total_all elements in a Collections are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: conduct_map(self, func: 'PythonFuncType', na_action: 'str | None' = None, **kwargs) -> 'KnowledgeFrame':
        Apply a function to a Dataframe elementwise.

        This method applies a function that accepts and returns a scalar
        to every element of a KnowledgeFrame.

        Parameters
        ----------
        func : ctotal_allable
            Python function, returns a single value from a single value.
        na_action : {None, 'ignore'}, default None
            If ‘ignore’, propagate NaN values, without passing them to func.

            .. versionadded:: 1.2

        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

            .. versionadded:: 1.3.0

        Returns
        -------
        KnowledgeFrame
            Transformed KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employ : Apply a function along input axis of KnowledgeFrame.

        

idx: 34
query: Is there a function that returns an numset with the results of dividing the next element by the previous one? Like a "difference()", but with dividing Not-beatnum-example: source = [1,3,6,24,36] target = [j / i for i, j in zip(source[:-1], source[1:])] Return: target implemented in beatnum.
API_1: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_2: renaming(self, name, inplace=False):
        Alter Index or MultiIndex name.

        Able to set new names without level. Defaults to returning new index.
        Length of names must match number of levels in MultiIndex.

        Parameters
        ----------
        name : label or list of labels
            Name(s) to set.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Index or
            MultiIndex.

        Returns
        -------
        Index or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Index.set_names : Able to set new names partitotal_ally and by level.

        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 35
query: Please insert a row into the knowledgeframe at a specific position without ignoring the index. After that, sort the index and reset it using sip=True. Return the updated knowledgeframe. Additionally, if a row in the knowledgeframe has a value of 0 in column2 and is at an odd index, please remove that row and the row above it. Finally, provide a new knowledgeframe with these rows removed.
API_1: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 36
query: How to sip rows of Monkey KnowledgeFrame whose value in a certain column is NaN
API_1: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 37
query: I want to add the first element on to the end of the numset. Return the appended numset.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: reindexing(self, target, method=None, level=None, limit=None, tolerance=None) -> 'tuple[MultiIndex, np.ndarray | None]':
        Create index with targetting's values (move/add/delete values as necessary)

        Returns
        -------
        new_index : mk.MultiIndex
            Resulting index
        indexer : np.ndarray[np.intp] or None
            Indices of output values in original index.

        

idx: 38
query: How can I measure the latency of a quantized model formmated with int8 on a Jetson AGX Orin device?
API_1: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: incontain(self, values) -> 'np.ndarray': Return a boolean array where True if the value is contained in the passed values.

idx: 39
query: I want to use a pretrained model for training.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 40
query: Can you help me apply GM Pruning to a customized model with a pruning ratio of 0.5?
API_1: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: getting(self, i):
        Extract element from each component at specified position.

        Extract element from lists, tuples, or strings in each element in the
        Collections/Index.

        Parameters
        ----------
        i : int
            Position of element to extract.

        Returns
        -------
        Collections or Index

        

idx: 41
query: I want to train a YOLOX model and measure the latency afterward.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: formating(self, name: 'bool' = False, formatingter: 'Ctotal_allable | None' = None, na_rep: 'str_t' = 'NaN') -> 'list[str_t]':
        Render a string representation of the Index.
        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 42
query: How can I normalize the knowledge frame using monkey? I want to subtract the average and divide by the standard deviation on the kf.iloc[:,0,-1] object with axis zero. After normalization, I would like to return the normalized knowledge frame. Additionally, I want to create a new knowledge frame that is the same as kf_original, but without any rows. Can you please return the new knowledge frame?
API_1: standard(self, axis=None, skipna=None, level=None, ddof=1, numeric_only=None, **kwargs):
Return sample_by_num standard deviation over requested axis.

Normalized by N-1 by default. This can be changed using the ddof argument

Parameters
----------
axis : {index (0)}
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
ddof : int, default 1
    Delta Degrees of Freedom. The divisionisor used in calculations is N - ddof,
    where N represents the number of elements.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.

Returns
-------
scalar or Collections (if level specified)

Notes
-----
To have the same behaviour as `numpy.standard`, use `ddof=0` (instead of the
default `ddof=1`)

API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)


idx: 43
query: How to get the first three elements of a datapipe?
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: clone(self: '_IndexT', name: 'Hashable | None' = None, deep: 'bool' = False, dtype: 'Dtype | None' = None, names: 'Sequence[Hashable] | None' = None) -> '_IndexT':
        Make a clone of this object.

        Name and dtype sets those attributes on the new object.

        Parameters
        ----------
        name : Label, optional
            Set name for new object.
        deep : bool, default False
        dtype : numpy dtype or monkey type, optional
            Set dtype for new object.

            .. deprecated:: 1.2.0
                use ``totype`` method instead.
        names : list-like, optional
            Kept for compatibility with MultiIndex. Should not be used.

        Returns
        -------
        Index
            Index refer to new object which is a clone of this object.

        Notes
        -----
        In most cases, there should be no functional difference from using
        ``deep``, but if ``deep`` is passed it will attempt to deepclone.
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 44
query: How can I check if a Monkey Column contains a specific value? Also, how can I find out the number of columns in a Monkey data frame?
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: distinctive(self: '_IndexT', level: 'Hashable | None' = None) -> '_IndexT':
        Return distinctive values in the index.

        Unique values are returned in order of appearance, this does NOT sort.

        Parameters
        ----------
        level : int or hashable, optional
            Only return values from specified level (for MultiIndex).
            If int, gettings the level by integer position, else by level name.

        Returns
        -------
        Index

        See Also
        --------
        distinctive : Numpy array of distinctive values in that column.
        Collections.distinctive : Return distinctive values of Collections object.
        
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 45
query: I want to make all column headers in my monkey data frame lower case
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 46
query: How can I determine the number of missing/NaN values in each column? Can I get a collection with the count of missing/NaN values in each column? Also, is there a way to identify which columns contain NaN values? Specifically, can I obtain a list of the column names that have NaNs? Please provide a list of the column names that contain NaNs.
API_1: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        

idx: 47
query: Flattening a list of BeatNum numsets? We can use beatnum.connect, which as the name suggests, basically connects all the elements of such an input list into a single BeatNum numset And then we can use beatnum.asview to flatten the numset
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 48
query: add the row at top in kf resort the index by inplace
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: renaming(self, name, inplace=False):
        Alter Index or MultiIndex name.

        Able to set new names without level. Defaults to returning new index.
        Length of names must match number of levels in MultiIndex.

        Parameters
        ----------
        name : label or list of labels
            Name(s) to set.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Index or
            MultiIndex.

        Returns
        -------
        Index or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Index.set_names : Able to set new names partitotal_ally and by level.

        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 49
query: transform timestamp to pydatetime object return pydatetime object
API_1: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 50
query: Drop consecutive duplicates Return the result
API_1: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_2: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        
API_3: remove_duplicates(self: '_IndexT', keep: 'str_t | bool' = 'first') -> '_IndexT':
        Return Index with duplicate values removed.

        Parameters
        ----------
        keep : {'first', 'final_item', ``False``}, default 'first'
            - 'first' : Drop duplicates except for the first occurrence.
            - 'final_item' : Drop duplicates except for the final_item occurrence.
            - ``False`` : Drop total_all duplicates.

        Returns
        -------
        deduplicated_values : Index

        See Also
        --------
        Collections.sip_duplicates : Equivalengtht method on Collections.
        KnowledgeFrame.sip_duplicates : Equivalengtht method on KnowledgeFrame.
        Index.duplicated_values : Related method on Index, indicating duplicate
            Index values.

        

idx: 51
query: Which model frameworks does the converter support?
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        

idx: 52
query: replace field that's entirely space (or empty) with NaN using regex return the result
API_1: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 53
query: I would simply like to slice the Data Frame and take the first n rows. Return the result
API_1: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        
API_2: nbiggest(self, n=5, keep='first') -> 'Collections':
        Return the largest `n` elements.

        Parameters
        ----------
        n : int, default 5
            Return this mwhatever descending sorted values.
        keep : {'first', 'final_item', 'total_all'}, default 'first'
            When there are duplicate values that cannot total_all fit in a
            Collections of `n` elements:

            - ``first`` : return the first `n` occurrences in order
                of appearance.
            - ``final_item`` : return the final_item `n` occurrences in reverse
                order of appearance.
            - ``total_all`` : keep total_all occurrences. This can result in a Collections of
                size larger than `n`.

        Returns
        -------
        Collections
            The `n` largest values in the Collections, sorted in decreasing order.

        See Also
        --------
        Collections.nsmtotal_allest: Get the `n` smtotal_allest elements.
        Collections.sort_the_values: Sort Collections by values.
        Collections.header_num: Return the first `n` rows.

        Notes
        -----
        Faster than ``.sort_the_values(ascending=False).header_num(n)`` for smtotal_all `n`
        relative to the size of the ``Collections`` object.

        
API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 54
query: creating a new knowledgeframe of all same with kf_original one, but no any rows return the new knowledgeframe
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 55
query: How do I unioner two knowledgeframes by index? Set left&right indexs to True
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: interst(self, other, sort=False):
        Form the interst of two Index objects.

        This returns a new Index with elements common to the index and `other`.

        Parameters
        ----------
        other : Index or array-like
        sort : False or None, default False
            Whether to sort the resulting index.

            * False : do not sort the result.
            * None : sort the result, except when `self` and `other` are equal
              or when the values cannot be compared.

        Returns
        -------
        interst : Index

        

idx: 56
query: How can I determine if there is a NaN value in a Monkey KnowledgeFrame? Please provide the result. Also, how can I convert True/False values to 1/0 in a Monkey KnowledgeFrame? Please return the knowledgeframe with the specified column converted to integers.
API_1: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        

idx: 57
query: creating a new knowledgeframe of all same with kf_original one, but no any rows return the new knowledgeframe
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: clone(self: '_IndexT', name: 'Hashable | None' = None, deep: 'bool' = False, dtype: 'Dtype | None' = None, names: 'Sequence[Hashable] | None' = None) -> '_IndexT':
        Make a clone of this object.

        Name and dtype sets those attributes on the new object.

        Parameters
        ----------
        name : Label, optional
            Set name for new object.
        deep : bool, default False
        dtype : numpy dtype or monkey type, optional
            Set dtype for new object.

            .. deprecated:: 1.2.0
                use ``totype`` method instead.
        names : list-like, optional
            Kept for compatibility with MultiIndex. Should not be used.

        Returns
        -------
        Index
            Index refer to new object which is a clone of this object.

        Notes
        -----
        In most cases, there should be no functional difference from using
        ``deep``, but if ``deep`` is passed it will attempt to deepclone.
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 58
query: unioner the above two knowledgeframes on column 'company'
API_1: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        

idx: 59
query: what is the fastest and easiest way to set the super low value named tol to zero? Handling of reality and imaginary numbers separately
API_1: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 60
query: To compute the average of each row and add a new column named `average_along_rows`, use the `mean` function with `axis=1` and assign it to the new column. Then, to obtain the counts of distinctive values in the knowledgeframe, use the `value_counts` function. To convert the output of `value_counts` to a monkey knowledgeframe, use the `to_frame` function. Rename the column from the index to 'distinctive_values' using `rename_axis('distinctive_values')`. Finally, reset the index to return the final knowledgeframe.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 61
query: How can I convert all column headers in my monkey data frame to lowercase? Also, how can I rename a single column header? Please return the updated knowledge frame.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: mapping(self, mapper, na_action=None):
        Map values using input correspondence (a dict, Collections, or function).

        Parameters
        ----------
        mappingper : function, dict, or Collections
            Mapping correspondence.
        na_action : {None, 'ignore'}
            If 'ignore', propagate NA values, without passing them to the
            mappingping correspondence.

        Returns
        -------
        applied : Union[Index, MultiIndex], inferred
            The output of the mappingping function applied to the index.
            If the function returns a tuple with more than one element
            a MultiIndex will be returned.
        

idx: 62
query: What method do I use to set an optimizer and a scheduler? Please provide an example.
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)


idx: 63
query: Move next value to first empty row monkey how do i move each value from a column to the first empty "row/cell" in monkey? use sorted to align non NULL data at the top, use sipna to sip all rows with all NaN
API_1: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        

idx: 64
query: How can I remove all the numbers from the Name column in a more efficient way at the collections/knowledgeframe level? Also, I want to change the data type of all columns in the KnowledgeFrame to numeric. Please return the updated KnowledgeFrame.
API_1: replacing(old, new, count=-1, /):Return a clone with total_all occurrences of substring old replacingd by new.

  count
    Maximum number of occurrences to replacing.
    -1 (the default value) averages replacing total_all occurrences.

If the optional argument count is given, only the first count occurrences are
replacingd.
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        

idx: 65
query: How to group values of monkey knowledgeframe and select the latest by date from each group? Sorting values by `date` (ascending is True), and then grouping by `id`
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_3: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        

idx: 66
query: Convert Column `Date` to Date Format using monkey function return the coverted knowledgeframe
API_1: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        
API_2: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 67
query: I would like to dynamically piece a beatnum numset along a specific axis. I think one way would be to use piece(start, end):
API_1: nbiggest(self, n=5, keep='first') -> 'Collections':
        Return the largest `n` elements.

        Parameters
        ----------
        n : int, default 5
            Return this mwhatever descending sorted values.
        keep : {'first', 'final_item', 'total_all'}, default 'first'
            When there are duplicate values that cannot total_all fit in a
            Collections of `n` elements:

            - ``first`` : return the first `n` occurrences in order
                of appearance.
            - ``final_item`` : return the final_item `n` occurrences in reverse
                order of appearance.
            - ``total_all`` : keep total_all occurrences. This can result in a Collections of
                size larger than `n`.

        Returns
        -------
        Collections
            The `n` largest values in the Collections, sorted in decreasing order.

        See Also
        --------
        Collections.nsmtotal_allest: Get the `n` smtotal_allest elements.
        Collections.sort_the_values: Sort Collections by values.
        Collections.header_num: Return the first `n` rows.

        Notes
        -----
        Faster than ``.sort_the_values(ascending=False).header_num(n)`` for smtotal_all `n`
        relative to the size of the ``Collections`` object.

        
API_2: header_num(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the first `n` rows.

        This function returns the first `n` rows for the object based
        on position. It is useful for quickly testing if your object
        has the right type of data in it.

        For negative values of `n`, this function returns total_all rows except
        the final_item `n` rows, equivalengtht to ``kf[:-n]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        same type as ctotal_aller
            The first `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.final_item_tail: Returns the final_item `n` rows.

        
API_3: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        

idx: 68
query: Is there a more concise and efficient method in Monkey KnowledgeFrames to choose columns based on their data type (dtype)? For example, I want to select only the columns that are of type float64 from a KnowledgeFrame. Additionally, I would like to convert all the columns in the KnowledgeFrame to a numeric data type and obtain the updated KnowledgeFrame as the result.
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 69
query: Zipping the above two data pipes and set keep_key to True according to merge_fn. Moreover, transform its type to List and get the first element.
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 70
query: Convert a YOLOX model with ONNX format to TFLite and optimize it for the Raspberry Pi 4B.
API_1: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_2: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        
API_3: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        

idx: 71
query: I need to divide all ['B','C'] columns but the first column 'A' in a KnowledgeFrame by the first column. Return the result.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 72
query: How to obtain monkey KnowledgeFrame without index I want to print the whole knowledgeframe, but I don't want to print the index
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 73
query: Can you show how to use the convert_model() function?
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: renaming(self, name, inplace=False):
        Alter Index or MultiIndex name.

        Able to set new names without level. Defaults to returning new index.
        Length of names must match number of levels in MultiIndex.

        Parameters
        ----------
        name : label or list of labels
            Name(s) to set.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Index or
            MultiIndex.

        Returns
        -------
        Index or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Index.set_names : Able to set new names partitotal_ally and by level.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 74
query: How to determine whether a Monkey Column contains a particular value? Return the result
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 75
query: kf = kf.reseting_index() make sure indexes pair with number of rows (for index, row in KnowledgeFrame.traversal) is a generator which yields both the index and row (as a Collections) for each row in the KnowledgeFrame, we need put the row['MSRA'] (as key) and row['THU'] (as value) into a rows_dict rows_dict = {} {MSRA: THU, ...}
API_1: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 76
query: list_of_lists format: [header, [row1], [row2], ...] header format: [column1, column2, ...] row format: [value1, value2, ...] How to convert list to knowledgeframe? Return the knowledgeframe
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 77
query: Removing columns with index 1 and 3 in beatnum If you ever want to remove more than one columns, you just pass indices of columns you want removed as a list to bn.remove_operation, like this:
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        

idx: 78
query: I want to get a list of the column headers from a Monkey KnowledgeFrame. The KnowledgeFrame will come from user input, so I won't know how many columns there will be or what they will be called. Return a list of the column headers.
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 79
query: How to check if any value is NaN in a Monkey KnowledgeFrame? Return the result.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_3: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        

idx: 80
query: How to check if any value is NaN in a Monkey KnowledgeFrame? Return the result.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 81
query: I want to make all column headers in my monkey data frame lower case
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: mapping(self, mapper, na_action=None):
        Map values using input correspondence (a dict, Collections, or function).

        Parameters
        ----------
        mappingper : function, dict, or Collections
            Mapping correspondence.
        na_action : {None, 'ignore'}
            If 'ignore', propagate NA values, without passing them to the
            mappingping correspondence.

        Returns
        -------
        applied : Union[Index, MultiIndex], inferred
            The output of the mappingping function applied to the index.
            If the function returns a tuple with more than one element
            a MultiIndex will be returned.
        

idx: 82
query: Make a batch operation on the datapipe `dp` of size 3 with droping last batch if it is not full. And then get the first two batches.
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    

idx: 83
query: How can I retrieve all the rows in a monkey KnowledgeFrame that have the highest value in the 'num' column, after grouping by the 'Mt' column? Additionally, how can I obtain the last N rows of a monkey KnowledgeFrame?
API_1: get_max(self, axis=None, skipna: 'bool' = True, *args, **kwargs):
        Return the getting_maximum value of the Index.

        Parameters
        ----------
        axis : int, optional
            For compatibility with NumPy. Only 0 or None are total_allowed.
        skipna : bool, default True
            Exclude NA/null values when showing the result.
        *args, **kwargs
            Additional arguments and keywords for compatibility with NumPy.

        Returns
        -------
        scalar
            Maximum value.

        See Also
        --------
        Index.getting_min : Return the getting_minimum value in an Index.
        Collections.getting_max : Return the getting_maximum value in a Collections.
        KnowledgeFrame.getting_max : Return the getting_maximum values in a KnowledgeFrame.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 84
query: How can I sort the columns in a monkey knowledgeframe based on their column names? Also, how do I change the column labels of a monkey KnowledgeFrame from ['A', 'B', 'C'] to ['a', 'b', 'c']?
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: reindexing(self, target, method=None, level=None, limit=None, tolerance=None) -> 'tuple[MultiIndex, np.ndarray | None]':
        Create index with targetting's values (move/add/delete values as necessary)

        Returns
        -------
        new_index : mk.MultiIndex
            Resulting index
        indexer : np.ndarray[np.intp] or None
            Indices of output values in original index.

        

idx: 85
query: transfer column date to datetime type when there is a string that is not capable of beeing turned into datetime format, skip that row, use errors='coerce' for this
API_1: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    

idx: 86
query: How do I set random seed for environmental setup?
API_1: replacing(old, new, count=-1, /):Return a clone with total_all occurrences of substring old replacingd by new.

  count
    Maximum number of occurrences to replacing.
    -1 (the default value) averages replacing total_all occurrences.

If the optional argument count is given, only the first count occurrences are
replacingd.
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 87
query: Firstly, We need to find the minimun value of each column with axis 0, Then conduct subtract operation between each element of the column and the minimum value.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        

idx: 88
query: return the column average/mean
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 89
query: Delete all columns that contain all NaN values Return the result.
API_1: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_2: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        
API_3: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        

idx: 90
query: Move next value to first empty row monkey how do i move each value from a column to the first empty "row/cell" in monkey? use sorted to align non NULL data at the top, use sipna to sip all rows with all NaN
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        

idx: 91
query: How can I randomly select a sample of size 50 from one of the 100 sections in a knowledge frame with 100,000 entries? The knowledge frame is already ordered in sections of 1000 entries each. Can I add a "section" column to the data and then use a grouper to sample_by_num(n=50)? Additionally, can I slice the data frame to take the first n rows? Please return the result.
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: sample_by_num(self: 'FrameOrCollections', n=None, frac: 'float | None' = None, replacing: 'bool_t' = False, weights=None, random_state=None, axis: 'Axis | None' = None, ignore_index: 'bool_t' = False) -> 'FrameOrCollections':
        Return a random sample_by_num of items from an axis of object.

        You can use `random_state` for reproducibility.

        Parameters
        ----------
        n : int, optional
            Number of items from axis to return. Cannot be used with `frac`.
            Default = 1 if `frac` = None.
        frac : float, optional
            Fraction of axis items to return. Cannot be used with `n`.
        replacing : bool, default False
            Allow or distotal_allow sampling of the same row more than once.
        weights : str or ndarray-like, optional
            Default 'None' results in equal probability weighting.
            If passed a Collections, will align with targetting object on index. Index
            values in weights not found in sample_by_numd object will be ignored and
            index values in sample_by_numd object not in weights will be total_allocateed
            weights of zero.
            If ctotal_alled on a KnowledgeFrame, will accept the name of a column
            when axis = 0.
            Unless weights are a Collections, weights must be same lengthgth as axis
            being sample_by_numd.
            If weights do not total_sum to 1, they will be normalized to total_sum to 1.
            Missing values in the weights column will be treated as zero.
            Infinite values not total_allowed.
        random_state : int, array-like, BitGenerator, np.random.RandomState, optional
            If int, array-like, or BitGenerator (NumPy>=1.17), seed for
            random number generator
            If np.random.RandomState, use as numpy RandomState object.

            .. versionchanged:: 1.1.0

                array-like and BitGenerator (for NumPy>=1.17) object now passed to
                np.random.RandomState() as seed

        axis : {0 or ‘index’, 1 or ‘columns’, None}, default None
            Axis to sample_by_num. Accepts axis number or name. Default is stat axis
            for given data type (0 for Collections and KnowledgeFrames).
        ignore_index : bool, default False
            If True, the resulting index will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.3.0

        Returns
        -------
        Collections or KnowledgeFrame
            A new object of same type as ctotal_aller containing `n` items randomly
            sample_by_numd from the ctotal_aller object.

        See Also
        --------
        KnowledgeFrameGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            KnowledgeFrame object.
        CollectionsGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            Collections object.
        numpy.random.choice: Generates a random sample_by_num from a given 1-D numpy
            array.

        Notes
        -----
        If `frac` > 1, `replacingment` should be set to `True`.

        

idx: 92
query: Given the weight, how to sample from two datapipes? Note that the sample seed is set to 1 for reproducibility
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 93
query: Reformulated query: 

Please reset the indexes of the KnowledgeFrame using the kf.reset_index() function. Make sure that the indexes pair with the number of rows in the KnowledgeFrame. The KnowledgeFrame.traversal function is a generator that yields both the index and row (as a Collections) for each row in the KnowledgeFrame. For each row, we need to add the value of row['MSRA'] as the key and the value of row['THU'] as the value in a dictionary called rows_dict. Additionally, please return the row-index values of the KnowledgeFrame as a list.
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 94
query: Monkey create empty KnowledgeFrame with only column names Return: KnowledgeFrame
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 95
query: Combine the two knowledge frames based on the 'company' column. Then, filter the resulting knowledge frame to include only the rows where the second column has the value 'Jimmy'.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 96
query: Please remove all rows containing NaN values from the knowledgeframe and provide the updated version. Additionally, please provide the knowledgeframe with rows that have one or more NaN values.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        

idx: 97
query: This will tell me those values generate a mask to find all values that are even numbers Is there an efficient Beatnum mechanism to retrieve the integer indexes of locations in an numset based on a condition is true as opposed to the Boolean mask numset?
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 98
query: ceiling of a monkey collections Return the result.
API_1: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        
API_2: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        
API_3: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        

idx: 99
query: adding dictionary to data frame return the data frame
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    
API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 100
query: How do I check the inference latency of an ONNX model on a Jetson Nano?
API_1: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: clone(self: '_IndexT', name: 'Hashable | None' = None, deep: 'bool' = False, dtype: 'Dtype | None' = None, names: 'Sequence[Hashable] | None' = None) -> '_IndexT':
        Make a clone of this object.

        Name and dtype sets those attributes on the new object.

        Parameters
        ----------
        name : Label, optional
            Set name for new object.
        deep : bool, default False
        dtype : numpy dtype or monkey type, optional
            Set dtype for new object.

            .. deprecated:: 1.2.0
                use ``totype`` method instead.
        names : list-like, optional
            Kept for compatibility with MultiIndex. Should not be used.

        Returns
        -------
        Index
            Index refer to new object which is a clone of this object.

        Notes
        -----
        In most cases, there should be no functional difference from using
        ``deep``, but if ``deep`` is passed it will attempt to deepclone.
        

idx: 101
query: Appending the source collections to the target collections, with ignoring the index or resetting index
API_1: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_2: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_3: final_item(self: 'FrameOrCollections', offset) -> 'FrameOrCollections':
        Select final periods of time collections data based on a date offset.

        For a KnowledgeFrame with a sorted DatetimeIndex, this function
        selects the final_item few rows based on a date offset.

        Parameters
        ----------
        offset : str, DateOffset, dateutil.relativedelta
            The offset lengthgth of the data that will be selected. For instance,
            '3D' will display total_all the rows having their index within the final_item 3 days.

        Returns
        -------
        Collections or KnowledgeFrame
            A subset of the ctotal_aller.

        Raises
        ------
        TypeError
            If the index is not  a :class:`DatetimeIndex`

        See Also
        --------
        first : Select initial periods of time collections based on a date offset.
        at_time : Select values at a particular time of the day.
        between_time : Select values between particular times of the day.

        

idx: 102
query: Converting int numsets to string numsets in beatnum without truncation
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_3: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        

idx: 103
query: list_of_lists format: [header, [row1], [row2], ...] header format: [column1, column2, ...] row format: [value1, value2, ...] How to convert list to knowledgeframe? Return the knowledgeframe
API_1: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 104
query: How can I remove duplicate values in column `col1` in my knowledgeframe? I want to keep the row with the last value in column `col2`. Please provide the final knowledgeframe. Additionally, can you suggest a solution to remove columns with duplicate names in the knowledgeframe? Please return the duplicated knowledgeframe.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: remove_duplicates(self: '_IndexT', keep: 'str_t | bool' = 'first') -> '_IndexT':
        Return Index with duplicate values removed.

        Parameters
        ----------
        keep : {'first', 'final_item', ``False``}, default 'first'
            - 'first' : Drop duplicates except for the first occurrence.
            - 'final_item' : Drop duplicates except for the final_item occurrence.
            - ``False`` : Drop total_all duplicates.

        Returns
        -------
        deduplicated_values : Index

        See Also
        --------
        Collections.sip_duplicates : Equivalengtht method on Collections.
        KnowledgeFrame.sip_duplicates : Equivalengtht method on KnowledgeFrame.
        Index.duplicated_values : Related method on Index, indicating duplicate
            Index values.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 105
query: How can I convert True/False values to 1/0 in a Monkey KnowledgeFrame? Please provide the knowledgeframe with the column converted to integers. Additionally, how can I retrieve the number of columns in a Monkey data frame? Please return the number of columns in the knowledgeframe.
API_1: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        
API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 106
query: How to group values of monkey knowledgeframe and select the latest by date from each group? Sorting values by `date` (ascending is True), and then grouping by `id`
API_1: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        

idx: 107
query: Check if each element in a beatnum numset is in another numset This problem seems easy but I cannot quite get a nice-looking solution. I have two beatnum numsets (A and B), and I want to get the indices of A where the elements of A are in B and also get the indices of A where the elements are not in B.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: distinctive(self: '_IndexT', level: 'Hashable | None' = None) -> '_IndexT':
        Return distinctive values in the index.

        Unique values are returned in order of appearance, this does NOT sort.

        Parameters
        ----------
        level : int or hashable, optional
            Only return values from specified level (for MultiIndex).
            If int, gettings the level by integer position, else by level name.

        Returns
        -------
        Index

        See Also
        --------
        distinctive : Numpy array of distinctive values in that column.
        Collections.distinctive : Return distinctive values of Collections object.
        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 108
query: How can I delete a column from a Monkey KnowledgeFrame and get the updated knowledgeframe? Also, I would like to convert all column headers in my monkey data frame to lowercase.
API_1: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        
API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 109
query: How to get the last N rows of a monkey KnowledgeFrame?
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        

idx: 110
query: Please find the rows in the knowledgeframe for monkey that contain non-numeric values. Additionally, remove any rows that have at least one non-numeric value. To obtain the subKnowledgeFrame of rogue non-numeric values, use the negation (~) of the previous query. After that, remove two rows from the knowledgeframe if the column contains a zero and it is on an odd index. Also, remove the previous row along with the current row if a zero is found on an odd index. Please note that the knowledgeframe is indexed starting from 0. Finally, create a new knowledgeframe without the rows mentioned above.
API_1: total_all(self, *args, **kwargs):
        Return whether total_all elements are Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        total_all : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.whatever : Return whether whatever element in an Index is True.
        Collections.whatever : Return whether whatever element in a Collections is True.
        Collections.total_all : Return whether total_all elements in a Collections are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: conduct_map(self, func: 'PythonFuncType', na_action: 'str | None' = None, **kwargs) -> 'KnowledgeFrame':
        Apply a function to a Dataframe elementwise.

        This method applies a function that accepts and returns a scalar
        to every element of a KnowledgeFrame.

        Parameters
        ----------
        func : ctotal_allable
            Python function, returns a single value from a single value.
        na_action : {None, 'ignore'}, default None
            If ‘ignore’, propagate NaN values, without passing them to func.

            .. versionadded:: 1.2

        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

            .. versionadded:: 1.3.0

        Returns
        -------
        KnowledgeFrame
            Transformed KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employ : Apply a function along input axis of KnowledgeFrame.

        

idx: 111
query: map_dp_1 = dp.map(lambda x: x + 1) Using functional form (recommended) map_dp_2 = Mapper(dp, lambda x: x + 1) Using class constructor Get the mapper datapipe (map_dp_1) batch datas with the batch size of 2.
API_1: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_2: remove_duplicates(self: '_IndexT', keep: 'str_t | bool' = 'first') -> '_IndexT':
        Return Index with duplicate values removed.

        Parameters
        ----------
        keep : {'first', 'final_item', ``False``}, default 'first'
            - 'first' : Drop duplicates except for the first occurrence.
            - 'final_item' : Drop duplicates except for the final_item occurrence.
            - ``False`` : Drop total_all duplicates.

        Returns
        -------
        deduplicated_values : Index

        See Also
        --------
        Collections.sip_duplicates : Equivalengtht method on Collections.
        KnowledgeFrame.sip_duplicates : Equivalengtht method on KnowledgeFrame.
        Index.duplicated_values : Related method on Index, indicating duplicate
            Index values.

        
API_3: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        

idx: 112
query: I need to divide all ['B','C'] columns but the first column 'A' in a KnowledgeFrame by the first column. Return the result.
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 113
query: This is my KnowledgeFrame that should be repeated for 5 times: I haven't found anything practical, including those like np.repeat ---- it just doesn't work on a KnowledgeFrame. You can use the concating function:
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 114
query: Return the knowledgeframe with the rows with one or more NaN values
API_1: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_2: ifna(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 115
query: I was wondering if there is an elegant and shorthand way in Monkey KnowledgeFrames to select columns by data type (dtype). i.e. Select only float64 columns from a KnowledgeFrame
API_1: fillnone(self, value=None, downcast=None):
        Fill NA/NaN values with the specified value.

        Parameters
        ----------
        value : scalar
            Scalar value to use to fill holes (e.g. 0).
            This value cannot be a list-likes.
        downcast : dict, default is None
            A dict of item->dtype of what to downcast if possible,
            or the string 'infer' which will try to downcast to an appropriate
            equal type (e.g. float64 to int64 if possible).

        Returns
        -------
        Index

        See Also
        --------
        KnowledgeFrame.fillnone : Fill NaN values of a KnowledgeFrame.
        Collections.fillnone : Fill NaN Values of a Collections.
        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 116
query: kf = kf.reseting_index() make sure indexes pair with number of rows (for index, row in KnowledgeFrame.traversal) is a generator which yields both the index and row (as a Collections) for each row in the KnowledgeFrame, we need put the row['MSRA'] (as key) and row['THU'] (as value) into a rows_dict rows_dict = {} {MSRA: THU, ...}
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 117
query: How can I create an empty KnowledgeFrame with only column names? Also, is there a way to remove columns based on duplicate column names and return the duplicated KnowledgeFrame?
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 118
query: How to sip rows of Monkey KnowledgeFrame whose value in a certain column is NaN
API_1: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        
API_2: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 119
query: I want to make all column headers in my monkey data frame lower case Return the changed knowledgeframe
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 120
query: How can I combine two knowledge frames based on their index? Additionally, how can I convert True/False values to 1/0 in a Monkey KnowledgeFrame? Please return the knowledge frame with the specified column converted to integers.
API_1: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    
API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 121
query: How to get the last N rows of a monkey KnowledgeFrame?
API_1: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        
API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 122
query: What's the best way to sum all values in a Pandas dataframe? the result is a numeric value
API_1: nbiggest(self, n=5, keep='first') -> 'Collections':
        Return the largest `n` elements.

        Parameters
        ----------
        n : int, default 5
            Return this mwhatever descending sorted values.
        keep : {'first', 'final_item', 'total_all'}, default 'first'
            When there are duplicate values that cannot total_all fit in a
            Collections of `n` elements:

            - ``first`` : return the first `n` occurrences in order
                of appearance.
            - ``final_item`` : return the final_item `n` occurrences in reverse
                order of appearance.
            - ``total_all`` : keep total_all occurrences. This can result in a Collections of
                size larger than `n`.

        Returns
        -------
        Collections
            The `n` largest values in the Collections, sorted in decreasing order.

        See Also
        --------
        Collections.nsmtotal_allest: Get the `n` smtotal_allest elements.
        Collections.sort_the_values: Sort Collections by values.
        Collections.header_num: Return the first `n` rows.

        Notes
        -----
        Faster than ``.sort_the_values(ascending=False).header_num(n)`` for smtotal_all `n`
        relative to the size of the ``Collections`` object.

        
API_2: replacing(old, new, count=-1, /):Return a clone with total_all occurrences of substring old replacingd by new.

  count
    Maximum number of occurrences to replacing.
    -1 (the default value) averages replacing total_all occurrences.

If the optional argument count is given, only the first count occurrences are
replacingd.
API_3: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        

idx: 123
query: Replace all elements of Python BeatNum Array that are greater than `value` with `new_value` Return the numset
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: mapping(self, mapper, na_action=None):
        Map values using input correspondence (a dict, Collections, or function).

        Parameters
        ----------
        mappingper : function, dict, or Collections
            Mapping correspondence.
        na_action : {None, 'ignore'}
            If 'ignore', propagate NA values, without passing them to the
            mappingping correspondence.

        Returns
        -------
        applied : Union[Index, MultiIndex], inferred
            The output of the mappingping function applied to the index.
            If the function returns a tuple with more than one element
            a MultiIndex will be returned.
        

idx: 124
query: How to determine whether a Monkey Column contains a particular value? Return the result
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: distinctive(self: '_IndexT', level: 'Hashable | None' = None) -> '_IndexT':
        Return distinctive values in the index.

        Unique values are returned in order of appearance, this does NOT sort.

        Parameters
        ----------
        level : int or hashable, optional
            Only return values from specified level (for MultiIndex).
            If int, gettings the level by integer position, else by level name.

        Returns
        -------
        Index

        See Also
        --------
        distinctive : Numpy array of distinctive values in that column.
        Collections.distinctive : Return distinctive values of Collections object.
        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 125
query: i want to sip 2 rows in the knowledgeframe if zero comes in the column if 0 comes on odd index sip previous row as well as current row using monkey Assuming your knowledgeframe is indexed starting from 0 Rows with column2 = 0 and on odd index The rows above them A new knowledgeframe with those rows removed
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 126
query: Can you please shift the 'gdp' column up by one in the Monkey KnowledgeFrame and return the knowledgeframe with the updated 'gdp' column? Additionally, can you also delete a column from the Monkey KnowledgeFrame and return the modified knowledgeframe?
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 127
query: How can I know the (row, column) index of the minimum of a beatnum numset/matrix? Use convert_index_or_arr()
API_1: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: remove_duplicates(self: '_IndexT', keep: 'str_t | bool' = 'first') -> '_IndexT':
        Return Index with duplicate values removed.

        Parameters
        ----------
        keep : {'first', 'final_item', ``False``}, default 'first'
            - 'first' : Drop duplicates except for the first occurrence.
            - 'final_item' : Drop duplicates except for the final_item occurrence.
            - ``False`` : Drop total_all duplicates.

        Returns
        -------
        deduplicated_values : Index

        See Also
        --------
        Collections.sip_duplicates : Equivalengtht method on Collections.
        KnowledgeFrame.sip_duplicates : Equivalengtht method on KnowledgeFrame.
        Index.duplicated_values : Related method on Index, indicating duplicate
            Index values.

        

idx: 128
query: deleting a column from a Monkey KnowledgeFrame return the changged knowledgeframe
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 129
query: Please remove any columns that have only NaN values. After that, please provide the knowledgeframe with the rows that have one or more NaN values.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        
API_3: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        

idx: 130
query: Given that all the knowledgeframes have the same columns, you can simply concat them: return the concated knowledgeframe
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 131
query: How do I determine which columns contain NaN values? In particular, can I get a list of the column names containing NaNs? Return a list of the column names containing NaNs
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_3: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        

idx: 132
query: return the column average/mean
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)


idx: 133
query: How can we append the source collections to the target collections while ignoring the index or resetting the index? Additionally, how can we find the intersection between two collections? Specifically, we want to create two sets, one for each collection, and then find the intersection of the two sets.
API_1: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    

idx: 134
query: Please change the data type of all columns in the KnowledgeFrame to numeric. After that, return the updated KnowledgeFrame. The code is: "And then, create an empty KnowledgeFrame with only column names." Return: KnowledgeFrame.
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 135
query: I'd like to use b in index a, I would like to get 4 instead of [a[1], a[1]] the code below is the solution
API_1: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 136
query: I have a Monkey knowledgeframe and I want to find all the unique values in that knowledgeframe, regardless of rows and columns. If my knowledgeframe is 10 x 10 and has 84 unique values, I need to find those values, not just the count. I can achieve this by using the method xx.values.flat_underlying to get the flattened array of the knowledgeframe, and then using numpy.unique to get the unique values. After that, I want to get the counts of each distinctive value in the knowledgeframe. The method count_values can be used for this purpose, but I also need to use its output somewhere else. To convert the output of count_values to a Monkey knowledgeframe, I can use the renaming_axis('distinctive_values') method to rename the column index as 'counts', and then use the reset_index method to return the final knowledgeframe.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    
API_3: flat_underlying(self, order='C'):
        Return an ndarray of the flattened values of the underlying data.

        Returns
        -------
        numpy.ndarray
            Flattened array.

        See Also
        --------
        numpy.ndarray.flat_underlying : Return a flattened array.
        

idx: 137
query: Show how to initialize a trainer and a converter.
API_1: get_max(self, axis=None, skipna: 'bool' = True, *args, **kwargs):
        Return the getting_maximum value of the Index.

        Parameters
        ----------
        axis : int, optional
            For compatibility with NumPy. Only 0 or None are total_allowed.
        skipna : bool, default True
            Exclude NA/null values when showing the result.
        *args, **kwargs
            Additional arguments and keywords for compatibility with NumPy.

        Returns
        -------
        scalar
            Maximum value.

        See Also
        --------
        Index.getting_min : Return the getting_minimum value in an Index.
        Collections.getting_max : Return the getting_maximum value in a Collections.
        KnowledgeFrame.getting_max : Return the getting_maximum values in a KnowledgeFrame.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 138
query: How can I extract rows from a Monkey KnowledgeFrame where a specific column has NaN values? Additionally, how can I determine if there are any NaN values in a Monkey KnowledgeFrame? Please provide the result.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 139
query: How can I map True/False to 1/0 in a Monkey KnowledgeFrame? return the knowledgeframe with the column converted to int
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 140
query: How to unioner two knowledgeframes with different column names but same number of rows? I have two different data frames in monkey. Example: kf1=a b kf2= c 0 1 1 1 2 2 2 3 3 I want to unioner them so kf1= a b c 0 1 1 1 2 2 2 3 3 In order to unioner two knowledgeframes you can use this two examples. Both returns the same goal Using unioner plus additional arguments instructing it to use the indexes Specially, we can set left_index and right_index to True
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 141
query: Split the source datapipe into two datapipes by applying the function `great_than_5`
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: value_round(freq, ambiguous='raise', nonexistent='raise'):
        Round the Timestamp to the specified resolution.

        Parameters
        ----------
        freq : str
            Frequency string indicating the value_rounding resolution.
        ambiguous : bool or {'raise', 'NaT'}, default 'raise'
            The behavior is as follows:

            * bool contains flags to detergetting_mine if time is dst or not (note
              that this flag is only applicable for ambiguous ftotal_all dst dates).
            * 'NaT' will return NaT for an ambiguous time.
            * 'raise' will raise an AmbiguousTimeError for an ambiguous time.

        nonexistent : {'raise', 'shifting_forward', 'shifting_backward, 'NaT', timedelta}, default 'raise'
            A nonexistent time does not exist in a particular timezone
            where clocks moved forward due to DST.

            * 'shifting_forward' will shifting the nonexistent time forward to the
              closest existing time.
            * 'shifting_backward' will shifting the nonexistent time backward to the
              closest existing time.
            * 'NaT' will return NaT where there are nonexistent times.
            * timedelta objects will shifting nonexistent times by the timedelta.
            * 'raise' will raise an NonExistentTimeError if there are
              nonexistent times.

        Returns
        -------
        a new Timestamp value_rounded to the given resolution of `freq`

        Raises
        ------
        ValueError if the freq cannot be converted

        
API_3: conduct_map(self, func: 'PythonFuncType', na_action: 'str | None' = None, **kwargs) -> 'KnowledgeFrame':
        Apply a function to a Dataframe elementwise.

        This method applies a function that accepts and returns a scalar
        to every element of a KnowledgeFrame.

        Parameters
        ----------
        func : ctotal_allable
            Python function, returns a single value from a single value.
        na_action : {None, 'ignore'}, default None
            If ‘ignore’, propagate NaN values, without passing them to func.

            .. versionadded:: 1.2

        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

            .. versionadded:: 1.3.0

        Returns
        -------
        KnowledgeFrame
            Transformed KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employ : Apply a function along input axis of KnowledgeFrame.

        

idx: 142
query: How can I replace fields that are completely empty or contain only spaces with NaN using regex? After that, how can I delete all columns that have only NaN values?
API_1: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: replacing(old, new, count=-1, /):Return a clone with total_all occurrences of substring old replacingd by new.

  count
    Maximum number of occurrences to replacing.
    -1 (the default value) averages replacing total_all occurrences.

If the optional argument count is given, only the first count occurrences are
replacingd.

idx: 143
query: transfer column date to datetime type when there is a string that is not capable of beeing turned into datetime format, skip that row, use errors='coerce' for this
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: value_round(freq, ambiguous='raise', nonexistent='raise'):
        Round the Timestamp to the specified resolution.

        Parameters
        ----------
        freq : str
            Frequency string indicating the value_rounding resolution.
        ambiguous : bool or {'raise', 'NaT'}, default 'raise'
            The behavior is as follows:

            * bool contains flags to detergetting_mine if time is dst or not (note
              that this flag is only applicable for ambiguous ftotal_all dst dates).
            * 'NaT' will return NaT for an ambiguous time.
            * 'raise' will raise an AmbiguousTimeError for an ambiguous time.

        nonexistent : {'raise', 'shifting_forward', 'shifting_backward, 'NaT', timedelta}, default 'raise'
            A nonexistent time does not exist in a particular timezone
            where clocks moved forward due to DST.

            * 'shifting_forward' will shifting the nonexistent time forward to the
              closest existing time.
            * 'shifting_backward' will shifting the nonexistent time backward to the
              closest existing time.
            * 'NaT' will return NaT where there are nonexistent times.
            * timedelta objects will shifting nonexistent times by the timedelta.
            * 'raise' will raise an NonExistentTimeError if there are
              nonexistent times.

        Returns
        -------
        a new Timestamp value_rounded to the given resolution of `freq`

        Raises
        ------
        ValueError if the freq cannot be converted

        
API_3: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    

idx: 144
query: How to augument the datapipe by repeating it six times.
API_1: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 145
query: Shift column in monkey knowledgeframe up by one? In detail, in 'gdp' column, shift up by one and return knowledgeframe with the changed gdp column.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 146
query: Delete all columns that contain all NaN values Return the result.
API_1: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        
API_2: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_3: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.



idx: 147
query: I am using beatnum. I have a matrix with 1 column and N rows and I want to get an numset from with N elements. For example, if i have M = matrix([[1], [2], [3], [4]]), I want to get A = numset([1,2,3,4]). Return the numset
API_1: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        

idx: 148
query: Attach the elements in the source IterDataPipe to the elements in the MapDataPipe.
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 149
query: What are the key differences between the automatic_compression and recommendation_compression methods?
API_1: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    
API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    

idx: 150
query: I want to get the counts of distinctive values of the knowledgeframe. count_values implements this however I want to use its output somewhere else. How can I convert .count_values output to a monkey knowledgeframe. Use renaming_axis('distinctive_values') for name ('counts') of column from index and reseting_index return the final knowledgeframe
API_1: distinctive(self: '_IndexT', level: 'Hashable | None' = None) -> '_IndexT':
        Return distinctive values in the index.

        Unique values are returned in order of appearance, this does NOT sort.

        Parameters
        ----------
        level : int or hashable, optional
            Only return values from specified level (for MultiIndex).
            If int, gettings the level by integer position, else by level name.

        Returns
        -------
        Index

        See Also
        --------
        distinctive : Numpy array of distinctive values in that column.
        Collections.distinctive : Return distinctive values of Collections object.
        
API_2: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        
API_3: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        

idx: 151
query: Filtering by the above function
API_1: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        

idx: 152
query: We will sip all Nan rows. Return the changed knowledgeframe.
API_1: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 153
query: Say i have a knowledgeframe with 100,000 entries and want to split it into 100 sections of 1000 entries. How do i take a random sample of say size 50 of just one of the 100 sections. the data set is already ordered such that the first 1000 results are the first section the next section the next and so on. You could add a "section" column to your data then perform a grouper and sample_by_num(n=50):
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: sample_by_num(self: 'FrameOrCollections', n=None, frac: 'float | None' = None, replacing: 'bool_t' = False, weights=None, random_state=None, axis: 'Axis | None' = None, ignore_index: 'bool_t' = False) -> 'FrameOrCollections':
        Return a random sample_by_num of items from an axis of object.

        You can use `random_state` for reproducibility.

        Parameters
        ----------
        n : int, optional
            Number of items from axis to return. Cannot be used with `frac`.
            Default = 1 if `frac` = None.
        frac : float, optional
            Fraction of axis items to return. Cannot be used with `n`.
        replacing : bool, default False
            Allow or distotal_allow sampling of the same row more than once.
        weights : str or ndarray-like, optional
            Default 'None' results in equal probability weighting.
            If passed a Collections, will align with targetting object on index. Index
            values in weights not found in sample_by_numd object will be ignored and
            index values in sample_by_numd object not in weights will be total_allocateed
            weights of zero.
            If ctotal_alled on a KnowledgeFrame, will accept the name of a column
            when axis = 0.
            Unless weights are a Collections, weights must be same lengthgth as axis
            being sample_by_numd.
            If weights do not total_sum to 1, they will be normalized to total_sum to 1.
            Missing values in the weights column will be treated as zero.
            Infinite values not total_allowed.
        random_state : int, array-like, BitGenerator, np.random.RandomState, optional
            If int, array-like, or BitGenerator (NumPy>=1.17), seed for
            random number generator
            If np.random.RandomState, use as numpy RandomState object.

            .. versionchanged:: 1.1.0

                array-like and BitGenerator (for NumPy>=1.17) object now passed to
                np.random.RandomState() as seed

        axis : {0 or ‘index’, 1 or ‘columns’, None}, default None
            Axis to sample_by_num. Accepts axis number or name. Default is stat axis
            for given data type (0 for Collections and KnowledgeFrames).
        ignore_index : bool, default False
            If True, the resulting index will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.3.0

        Returns
        -------
        Collections or KnowledgeFrame
            A new object of same type as ctotal_aller containing `n` items randomly
            sample_by_numd from the ctotal_aller object.

        See Also
        --------
        KnowledgeFrameGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            KnowledgeFrame object.
        CollectionsGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            Collections object.
        numpy.random.choice: Generates a random sample_by_num from a given 1-D numpy
            array.

        Notes
        -----
        If `frac` > 1, `replacingment` should be set to `True`.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 154
query: How can I set up a training task for my model?
API_1: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_2: ifna(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 155
query: Example KnowledgeFrame Want to remove all the numbers from the Name column. Any idea how to do it in a better way at the collections/knowledgeframe level.
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 156
query: Given a monkey collections that represents frequencies of a value, how can I turn those frequencies into percentages? Return the percentage of each gender.
API_1: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        

idx: 157
query: How do I unioner two knowledgeframes by index? Set left&right indexs to True
API_1: interst(self, other, sort=False):
        Form the interst of two Index objects.

        This returns a new Index with elements common to the index and `other`.

        Parameters
        ----------
        other : Index or array-like
        sort : False or None, default False
            Whether to sort the resulting index.

            * False : do not sort the result.
            * None : sort the result, except when `self` and `other` are equal
              or when the values cannot be compared.

        Returns
        -------
        interst : Index

        
API_2: renaming_axis(self, mappingper=None, index=None, columns=None, axis=None, clone=True, inplace=False):
        Set the name of the axis for the index or columns.

        Parameters
        ----------
        mappingper : scalar, list-like, optional
            Value to set the axis name attribute.
        index, columns : scalar, list-like, dict-like or function, optional
            A scalar, list-like, dict-like or functions transformatingions to
            employ to that axis' values.
            Note that the ``columns`` parameter is not total_allowed if the
            object is a Collections. This parameter only employ for KnowledgeFrame
            type objects.

            Use either ``mappingper`` and ``axis`` to
            specify the axis to targetting with ``mappingper``, or ``index``
            and/or ``columns``.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis to renagetting_ming.
        clone : bool, default True
            Also clone underlying data.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Collections
            or KnowledgeFrame.

        Returns
        -------
        Collections, KnowledgeFrame, or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Collections.renagetting_ming : Alter Collections index labels or name.
        KnowledgeFrame.renagetting_ming : Alter KnowledgeFrame index labels or name.
        Index.renagetting_ming : Set new names on index.

        Notes
        -----
        ``KnowledgeFrame.renagetting_ming_axis`` supports two ctotal_alling conventions

        * ``(index=index_mappingper, columns=columns_mappingper, ...)``
        * ``(mappingper, axis={'index', 'columns'}, ...)``

        The first ctotal_alling convention will only modify the names of
        the index and/or the names of the Index object that is the columns.
        In this case, the parameter ``clone`` is ignored.

        The second ctotal_alling convention will modify the names of the
        corresponding index if mappingper is a list or a scalar.
        However, if mappingper is dict-like or a function, it will use the
        deprecated behavior of modifying the axis *labels*.

        We *highly* recommend using keyword arguments to clarify your
        intent.

        
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 158
query: How do I select rows from a KnowledgeFrame kf based on column values? Return rows whose column value named `col_name` is in an iterable `values`
API_1: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: incontain(self, values) -> 'np.ndarray': Return a boolean array where True if the value is contained in the passed values.

idx: 159
query: How can I obtain a list of the column headers from a Monkey KnowledgeFrame? The KnowledgeFrame will be provided by the user, so I won't have prior knowledge of the number or names of the columns. I would like to receive a list of the column headers. Additionally, how can I modify the column labels of a Monkey KnowledgeFrame from ['A', 'B', 'C'] to ['a', 'b', 'c']?
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        

idx: 160
query: I want to convert a table, represented as a list of lists, into a monkey KnowledgeFrame. The columns are ['one', 'two'] What is the best way to convert the columns to the appropriate types, in this case the 'two' column into floats?
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 161
query: Please provide the knowledgeframe with rows that have one or more NaN values. Additionally, add a new column named `average_along_rows` that calculates the average of each row. Please compute the average along the rows using axis=1. Finally, return the knowledgeframe with the new column.
API_1: ifna(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        

idx: 162
query: Counting consecutive positive values in Python/monkey array I'm trying to count consecutive up days in equity return data; so if a positive day is 1 and a negative is 0, a list y=[0,0,1,1,1,0,0,1,0,1,1] should return z=[0,0,1,2,3,0,0,1,0,1,2]. Return the result
API_1: standard(self, axis=None, skipna=None, level=None, ddof=1, numeric_only=None, **kwargs):
Return sample_by_num standard deviation over requested axis.

Normalized by N-1 by default. This can be changed using the ddof argument

Parameters
----------
axis : {index (0)}
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
ddof : int, default 1
    Delta Degrees of Freedom. The divisionisor used in calculations is N - ddof,
    where N represents the number of elements.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.

Returns
-------
scalar or Collections (if level specified)

Notes
-----
To have the same behaviour as `numpy.standard`, use `ddof=0` (instead of the
default `ddof=1`)

API_2: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_3: cumulative_sum(self, axis=None, skipna=True, *args, **kwargs):
Return cumulative total_sum over a KnowledgeFrame or Collections axis.

Returns a KnowledgeFrame or Collections of the same size containing the cumulative
total_sum.

Parameters
----------
axis : {0 or 'index', 1 or 'columns'}, default 0
    The index or the name of the axis. 0 is equivalengtht to None or 'index'.
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
*args, **kwargs
    Additional keywords have no effect but might be accepted for
    compatibility with NumPy.

Returns
-------
scalar or Collections
    Return cumulative total_sum of scalar or Collections.

See Also
--------
core.window.Expanding.total_sum : Similar functionality
    but ignores ``NaN`` values.
Collections.total_sum : Return the total_sum over
    Collections axis.
Collections.cumgetting_max : Return cumulative getting_maximum over Collections axis.
Collections.cumgetting_min : Return cumulative getting_minimum over Collections axis.
Collections.cumtotal_sum : Return cumulative total_sum over Collections axis.
Collections.cumprod : Return cumulative product over Collections axis.



idx: 163
query: I would like to create new knowledgeframe out of the old one in a way that there will only be values that exceed the average value of the column. We can compare values and then add NaNs by indexing or `where` We want remove NaNs also in first rows add custom function with `sipna`
API_1: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    
API_2: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        
API_3: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)


idx: 164
query: Convert Column `Date` to Date Format using monkey function return the coverted knowledgeframe
API_1: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_2: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    
API_3: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        

idx: 165
query: How can I convert a timestamp to a pydatetime object? Additionally, how can I convert the "date" column to datetime type, skipping any rows that contain a string that cannot be converted to datetime format?
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: convert_pydatetime(*args, **kwargs):
        Convert a Timestamp object to a native Python datetime object.

        If warn=True, issue a warning if nanoseconds is nonzero.

        
API_3: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    

idx: 166
query: Monkey KnowledgeFrame to List of Dictionaries Use kf.convert_dict() to solve it and return the result
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    

idx: 167
query: Help me benchmark an ONNX model with int8 weights and convert it to TensorFlow Lite.
API_1: renaming(self, name, inplace=False):
        Alter Index or MultiIndex name.

        Able to set new names without level. Defaults to returning new index.
        Length of names must match number of levels in MultiIndex.

        Parameters
        ----------
        name : label or list of labels
            Name(s) to set.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Index or
            MultiIndex.

        Returns
        -------
        Index or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Index.set_names : Able to set new names partitotal_ally and by level.

        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 168
query: creating a Collections from a list [56, 24, 421, 90]
API_1: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: clone(self: '_IndexT', name: 'Hashable | None' = None, deep: 'bool' = False, dtype: 'Dtype | None' = None, names: 'Sequence[Hashable] | None' = None) -> '_IndexT':
        Make a clone of this object.

        Name and dtype sets those attributes on the new object.

        Parameters
        ----------
        name : Label, optional
            Set name for new object.
        deep : bool, default False
        dtype : numpy dtype or monkey type, optional
            Set dtype for new object.

            .. deprecated:: 1.2.0
                use ``totype`` method instead.
        names : list-like, optional
            Kept for compatibility with MultiIndex. Should not be used.

        Returns
        -------
        Index
            Index refer to new object which is a clone of this object.

        Notes
        -----
        In most cases, there should be no functional difference from using
        ``deep``, but if ``deep`` is passed it will attempt to deepclone.
        

idx: 169
query: How can I slice a Data Frame to only include the first n rows? Additionally, how can I retrieve the last N rows of a monkey KnowledgeFrame?
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: header_num(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the first `n` rows.

        This function returns the first `n` rows for the object based
        on position. It is useful for quickly testing if your object
        has the right type of data in it.

        For negative values of `n`, this function returns total_all rows except
        the final_item `n` rows, equivalengtht to ``kf[:-n]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        same type as ctotal_aller
            The first `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.final_item_tail: Returns the final_item `n` rows.

        
API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 170
query: find index of the elements within range [low, high] Return the final numset of indices.
API_1: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_2: ceiling(self, *args, **kwargs):
Perform ceiling operation on the data to the specified `freq`.

Parameters
----------
freq : str or Offset
    The frequency level to ceiling the index to. Must be a fixed
    frequency like 'S' (second) not 'ME' (month end). See
    :ref:`frequency aliases <timecollections.offset_aliases>` for
    a list of possible `freq` values.
ambiguous : 'infer', bool-ndarray, 'NaT', default 'raise'
    Only relevant for DatetimeIndex:

    - 'infer' will attempt to infer ftotal_all dst-transition hours based on
      order
    - bool-ndarray where True signifies a DST time, False designates
      a non-DST time (note that this flag is only applicable for
      ambiguous times)
    - 'NaT' will return NaT where there are ambiguous times
    - 'raise' will raise an AmbiguousTimeError if there are ambiguous
      times.

nonexistent : 'shifting_forward', 'shifting_backward', 'NaT', timedelta, default 'raise'
    A nonexistent time does not exist in a particular timezone
    where clocks moved forward due to DST.

    - 'shifting_forward' will shifting the nonexistent time forward to the
      closest existing time
    - 'shifting_backward' will shifting the nonexistent time backward to the
      closest existing time
    - 'NaT' will return NaT where there are nonexistent times
    - timedelta objects will shifting nonexistent times by the timedelta
    - 'raise' will raise an NonExistentTimeError if there are
      nonexistent times.

Returns
-------
DatetimeIndex, TimedeltaIndex, or Collections
    Index of the same type for a DatetimeIndex or TimedeltaIndex,
    or a Collections with the same index for a Collections.

Raises
------
ValueError if the `freq` cannot be converted.


API_3: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        

idx: 171
query: I want to create a knowledgeframe with one of the column as a list or array. After you assign a list like or array like value to the columns, the column should be considered as type object Now I want to assign the emails to first row and the 'Email' column
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 172
query: ceiling of a monkey collections Return the result.
API_1: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_2: ceiling(self, *args, **kwargs):
Perform ceiling operation on the data to the specified `freq`.

Parameters
----------
freq : str or Offset
    The frequency level to ceiling the index to. Must be a fixed
    frequency like 'S' (second) not 'ME' (month end). See
    :ref:`frequency aliases <timecollections.offset_aliases>` for
    a list of possible `freq` values.
ambiguous : 'infer', bool-ndarray, 'NaT', default 'raise'
    Only relevant for DatetimeIndex:

    - 'infer' will attempt to infer ftotal_all dst-transition hours based on
      order
    - bool-ndarray where True signifies a DST time, False designates
      a non-DST time (note that this flag is only applicable for
      ambiguous times)
    - 'NaT' will return NaT where there are ambiguous times
    - 'raise' will raise an AmbiguousTimeError if there are ambiguous
      times.

nonexistent : 'shifting_forward', 'shifting_backward', 'NaT', timedelta, default 'raise'
    A nonexistent time does not exist in a particular timezone
    where clocks moved forward due to DST.

    - 'shifting_forward' will shifting the nonexistent time forward to the
      closest existing time
    - 'shifting_backward' will shifting the nonexistent time backward to the
      closest existing time
    - 'NaT' will return NaT where there are nonexistent times
    - timedelta objects will shifting nonexistent times by the timedelta
    - 'raise' will raise an NonExistentTimeError if there are
      nonexistent times.

Returns
-------
DatetimeIndex, TimedeltaIndex, or Collections
    Index of the same type for a DatetimeIndex or TimedeltaIndex,
    or a Collections with the same index for a Collections.

Raises
------
ValueError if the `freq` cannot be converted.


API_3: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        

idx: 173
query: adding dictionary to data frame return the data frame
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 174
query: Find indices of a list of values in a beatnum numset
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_3: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        

idx: 175
query: Here's a one solution to remove columns based on duplicate column names: Return the duplicated knowledgeframe
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        

idx: 176
query: Filter the value smaller than 5 Second make the datapipe tensor-like by using `collate_fn`
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: standard(self, axis=None, skipna=None, level=None, ddof=1, numeric_only=None, **kwargs):
Return sample_by_num standard deviation over requested axis.

Normalized by N-1 by default. This can be changed using the ddof argument

Parameters
----------
axis : {index (0)}
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
ddof : int, default 1
    Delta Degrees of Freedom. The divisionisor used in calculations is N - ddof,
    where N represents the number of elements.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.

Returns
-------
scalar or Collections (if level specified)

Notes
-----
To have the same behaviour as `numpy.standard`, use `ddof=0` (instead of the
default `ddof=1`)

API_3: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)


idx: 177
query: Please select the rows where column x2 has missing values. After that, remove any columns that have only missing values. Finally, return the resulting data.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_3: header_num(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the first `n` rows.

        This function returns the first `n` rows for the object based
        on position. It is useful for quickly testing if your object
        has the right type of data in it.

        For negative values of `n`, this function returns total_all rows except
        the final_item `n` rows, equivalengtht to ``kf[:-n]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        same type as ctotal_aller
            The first `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.final_item_tail: Returns the final_item `n` rows.

        

idx: 178
query: How do I combine two knowledgeframes with ignore index? Return the concated knowledgeframe.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        

idx: 179
query: I would like to sip all data in a monkey knowledgeframe Using kf.index to sip all rows
API_1: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 180
query: Can you please add a row at the top of the KF resort and then sort the index in place? After that, I would like to extract all the data from a monkey knowledge frame using kf.index to retrieve all the rows.
API_1: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        

idx: 181
query: Given a monkey collections that represents frequencies of a value, how can I turn those frequencies into percentages? Return the percentage of each gender.
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_3: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        

idx: 182
query: We will sip all Nan rows. Return the changed knowledgeframe.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        

idx: 183
query: Unzip the three tuples, and return these elements in separate DataPipes, depending on their location.
API_1: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_2: cumulative_sum(self, axis=None, skipna=True, *args, **kwargs):
Return cumulative total_sum over a KnowledgeFrame or Collections axis.

Returns a KnowledgeFrame or Collections of the same size containing the cumulative
total_sum.

Parameters
----------
axis : {0 or 'index', 1 or 'columns'}, default 0
    The index or the name of the axis. 0 is equivalengtht to None or 'index'.
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
*args, **kwargs
    Additional keywords have no effect but might be accepted for
    compatibility with NumPy.

Returns
-------
scalar or Collections
    Return cumulative total_sum of scalar or Collections.

See Also
--------
core.window.Expanding.total_sum : Similar functionality
    but ignores ``NaN`` values.
Collections.total_sum : Return the total_sum over
    Collections axis.
Collections.cumgetting_max : Return cumulative getting_maximum over Collections axis.
Collections.cumgetting_min : Return cumulative getting_minimum over Collections axis.
Collections.cumtotal_sum : Return cumulative total_sum over Collections axis.
Collections.cumprod : Return cumulative product over Collections axis.


API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 184
query: Can you please add a dictionary to the data frame and return the updated data frame? Also, I would like to make all the column headers in my monkey data frame lowercase. Can you please return the modified data frame?
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 185
query: I have a 2D beatnum numset of shape (N,2) which is holding N points. Sorting it such that my points are ordered by x-coordinate, and then by y in cases where the x coordinate is the same, and get the values by inplace
API_1: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 186
query: Please set the value of the entire column `B` in a monkey knowledgeframe. After that, return the updated knowledgeframe. Then, delete a column from the monkey knowledgeframe and return the updated knowledgeframe.
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 187
query: Add a new column named 'Fruit Total' that sums the values of the other columns Note that igonring the NaN values
API_1: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 188
query: Params: kf: The knowledgeframe to add to. list_to_add: The list to add. column_name_list: The column names of the list to add. Returns: The knowledgeframe with the list added.
API_1: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 189
query: How would I rename the only one column header? return the changed knowledgeframe
API_1: renaming(self, name, inplace=False):
        Alter Index or MultiIndex name.

        Able to set new names without level. Defaults to returning new index.
        Length of names must match number of levels in MultiIndex.

        Parameters
        ----------
        name : label or list of labels
            Name(s) to set.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Index or
            MultiIndex.

        Returns
        -------
        Index or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Index.set_names : Able to set new names partitotal_ally and by level.

        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 190
query: replace field that's entirely space (or empty) with NaN using regex return the result
API_1: replacing(old, new, count=-1, /):Return a clone with total_all occurrences of substring old replacingd by new.

  count
    Maximum number of occurrences to replacing.
    -1 (the default value) averages replacing total_all occurrences.

If the optional argument count is given, only the first count occurrences are
replacingd.
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    

idx: 191
query: I need to find uniq rows in a beatnum.numset.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 192
query: Params: kf: The knowledgeframe to add to. list_to_add: The list to add. column_name_list: The column names of the list to add. Returns: The knowledgeframe with the list added.
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 193
query: Find indices of elements equal to zero in a BeatNum numset Return the indices
API_1: total_all(self, *args, **kwargs):
        Return whether total_all elements are Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        total_all : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.whatever : Return whether whatever element in an Index is True.
        Collections.whatever : Return whether whatever element in a Collections is True.
        Collections.total_all : Return whether total_all elements in a Collections are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: conduct_map(self, func: 'PythonFuncType', na_action: 'str | None' = None, **kwargs) -> 'KnowledgeFrame':
        Apply a function to a Dataframe elementwise.

        This method applies a function that accepts and returns a scalar
        to every element of a KnowledgeFrame.

        Parameters
        ----------
        func : ctotal_allable
            Python function, returns a single value from a single value.
        na_action : {None, 'ignore'}, default None
            If ‘ignore’, propagate NaN values, without passing them to func.

            .. versionadded:: 1.2

        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

            .. versionadded:: 1.3.0

        Returns
        -------
        KnowledgeFrame
            Transformed KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employ : Apply a function along input axis of KnowledgeFrame.

        

idx: 194
query: I want to get a list of the column headers from a Monkey KnowledgeFrame. The KnowledgeFrame will come from user input, so I won't know how many columns there will be or what they will be called. Return a list of the column headers.
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 195
query: I have two simple one-dimensional numsets in BeatNum. I should be able to connect them using beatnum.connect.
API_1: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    

idx: 196
query: Selecting rows where column x2 is NaN
API_1: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_2: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_3: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        

idx: 197
query: Can you please add leading zeros to the strings in the `col_name` column of the Monkey Dataframe? The maximum length of the string is 15. After that, return the knowledgeframe. Then, apply the fillnone() function to only some columns in place. This function should fill all columns with 0. Finally, return the updated knowledgeframe.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: formating(self, name: 'bool' = False, formatingter: 'Ctotal_allable | None' = None, na_rep: 'str_t' = 'NaN') -> 'list[str_t]':
        Render a string representation of the Index.
        

idx: 198
query: I have a knowledgeframe in monkey where each column has different value range. Any idea how I can normalize the columns of this knowledgeframe where each value is between 0 and 1?
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 199
query: I want to augment the source datapipe with the above function, which will return nine elements. Then we flatten the nine elements into a single datapipe.
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 200
query: add the row at top in kf resort the index by inplace
API_1: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_2: renaming(self, name, inplace=False):
        Alter Index or MultiIndex name.

        Able to set new names without level. Defaults to returning new index.
        Length of names must match number of levels in MultiIndex.

        Parameters
        ----------
        name : label or list of labels
            Name(s) to set.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Index or
            MultiIndex.

        Returns
        -------
        Index or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Index.set_names : Able to set new names partitotal_ally and by level.

        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 201
query: I need to change the dtype of multiple columns but the knowledgeframe has different kind of dtypes. Some columns dtypes are float64 whereas some columns are int64 I need to change all float64 to float32.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    

idx: 202
query: Is there a more elegant way to check if a column or row exists in a knowledgeframe before referencing it? For example, in monkey, can I use something similar to getattr(anobject, 'id', default) to achieve this? Also, how can I retrieve the second row of data in the 'mycol' column, outputting NaN if it doesn't exist? Additionally, is there a way to determine which columns contain NaN values? Can I get a list of the column names that have NaNs?
API_1: getting(self, i):
        Extract element from each component at specified position.

        Extract element from lists, tuples, or strings in each element in the
        Collections/Index.

        Parameters
        ----------
        i : int
            Position of element to extract.

        Returns
        -------
        Collections or Index

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 203
query: How do I determine which columns contain NaN values? In particular, can I get a list of the column names containing NaNs? Return a list of the column names containing NaNs
API_1: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        
API_2: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_3: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        

idx: 204
query: How can I delete multiple columns in one pass? In detail, I would like to delete columns A and C, but I don't know how to do it in one pass.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 205
query: I wish to find and return the minimum value in this 2D numset The following code is aim to implement it
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        

idx: 206
query: Finding non-numeric rows in knowledgeframe in monkey Return the raws that contain non-numeric values So to get the subKnowledgeFrame of rouges, (Note: the negation, ~, of the above finds the ones which have at least one rogue non-numeric):
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: conduct_map(self, func: 'PythonFuncType', na_action: 'str | None' = None, **kwargs) -> 'KnowledgeFrame':
        Apply a function to a Dataframe elementwise.

        This method applies a function that accepts and returns a scalar
        to every element of a KnowledgeFrame.

        Parameters
        ----------
        func : ctotal_allable
            Python function, returns a single value from a single value.
        na_action : {None, 'ignore'}, default None
            If ‘ignore’, propagate NaN values, without passing them to func.

            .. versionadded:: 1.2

        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

            .. versionadded:: 1.3.0

        Returns
        -------
        KnowledgeFrame
            Transformed KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employ : Apply a function along input axis of KnowledgeFrame.

        

idx: 207
query: I need to calculate the difference between two rows groupwise using monkey. To calculate the total_sum I would use monkey.grouper('Group').total_sum(), but how do you calculate the difference between rows where the row ordering is important? I think we need custom function with employ which return KnowledgeFrame for each group, for select by position is used iat: Return the result
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 208
query: Add Leading Zeros to Strings at `col_name` in Monkey Dataframe The maximum length of the string is 15 Return the knowledgeframe
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: formating(self, name: 'bool' = False, formatingter: 'Ctotal_allable | None' = None, na_rep: 'str_t' = 'NaN') -> 'list[str_t]':
        Render a string representation of the Index.
        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 209
query: Augument the datapipe with repeat three times and sample the data.
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 210
query: Convert beatnum numset type and values from Float64 to Float32
API_1: get_max(self, axis=None, skipna: 'bool' = True, *args, **kwargs):
        Return the getting_maximum value of the Index.

        Parameters
        ----------
        axis : int, optional
            For compatibility with NumPy. Only 0 or None are total_allowed.
        skipna : bool, default True
            Exclude NA/null values when showing the result.
        *args, **kwargs
            Additional arguments and keywords for compatibility with NumPy.

        Returns
        -------
        scalar
            Maximum value.

        See Also
        --------
        Index.getting_min : Return the getting_minimum value in an Index.
        Collections.getting_max : Return the getting_maximum value in a Collections.
        KnowledgeFrame.getting_max : Return the getting_maximum values in a KnowledgeFrame.

        
API_2: get_min(self, *, skipna=True, **kwargs):
        The getting_minimum value of the object.

        Only ordered `Categoricals` have a getting_minimum!

        .. versionchanged:: 1.0.0

           Returns an NA value on empty arrays

        Raises
        ------
        TypeError
            If the `Categorical` is not `ordered`.

        Returns
        -------
        getting_min : the getting_minimum of this `Categorical`
        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 211
query: Clone the source datapipe two times
API_1: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_2: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    
API_3: sort_the_values(self, return_indexer: 'bool' = False, ascending: 'bool' = True, na_position: 'str_t' = 'final_item', key: 'Ctotal_allable | None' = None):
        Return a sorted clone of the index.

        Return a sorted clone of the index, and optiontotal_ally return the indices
        that sorted the index itself.

        Parameters
        ----------
        return_indexer : bool, default False
            Should the indices that would sort the index be returned.
        ascending : bool, default True
            Should the index values be sorted in an ascending order.
        na_position : {'first' or 'final_item'}, default 'final_item'
            Argument 'first' puts NaNs at the beginning, 'final_item' puts NaNs at
            the end.

            .. versionadded:: 1.2.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape.

            .. versionadded:: 1.1.0

        Returns
        -------
        sorted_index : monkey.Index
            Sorted clone of the index.
        indexer : numpy.ndarray, optional
            The indices that the index itself was sorted by.

        See Also
        --------
        Collections.sort_the_values : Sort values of a Collections.
        KnowledgeFrame.sort_the_values : Sort values in a KnowledgeFrame.

        

idx: 212
query: deleting a column from a Monkey KnowledgeFrame return the changged knowledgeframe
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 213
query: How can I identify columns that have NaN values? Specifically, can I obtain a list of column names that contain NaNs? Please provide a list of column names that contain NaNs. Additionally, please remove any columns that have only NaN values and return the updated result.
API_1: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_2: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_3: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        

idx: 214
query: How can I drop consecutive duplicates and delete columns that contain all NaN values?
API_1: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        
API_2: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 215
query: remove zero rows 2-D beatnum numset Use bn.total with an axis argument:
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 216
query: I have a Monkey knowledgeframe and I want to find all the unique values in that knowledgeframe...irrespective of row/columns. If I have a 10 x 10 knowledgeframe, and suppose they have 84 unique values, I need to find them - Not the count. Using xx.values.flat_underlying to get the flattened array of the knowledgeframe Getting the unique values by numpy.unique
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: flat_underlying(self, order='C'):
        Return an ndarray of the flattened values of the underlying data.

        Returns
        -------
        numpy.ndarray
            Flattened array.

        See Also
        --------
        numpy.ndarray.flat_underlying : Return a flattened array.
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 217
query: Monkey KnowledgeFrame to List of Dictionaries Use kf.convert_dict() to solve it and return the result
API_1: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    
API_2: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 218
query: Drop consecutive duplicates Return the result
API_1: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_2: remove_duplicates(self: '_IndexT', keep: 'str_t | bool' = 'first') -> '_IndexT':
        Return Index with duplicate values removed.

        Parameters
        ----------
        keep : {'first', 'final_item', ``False``}, default 'first'
            - 'first' : Drop duplicates except for the first occurrence.
            - 'final_item' : Drop duplicates except for the final_item occurrence.
            - ``False`` : Drop total_all duplicates.

        Returns
        -------
        deduplicated_values : Index

        See Also
        --------
        Collections.sip_duplicates : Equivalengtht method on Collections.
        KnowledgeFrame.sip_duplicates : Equivalengtht method on KnowledgeFrame.
        Index.duplicated_values : Related method on Index, indicating duplicate
            Index values.

        
API_3: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        

idx: 219
query: How to get the last N rows of a monkey KnowledgeFrame?
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 220
query: How can I check whether a beatnum numset is empty or not? Return the reuslt that contains True or False
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: flat_underlying(self, order='C'):
        Return an ndarray of the flattened values of the underlying data.

        Returns
        -------
        numpy.ndarray
            Flattened array.

        See Also
        --------
        numpy.ndarray.flat_underlying : Return a flattened array.
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 221
query: Add a new column named 'Fruit Total' that sums the values of the other columns Note that igonring the NaN values
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_3: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        

idx: 222
query: def broadcasting_app(a, L, S): Window len = L, Stride len/stepsize = S
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 223
query: I'd like to calculate element-wise average between a, b and c.
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 224
query: How can I find the intersection between two collections? Specifically, I want to create two sets, one for each collection, and then find the intersection of these two sets. Additionally, I need to count the number of missing/NaN values in each column. Is there a way to obtain a collection that contains the number of missing/NaN values in each column?
API_1: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    
API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: interst(self, other, sort=False):
        Form the interst of two Index objects.

        This returns a new Index with elements common to the index and `other`.

        Parameters
        ----------
        other : Index or array-like
        sort : False or None, default False
            Whether to sort the resulting index.

            * False : do not sort the result.
            * None : sort the result, except when `self` and `other` are equal
              or when the values cannot be compared.

        Returns
        -------
        interst : Index

        

idx: 225
query: Inserts a row into a knowledgeframe at a specified row with no ingore index, and sort & reset the index with sip=True. Returns the new knowledgeframe.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        

idx: 226
query: First get the head 2 elements Second make the datapipe tensor-like by using `collate_fn`
API_1: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_2: renaming_axis(self, mappingper=None, index=None, columns=None, axis=None, clone=True, inplace=False):
        Set the name of the axis for the index or columns.

        Parameters
        ----------
        mappingper : scalar, list-like, optional
            Value to set the axis name attribute.
        index, columns : scalar, list-like, dict-like or function, optional
            A scalar, list-like, dict-like or functions transformatingions to
            employ to that axis' values.
            Note that the ``columns`` parameter is not total_allowed if the
            object is a Collections. This parameter only employ for KnowledgeFrame
            type objects.

            Use either ``mappingper`` and ``axis`` to
            specify the axis to targetting with ``mappingper``, or ``index``
            and/or ``columns``.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis to renagetting_ming.
        clone : bool, default True
            Also clone underlying data.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Collections
            or KnowledgeFrame.

        Returns
        -------
        Collections, KnowledgeFrame, or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Collections.renagetting_ming : Alter Collections index labels or name.
        KnowledgeFrame.renagetting_ming : Alter KnowledgeFrame index labels or name.
        Index.renagetting_ming : Set new names on index.

        Notes
        -----
        ``KnowledgeFrame.renagetting_ming_axis`` supports two ctotal_alling conventions

        * ``(index=index_mappingper, columns=columns_mappingper, ...)``
        * ``(mappingper, axis={'index', 'columns'}, ...)``

        The first ctotal_alling convention will only modify the names of
        the index and/or the names of the Index object that is the columns.
        In this case, the parameter ``clone`` is ignored.

        The second ctotal_alling convention will modify the names of the
        corresponding index if mappingper is a list or a scalar.
        However, if mappingper is dict-like or a function, it will use the
        deprecated behavior of modifying the axis *labels*.

        We *highly* recommend using keyword arguments to clarify your
        intent.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 227
query: I would like to sip all data in a monkey knowledgeframe Using kf.index to sip all rows
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 228
query: How to get one training data from the batch_dp
API_1: final_item(self: 'FrameOrCollections', offset) -> 'FrameOrCollections':
        Select final periods of time collections data based on a date offset.

        For a KnowledgeFrame with a sorted DatetimeIndex, this function
        selects the final_item few rows based on a date offset.

        Parameters
        ----------
        offset : str, DateOffset, dateutil.relativedelta
            The offset lengthgth of the data that will be selected. For instance,
            '3D' will display total_all the rows having their index within the final_item 3 days.

        Returns
        -------
        Collections or KnowledgeFrame
            A subset of the ctotal_aller.

        Raises
        ------
        TypeError
            If the index is not  a :class:`DatetimeIndex`

        See Also
        --------
        first : Select initial periods of time collections based on a date offset.
        at_time : Select values at a particular time of the day.
        between_time : Select values between particular times of the day.

        
API_2: interst(self, other, sort=False):
        Form the interst of two Index objects.

        This returns a new Index with elements common to the index and `other`.

        Parameters
        ----------
        other : Index or array-like
        sort : False or None, default False
            Whether to sort the resulting index.

            * False : do not sort the result.
            * None : sort the result, except when `self` and `other` are equal
              or when the values cannot be compared.

        Returns
        -------
        interst : Index

        
API_3: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    

idx: 229
query: How to unioner two knowledgeframes with different column names but same number of rows? I have two different data frames in monkey. Example: kf1=a b kf2= c 0 1 1 1 2 2 2 3 3 I want to unioner them so kf1= a b c 0 1 1 1 2 2 2 3 3 In order to unioner two knowledgeframes you can use this two examples. Both returns the same goal Using unioner plus additional arguments instructing it to use the indexes Specially, we can set left_index and right_index to True
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    

idx: 230
query: Join the two data pipes and add an index with the name `Ids`. Then create three copies of the datapipe.
API_1: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 231
query: How can I initantiate a NetsPresso instance through self-verification?
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 232
query: Counting consecutive positive values in Python/monkey array I'm trying to count consecutive up days in equity return data; so if a positive day is 1 and a negative is 0, a list y=[0,0,1,1,1,0,0,1,0,1,1] should return z=[0,0,1,2,3,0,0,1,0,1,2]. Return the result
API_1: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_2: cumulative_sum(self, axis=None, skipna=True, *args, **kwargs):
Return cumulative total_sum over a KnowledgeFrame or Collections axis.

Returns a KnowledgeFrame or Collections of the same size containing the cumulative
total_sum.

Parameters
----------
axis : {0 or 'index', 1 or 'columns'}, default 0
    The index or the name of the axis. 0 is equivalengtht to None or 'index'.
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
*args, **kwargs
    Additional keywords have no effect but might be accepted for
    compatibility with NumPy.

Returns
-------
scalar or Collections
    Return cumulative total_sum of scalar or Collections.

See Also
--------
core.window.Expanding.total_sum : Similar functionality
    but ignores ``NaN`` values.
Collections.total_sum : Return the total_sum over
    Collections axis.
Collections.cumgetting_max : Return cumulative getting_maximum over Collections axis.
Collections.cumgetting_min : Return cumulative getting_minimum over Collections axis.
Collections.cumtotal_sum : Return cumulative total_sum over Collections axis.
Collections.cumprod : Return cumulative product over Collections axis.


API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 233
query: This is my KnowledgeFrame that should be repeated for 5 times: I haven't found anything practical, including those like np.repeat ---- it just doesn't work on a KnowledgeFrame. You can use the concating function:
API_1: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 234
query: You can specify a new column named `average_along_rows` that contains the average of each row. You also need to compute the average along the rows, so use axis=1. Finally, return the knowledgeframe with the new column.
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        

idx: 235
query: Set value to an entire column `B` of a monkey knowledgeframe Return the changed knowledgeframe.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: standard(self, axis=None, skipna=None, level=None, ddof=1, numeric_only=None, **kwargs):
Return sample_by_num standard deviation over requested axis.

Normalized by N-1 by default. This can be changed using the ddof argument

Parameters
----------
axis : {index (0)}
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
ddof : int, default 1
    Delta Degrees of Freedom. The divisionisor used in calculations is N - ddof,
    where N represents the number of elements.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.

Returns
-------
scalar or Collections (if level specified)

Notes
-----
To have the same behaviour as `numpy.standard`, use `ddof=0` (instead of the
default `ddof=1`)


idx: 236
query: Finding the intersection between two collections In detail, first we create two sets, one for each collections. Then we find the intersection of the two sets.
API_1: final_item(self: 'FrameOrCollections', offset) -> 'FrameOrCollections':
        Select final periods of time collections data based on a date offset.

        For a KnowledgeFrame with a sorted DatetimeIndex, this function
        selects the final_item few rows based on a date offset.

        Parameters
        ----------
        offset : str, DateOffset, dateutil.relativedelta
            The offset lengthgth of the data that will be selected. For instance,
            '3D' will display total_all the rows having their index within the final_item 3 days.

        Returns
        -------
        Collections or KnowledgeFrame
            A subset of the ctotal_aller.

        Raises
        ------
        TypeError
            If the index is not  a :class:`DatetimeIndex`

        See Also
        --------
        first : Select initial periods of time collections based on a date offset.
        at_time : Select values at a particular time of the day.
        between_time : Select values between particular times of the day.

        
API_2: interst(self, other, sort=False):
        Form the interst of two Index objects.

        This returns a new Index with elements common to the index and `other`.

        Parameters
        ----------
        other : Index or array-like
        sort : False or None, default False
            Whether to sort the resulting index.

            * False : do not sort the result.
            * None : sort the result, except when `self` and `other` are equal
              or when the values cannot be compared.

        Returns
        -------
        interst : Index

        
API_3: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    

idx: 237
query: Copy beatnum numset 'a' into part of another numset 'b' in [1:4, 1:4]
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        

idx: 238
query: How can I compress a custom ONNX model using singular value decomposition?
API_1: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_2: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_3: final_item(self: 'FrameOrCollections', offset) -> 'FrameOrCollections':
        Select final periods of time collections data based on a date offset.

        For a KnowledgeFrame with a sorted DatetimeIndex, this function
        selects the final_item few rows based on a date offset.

        Parameters
        ----------
        offset : str, DateOffset, dateutil.relativedelta
            The offset lengthgth of the data that will be selected. For instance,
            '3D' will display total_all the rows having their index within the final_item 3 days.

        Returns
        -------
        Collections or KnowledgeFrame
            A subset of the ctotal_aller.

        Raises
        ------
        TypeError
            If the index is not  a :class:`DatetimeIndex`

        See Also
        --------
        first : Select initial periods of time collections based on a date offset.
        at_time : Select values at a particular time of the day.
        between_time : Select values between particular times of the day.

        

idx: 239
query: Inserts a row into a knowledgeframe at a specified row with no ingore index, and sort & reset the index with sip=True. Returns the new knowledgeframe.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 240
query: Any word from `targets` are present in sentence.
API_1: sample_by_num(self: 'FrameOrCollections', n=None, frac: 'float | None' = None, replacing: 'bool_t' = False, weights=None, random_state=None, axis: 'Axis | None' = None, ignore_index: 'bool_t' = False) -> 'FrameOrCollections':
        Return a random sample_by_num of items from an axis of object.

        You can use `random_state` for reproducibility.

        Parameters
        ----------
        n : int, optional
            Number of items from axis to return. Cannot be used with `frac`.
            Default = 1 if `frac` = None.
        frac : float, optional
            Fraction of axis items to return. Cannot be used with `n`.
        replacing : bool, default False
            Allow or distotal_allow sampling of the same row more than once.
        weights : str or ndarray-like, optional
            Default 'None' results in equal probability weighting.
            If passed a Collections, will align with targetting object on index. Index
            values in weights not found in sample_by_numd object will be ignored and
            index values in sample_by_numd object not in weights will be total_allocateed
            weights of zero.
            If ctotal_alled on a KnowledgeFrame, will accept the name of a column
            when axis = 0.
            Unless weights are a Collections, weights must be same lengthgth as axis
            being sample_by_numd.
            If weights do not total_sum to 1, they will be normalized to total_sum to 1.
            Missing values in the weights column will be treated as zero.
            Infinite values not total_allowed.
        random_state : int, array-like, BitGenerator, np.random.RandomState, optional
            If int, array-like, or BitGenerator (NumPy>=1.17), seed for
            random number generator
            If np.random.RandomState, use as numpy RandomState object.

            .. versionchanged:: 1.1.0

                array-like and BitGenerator (for NumPy>=1.17) object now passed to
                np.random.RandomState() as seed

        axis : {0 or ‘index’, 1 or ‘columns’, None}, default None
            Axis to sample_by_num. Accepts axis number or name. Default is stat axis
            for given data type (0 for Collections and KnowledgeFrames).
        ignore_index : bool, default False
            If True, the resulting index will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.3.0

        Returns
        -------
        Collections or KnowledgeFrame
            A new object of same type as ctotal_aller containing `n` items randomly
            sample_by_numd from the ctotal_aller object.

        See Also
        --------
        KnowledgeFrameGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            KnowledgeFrame object.
        CollectionsGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            Collections object.
        numpy.random.choice: Generates a random sample_by_num from a given 1-D numpy
            array.

        Notes
        -----
        If `frac` > 1, `replacingment` should be set to `True`.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    

idx: 241
query: I am trying to extract the last year (YY) of a fiscal date string in the format of YYYY-YY. e.g The last year of this '1999-00' would be 2000. I need a logic to include a case where if it is the end of the century then my employ method should add to the first two digits. the column_name is the column name of the knowledgeframe that contains the date strings. return the numerical Collections obj of the last year.
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    

idx: 242
query: Monkey knowledgeframe fillnone() only some columns in place This function fills all columns with 0 Return the changed knowledgeframe
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 243
query: transform timestamp to pydatetime object return pydatetime object
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: convert_pydatetime(*args, **kwargs):
        Convert a Timestamp object to a native Python datetime object.

        If warn=True, issue a warning if nanoseconds is nonzero.

        
API_3: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    

idx: 244
query: Can you please provide the average/mean of each column? Additionally, I would like to know the number of missing/NaN values in each column. Can you please provide a summary of the number of missing/NaN values in each column?
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 245
query: I want to check if all values in the columns of a beatnum numset/matrix are the same. A column shares a common value if all the values in that column are True: The below code checks if all values in the columns are the same using a == a[0,:] and axis=0
API_1: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        

idx: 246
query: How can I map True/False to 1/0 in a Monkey KnowledgeFrame? return the knowledgeframe with the column converted to int
API_1: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        
API_2: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 247
query: Sorting columns in monkey knowledgeframe based on column name Note that axis is one
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 248
query: Normalization using monkey We simply subtract the average and divide by standard deviation on kf.iloc[:,0,-1] obj with axis is zero. Return the normalized knowledgeframe
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: standard(self, axis=None, skipna=None, level=None, ddof=1, numeric_only=None, **kwargs):
Return sample_by_num standard deviation over requested axis.

Normalized by N-1 by default. This can be changed using the ddof argument

Parameters
----------
axis : {index (0)}
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
ddof : int, default 1
    Delta Degrees of Freedom. The divisionisor used in calculations is N - ddof,
    where N represents the number of elements.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.

Returns
-------
scalar or Collections (if level specified)

Notes
-----
To have the same behaviour as `numpy.standard`, use `ddof=0` (instead of the
default `ddof=1`)

API_3: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)


idx: 249
query: I need to change the dtype of multiple columns but the knowledgeframe has different kind of dtypes. Some columns dtypes are float64 whereas some columns are int64 I need to change all float64 to float32.
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        

idx: 250
query: How can I retrieve the last N rows of a Monkey KnowledgeFrame? Additionally, how do I find out the number of columns in a Monkey data frame? Please return the number of columns in the knowledgeframe.
API_1: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 251
query: I have a Monkey knowledgeframe and I want to find all the unique values in that knowledgeframe...irrespective of row/columns. If I have a 10 x 10 knowledgeframe, and suppose they have 84 unique values, I need to find them - Not the count. Using xx.values.flat_underlying to get the flattened array of the knowledgeframe Getting the unique values by numpy.unique
API_1: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 252
query: How do I set the batch size to 32 and use the Adagrad optimizer?
API_1: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_2: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 253
query: What is the best way to do a grouper on a Monkey knowledgeframe, but exclude some columns from that grouper? I want to grouper the column `Country` and `Item_Code` and only compute the sum of the rows falling under the columns ['Y1961', 'Y1962' and 'Y1963'].
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 254
query: How to obtain monkey KnowledgeFrame without index I want to print the whole knowledgeframe, but I don't want to print the index
API_1: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        

idx: 255
query: I want to make all column headers in my monkey data frame lower case Return the changed knowledgeframe
API_1: mapping(self, mapper, na_action=None):
        Map values using input correspondence (a dict, Collections, or function).

        Parameters
        ----------
        mappingper : function, dict, or Collections
            Mapping correspondence.
        na_action : {None, 'ignore'}
            If 'ignore', propagate NA values, without passing them to the
            mappingping correspondence.

        Returns
        -------
        applied : Union[Index, MultiIndex], inferred
            The output of the mappingping function applied to the index.
            If the function returns a tuple with more than one element
            a MultiIndex will be returned.
        
API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 256
query: How to convert an numset of strings to an numset of floats in beatnum? Return the final result
API_1: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_2: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_3: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        

idx: 257
query: I would simply like to slice the Data Frame and take the first n rows. Return the result
API_1: nbiggest(self, n=5, keep='first') -> 'Collections':
        Return the largest `n` elements.

        Parameters
        ----------
        n : int, default 5
            Return this mwhatever descending sorted values.
        keep : {'first', 'final_item', 'total_all'}, default 'first'
            When there are duplicate values that cannot total_all fit in a
            Collections of `n` elements:

            - ``first`` : return the first `n` occurrences in order
                of appearance.
            - ``final_item`` : return the final_item `n` occurrences in reverse
                order of appearance.
            - ``total_all`` : keep total_all occurrences. This can result in a Collections of
                size larger than `n`.

        Returns
        -------
        Collections
            The `n` largest values in the Collections, sorted in decreasing order.

        See Also
        --------
        Collections.nsmtotal_allest: Get the `n` smtotal_allest elements.
        Collections.sort_the_values: Sort Collections by values.
        Collections.header_num: Return the first `n` rows.

        Notes
        -----
        Faster than ``.sort_the_values(ascending=False).header_num(n)`` for smtotal_all `n`
        relative to the size of the ``Collections`` object.

        
API_2: header_num(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the first `n` rows.

        This function returns the first `n` rows for the object based
        on position. It is useful for quickly testing if your object
        has the right type of data in it.

        For negative values of `n`, this function returns total_all rows except
        the final_item `n` rows, equivalengtht to ``kf[:-n]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        same type as ctotal_aller
            The first `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.final_item_tail: Returns the final_item `n` rows.

        
API_3: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        

idx: 258
query: Add Leading Zeros to Strings at `col_name` in Monkey Dataframe The maximum length of the string is 15 Return the knowledgeframe
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 259
query: Create batch datapipe with batch size 3, batch num is 100, and drop the last batch if it is not full. Also, useing the sort_bucket function to sort the bucket, where the bucket_num is 1.
API_1: mapping(self, mapper, na_action=None):
        Map values using input correspondence (a dict, Collections, or function).

        Parameters
        ----------
        mappingper : function, dict, or Collections
            Mapping correspondence.
        na_action : {None, 'ignore'}
            If 'ignore', propagate NA values, without passing them to the
            mappingping correspondence.

        Returns
        -------
        applied : Union[Index, MultiIndex], inferred
            The output of the mappingping function applied to the index.
            If the function returns a tuple with more than one element
            a MultiIndex will be returned.
        
API_2: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 260
query: if function is c(i, j) = a(i) + b(j)*2:
API_1: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        
API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 261
query: How many times does each value appear in the collection? Also, please provide the count of missing/NaN values in each column.
API_1: length(self):
        Compute the lengthgth of each element in the Collections/Index.

        The element may be a sequence (such as a string, tuple or list) or a collection
        (such as a dictionary).

        Returns
        -------
        Collections or Index of int
            A Collections or Index of integer values indicating the lengthgth of each
            element in the Collections or Index.

        See Also
        --------
        str.length : Python built-in function returning the lengthgth of an object.
        Collections.size : Returns the lengthgth of the Collections.

        
API_2: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_3: distinctive(self: '_IndexT', level: 'Hashable | None' = None) -> '_IndexT':
        Return distinctive values in the index.

        Unique values are returned in order of appearance, this does NOT sort.

        Parameters
        ----------
        level : int or hashable, optional
            Only return values from specified level (for MultiIndex).
            If int, gettings the level by integer position, else by level name.

        Returns
        -------
        Index

        See Also
        --------
        distinctive : Numpy array of distinctive values in that column.
        Collections.distinctive : Return distinctive values of Collections object.
        

idx: 262
query: Find the index of the k smallest values of a beatnum numset
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: convert_pydatetime(*args, **kwargs):
        Convert a Timestamp object to a native Python datetime object.

        If warn=True, issue a warning if nanoseconds is nonzero.

        
API_3: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    

idx: 263
query: How can I find the largest value in column a? Can I use nbiggest and iloc for this? Additionally, how do I identify all the rows in a Monkey KnowledgeFrame that have the maximum value in the 'num' column, after grouping by the 'Mt' column?
API_1: nbiggest(self, n=5, keep='first') -> 'Collections':
        Return the largest `n` elements.

        Parameters
        ----------
        n : int, default 5
            Return this mwhatever descending sorted values.
        keep : {'first', 'final_item', 'total_all'}, default 'first'
            When there are duplicate values that cannot total_all fit in a
            Collections of `n` elements:

            - ``first`` : return the first `n` occurrences in order
                of appearance.
            - ``final_item`` : return the final_item `n` occurrences in reverse
                order of appearance.
            - ``total_all`` : keep total_all occurrences. This can result in a Collections of
                size larger than `n`.

        Returns
        -------
        Collections
            The `n` largest values in the Collections, sorted in decreasing order.

        See Also
        --------
        Collections.nsmtotal_allest: Get the `n` smtotal_allest elements.
        Collections.sort_the_values: Sort Collections by values.
        Collections.header_num: Return the first `n` rows.

        Notes
        -----
        Faster than ``.sort_the_values(ascending=False).header_num(n)`` for smtotal_all `n`
        relative to the size of the ``Collections`` object.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 264
query: How do I combine two knowledgeframes with ignore index? Return the concated knowledgeframe.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 265
query: How do I change the column labels of kf？ And return the knowledgeframe that has been renamed
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    
API_3: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        

idx: 266
query: I find myself often having to check whether a column or row exists in a knowledgeframe before trying to reference it. Is there any way to do this more nicely? For example on an arbitrary object I can do x = getattr(anobject, 'id', default) - is there anything similar to this in monkey? Really any way to achieve what I'm doing more gracefully? Output the second row of data in `mycol` column if it exists, otherwise output NaN
API_1: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: getting(self, i):
        Extract element from each component at specified position.

        Extract element from lists, tuples, or strings in each element in the
        Collections/Index.

        Parameters
        ----------
        i : int
            Position of element to extract.

        Returns
        -------
        Collections or Index

        

idx: 267
query: I have a dictionary that I need to convert to a BeatNum structured numset.
API_1: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 268
query: I was wondering if there is an elegant and shorthand way in Monkey KnowledgeFrames to select columns by data type (dtype). i.e. Select only float64 columns from a KnowledgeFrame
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 269
query: Change all columns type of KnowledgeFrame to numeric And return the new KnowledgeFrame The code is:
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: renaming_axis(self, mappingper=None, index=None, columns=None, axis=None, clone=True, inplace=False):
        Set the name of the axis for the index or columns.

        Parameters
        ----------
        mappingper : scalar, list-like, optional
            Value to set the axis name attribute.
        index, columns : scalar, list-like, dict-like or function, optional
            A scalar, list-like, dict-like or functions transformatingions to
            employ to that axis' values.
            Note that the ``columns`` parameter is not total_allowed if the
            object is a Collections. This parameter only employ for KnowledgeFrame
            type objects.

            Use either ``mappingper`` and ``axis`` to
            specify the axis to targetting with ``mappingper``, or ``index``
            and/or ``columns``.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis to renagetting_ming.
        clone : bool, default True
            Also clone underlying data.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Collections
            or KnowledgeFrame.

        Returns
        -------
        Collections, KnowledgeFrame, or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Collections.renagetting_ming : Alter Collections index labels or name.
        KnowledgeFrame.renagetting_ming : Alter KnowledgeFrame index labels or name.
        Index.renagetting_ming : Set new names on index.

        Notes
        -----
        ``KnowledgeFrame.renagetting_ming_axis`` supports two ctotal_alling conventions

        * ``(index=index_mappingper, columns=columns_mappingper, ...)``
        * ``(mappingper, axis={'index', 'columns'}, ...)``

        The first ctotal_alling convention will only modify the names of
        the index and/or the names of the Index object that is the columns.
        In this case, the parameter ``clone`` is ignored.

        The second ctotal_alling convention will modify the names of the
        corresponding index if mappingper is a list or a scalar.
        However, if mappingper is dict-like or a function, it will use the
        deprecated behavior of modifying the axis *labels*.

        We *highly* recommend using keyword arguments to clarify your
        intent.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 270
query: I need to calculate the difference between two rows groupwise using monkey. To calculate the total_sum I would use monkey.grouper('Group').total_sum(), but how do you calculate the difference between rows where the row ordering is important? I think we need custom function with employ which return KnowledgeFrame for each group, for select by position is used iat: Return the result
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 271
query: How do I find all rows in a monkey KnowledgeFrame which have the max value for 'num' column, after grouping by 'Mt' column?
API_1: get_max(self, axis=None, skipna: 'bool' = True, *args, **kwargs):
        Return the getting_maximum value of the Index.

        Parameters
        ----------
        axis : int, optional
            For compatibility with NumPy. Only 0 or None are total_allowed.
        skipna : bool, default True
            Exclude NA/null values when showing the result.
        *args, **kwargs
            Additional arguments and keywords for compatibility with NumPy.

        Returns
        -------
        scalar
            Maximum value.

        See Also
        --------
        Index.getting_min : Return the getting_minimum value in an Index.
        Collections.getting_max : Return the getting_maximum value in a Collections.
        KnowledgeFrame.getting_max : Return the getting_maximum values in a KnowledgeFrame.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 272
query: You can specify a new column named `average_along_rows` that contains the average of each row. You also need to compute the average along the rows, so use axis=1. Finally, return the knowledgeframe with the new column.
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        
API_3: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)


idx: 273
query: Example KnowledgeFrame Want to remove all the numbers from the Name column. Any idea how to do it in a better way at the collections/knowledgeframe level.
API_1: replacing(old, new, count=-1, /):Return a clone with total_all occurrences of substring old replacingd by new.

  count
    Maximum number of occurrences to replacing.
    -1 (the default value) averages replacing total_all occurrences.

If the optional argument count is given, only the first count occurrences are
replacingd.
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 274
query: Here's a one solution to remove columns based on duplicate column names: Return the duplicated knowledgeframe
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 275
query: How can I count the consecutive positive values in a Python/monkey array? I want to count the number of consecutive up days in equity return data. For example, if a positive day is represented by 1 and a negative day is represented by 0, how can I transform a list like y=[0,0,1,1,1,0,0,1,0,1,1] into z=[0,0,1,2,3,0,0,1,0,1,2]? Additionally, I would like to know how to count the number of occurrences of a specific value in a collection. Can you please provide the solution for both of these tasks?
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: cumulative_sum(self, axis=None, skipna=True, *args, **kwargs):
Return cumulative total_sum over a KnowledgeFrame or Collections axis.

Returns a KnowledgeFrame or Collections of the same size containing the cumulative
total_sum.

Parameters
----------
axis : {0 or 'index', 1 or 'columns'}, default 0
    The index or the name of the axis. 0 is equivalengtht to None or 'index'.
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
*args, **kwargs
    Additional keywords have no effect but might be accepted for
    compatibility with NumPy.

Returns
-------
scalar or Collections
    Return cumulative total_sum of scalar or Collections.

See Also
--------
core.window.Expanding.total_sum : Similar functionality
    but ignores ``NaN`` values.
Collections.total_sum : Return the total_sum over
    Collections axis.
Collections.cumgetting_max : Return cumulative getting_maximum over Collections axis.
Collections.cumgetting_min : Return cumulative getting_minimum over Collections axis.
Collections.cumtotal_sum : Return cumulative total_sum over Collections axis.
Collections.cumprod : Return cumulative product over Collections axis.


API_3: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        

idx: 276
query: Please add a new column called 'Fruit Total' that calculates the sum of the values in the other columns, excluding any NaN values. Additionally, create a new column named 'average_along_rows' that contains the average value of each row. To calculate the average along the rows, please use axis=1. Finally, return the knowledgeframe with the new column.
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_3: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.



idx: 277
query: Return the row-index values of the knowledgeframe as a list
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        

idx: 278
query: Finding the intersection between two collections In detail, first we create two sets, one for each collections. Then we find the intersection of the two sets.
API_1: Collections(data=None, index=None, dtype: 'Dtype | None' = None, name=None, clone: 'bool' = False, fastpath: 'bool' = False):
    One-dimensional ndarray with axis labels (including time collections).

    Labels need not be distinctive but must be a hashable type. The object
    supports both integer- and label-based indexing and provides a host of
    methods for perforgetting_ming operations involving the index. Statistical
    methods from ndarray have been overridden to automatictotal_ally exclude
    missing data (currently represented as NaN).

    Operations between Collections (+, -, /, *, **) align values based on their
    associated index values-- they need not be the same lengthgth. The result
    index will be the sorted union of the two indexes.

    Parameters
    ----------
    data : array-like, Iterable, dict, or scalar value
        Contains data stored in Collections. If data is a dict, argument order is
        maintained.
    index : array-like or Index (1d)
        Values must be hashable and have the same lengthgth as `data`.
        Non-distinctive index values are total_allowed. Will default to
        RangeIndex (0, 1, 2, ..., n) if not provided. If data is dict-like
        and index is None, then the keys in the data are used as the index. If the
        index is not None, the resulting Collections is reindexinged with the index values.
    dtype : str, numpy.dtype, or ExtensionDtype, optional
        Data type for the output Collections. If not specified, this will be
        inferred from `data`.
        See the :ref:`user guide <basics.dtypes>` for more usages.
    name : str, optional
        The name to give to the Collections.
    clone : bool, default False
        Copy input data. Only affects Collections or 1d ndarray input. See examples.

    
API_2: clone(self: '_IndexT', name: 'Hashable | None' = None, deep: 'bool' = False, dtype: 'Dtype | None' = None, names: 'Sequence[Hashable] | None' = None) -> '_IndexT':
        Make a clone of this object.

        Name and dtype sets those attributes on the new object.

        Parameters
        ----------
        name : Label, optional
            Set name for new object.
        deep : bool, default False
        dtype : numpy dtype or monkey type, optional
            Set dtype for new object.

            .. deprecated:: 1.2.0
                use ``totype`` method instead.
        names : list-like, optional
            Kept for compatibility with MultiIndex. Should not be used.

        Returns
        -------
        Index
            Index refer to new object which is a clone of this object.

        Notes
        -----
        In most cases, there should be no functional difference from using
        ``deep``, but if ``deep`` is passed it will attempt to deepclone.
        
API_3: final_item(self: 'FrameOrCollections', offset) -> 'FrameOrCollections':
        Select final periods of time collections data based on a date offset.

        For a KnowledgeFrame with a sorted DatetimeIndex, this function
        selects the final_item few rows based on a date offset.

        Parameters
        ----------
        offset : str, DateOffset, dateutil.relativedelta
            The offset lengthgth of the data that will be selected. For instance,
            '3D' will display total_all the rows having their index within the final_item 3 days.

        Returns
        -------
        Collections or KnowledgeFrame
            A subset of the ctotal_aller.

        Raises
        ------
        TypeError
            If the index is not  a :class:`DatetimeIndex`

        See Also
        --------
        first : Select initial periods of time collections based on a date offset.
        at_time : Select values at a particular time of the day.
        between_time : Select values between particular times of the day.

        

idx: 279
query: I want to convert a table, represented as a list of lists, into a monkey KnowledgeFrame. The columns are ['one', 'two'] What is the best way to convert the columns to the appropriate types, in this case the 'two' column into floats?
API_1: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: totype(self, dtype: 'Dtype | None' = None, clone=True):
        Change the dtype of a SparseArray.

        The output will always be a SparseArray. To convert to a dense
        ndarray with a certain dtype, use :meth:`numpy.asarray`.

        Parameters
        ----------
        dtype : np.dtype or ExtensionDtype
            For SparseDtype, this changes the dtype of
            ``self.sp_values`` and the ``self.fill_value``.

            For other dtypes, this only changes the dtype of
            ``self.sp_values``.

        clone : bool, default True
            Whether to ensure a clone is made, even if not necessary.

        Returns
        -------
        SparseArray

        

idx: 280
query: Group the files by their file name using the group_fn function. Then, reserving the length of group result greater than 1.
API_1: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    
API_2: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 281
query: How would I rename the only one column header? return the changed knowledgeframe
API_1: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 282
query: How can I convert the frequencies in a monkey collection to percentages? I want to get the percentage of each gender. Additionally, how can I group values in a monkey knowledgeframe and select the latest value from each group based on the date? I want to sort the values by date in ascending order and then group them by ID.
API_1: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_2: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    
API_3: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        

idx: 283
query: How do I configure a fx model for training?
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: fillnone(self, value=None, downcast=None):
        Fill NA/NaN values with the specified value.

        Parameters
        ----------
        value : scalar
            Scalar value to use to fill holes (e.g. 0).
            This value cannot be a list-likes.
        downcast : dict, default is None
            A dict of item->dtype of what to downcast if possible,
            or the string 'infer' which will try to downcast to an appropriate
            equal type (e.g. float64 to int64 if possible).

        Returns
        -------
        Index

        See Also
        --------
        KnowledgeFrame.fillnone : Fill NaN values of a KnowledgeFrame.
        Collections.fillnone : Fill NaN Values of a Collections.
        

idx: 284
query: How can I fill only some columns in a monkey knowledge frame with 0 using the fillnone() function? After filling the columns, I would like to convert all the column headers in my monkey data frame to lowercase. Can you please provide the modified knowledge frame?
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: fillnone(self, value=None, downcast=None):
        Fill NA/NaN values with the specified value.

        Parameters
        ----------
        value : scalar
            Scalar value to use to fill holes (e.g. 0).
            This value cannot be a list-likes.
        downcast : dict, default is None
            A dict of item->dtype of what to downcast if possible,
            or the string 'infer' which will try to downcast to an appropriate
            equal type (e.g. float64 to int64 if possible).

        Returns
        -------
        Index

        See Also
        --------
        KnowledgeFrame.fillnone : Fill NaN values of a KnowledgeFrame.
        Collections.fillnone : Fill NaN Values of a Collections.
        

idx: 285
query: How can I convert the output of the .count_values function to a monkey knowledgeframe? I want to rename the column from index to 'counts' using renaming_axis('distinctive_values') and reset the index. After that, I have a knowledgeframe with duplicate values in the 'col1' column. How can I remove the duplicates and keep only the rows with the last value in the 'col2' column? Please return the final knowledgeframe.
API_1: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_2: renaming_axis(self, mappingper=None, index=None, columns=None, axis=None, clone=True, inplace=False):
        Set the name of the axis for the index or columns.

        Parameters
        ----------
        mappingper : scalar, list-like, optional
            Value to set the axis name attribute.
        index, columns : scalar, list-like, dict-like or function, optional
            A scalar, list-like, dict-like or functions transformatingions to
            employ to that axis' values.
            Note that the ``columns`` parameter is not total_allowed if the
            object is a Collections. This parameter only employ for KnowledgeFrame
            type objects.

            Use either ``mappingper`` and ``axis`` to
            specify the axis to targetting with ``mappingper``, or ``index``
            and/or ``columns``.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis to renagetting_ming.
        clone : bool, default True
            Also clone underlying data.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Collections
            or KnowledgeFrame.

        Returns
        -------
        Collections, KnowledgeFrame, or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Collections.renagetting_ming : Alter Collections index labels or name.
        KnowledgeFrame.renagetting_ming : Alter KnowledgeFrame index labels or name.
        Index.renagetting_ming : Set new names on index.

        Notes
        -----
        ``KnowledgeFrame.renagetting_ming_axis`` supports two ctotal_alling conventions

        * ``(index=index_mappingper, columns=columns_mappingper, ...)``
        * ``(mappingper, axis={'index', 'columns'}, ...)``

        The first ctotal_alling convention will only modify the names of
        the index and/or the names of the Index object that is the columns.
        In this case, the parameter ``clone`` is ignored.

        The second ctotal_alling convention will modify the names of the
        corresponding index if mappingper is a list or a scalar.
        However, if mappingper is dict-like or a function, it will use the
        deprecated behavior of modifying the axis *labels*.

        We *highly* recommend using keyword arguments to clarify your
        intent.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 286
query: How can I merge two knowledgeframes while ignoring the index? Please return the concatenated knowledgeframe. Additionally, how can I merge two knowledgeframes by their indexes, with both the left and right indexes set to True?
API_1: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: adding(self, other: 'Index | Sequence[Index]') -> 'Index':
        Append a collection of Index options togettingher.

        Parameters
        ----------
        other : Index or list/tuple of indices

        Returns
        -------
        Index
        

idx: 287
query: How can I apply augmentations only during training and not inference?
API_1: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        
API_2: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_3: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        

idx: 288
query: y = bn.numset([2,1,5,2]) y axis filter out values larger than 2 remove masked values from m
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: sip(self, labels, errors: 'str_t' = 'raise') -> 'Index':
        Make new Index with passed list of labels deleted.

        Parameters
        ----------
        labels : array-like
        errors : {'ignore', 'raise'}, default 'raise'
            If 'ignore', suppress error and existing labels are sipped.

        Returns
        -------
        sipped : Index
            Will be same type as self, except for RangeIndex.

        Raises
        ------
        KeyError
            If not total_all of the labels are found in the selected axis
        

idx: 289
query: I have a knowledgeframe with repeat values in column `col1`. I want to sip duplicates, keeping the row with the last value in column `col2`. How would I do that? return the final knowledgeframe
API_1: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: remove_duplicates(self: '_IndexT', keep: 'str_t | bool' = 'first') -> '_IndexT':
        Return Index with duplicate values removed.

        Parameters
        ----------
        keep : {'first', 'final_item', ``False``}, default 'first'
            - 'first' : Drop duplicates except for the first occurrence.
            - 'final_item' : Drop duplicates except for the final_item occurrence.
            - ``False`` : Drop total_all duplicates.

        Returns
        -------
        deduplicated_values : Index

        See Also
        --------
        Collections.sip_duplicates : Equivalengtht method on Collections.
        KnowledgeFrame.sip_duplicates : Equivalengtht method on KnowledgeFrame.
        Index.duplicated_values : Related method on Index, indicating duplicate
            Index values.

        

idx: 290
query: Please provide the row-index values of the knowledgeframe as a list. Additionally, include the knowledgeframe with any rows that have one or more NaN values.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: convert_list(self, *args, **kwargs):
        Return a list of the values.

        These are each a scalar type, which is a Python scalar
        (for str, int, float) or a monkey scalar
        (for Timestamp/Timedelta/Interval/Period)
        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 291
query: How can I get a Monkey KnowledgeFrame without the index? I want to print the entire knowledgeframe, but without the index. Also, how can I convert True/False values to 1/0 in a Monkey KnowledgeFrame? Please return the knowledgeframe with the specified column converted to integers.
API_1: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_2: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 292
query: Return the row-index values of the knowledgeframe as a list
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: standard(self, axis=None, skipna=None, level=None, ddof=1, numeric_only=None, **kwargs):
Return sample_by_num standard deviation over requested axis.

Normalized by N-1 by default. This can be changed using the ddof argument

Parameters
----------
axis : {index (0)}
skipna : bool, default True
    Exclude NA/null values. If an entire row/column is NA, the result
    will be NA.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
ddof : int, default 1
    Delta Degrees of Freedom. The divisionisor used in calculations is N - ddof,
    where N represents the number of elements.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.

Returns
-------
scalar or Collections (if level specified)

Notes
-----
To have the same behaviour as `numpy.standard`, use `ddof=0` (instead of the
default `ddof=1`)

API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 293
query: Please convert the `Date` column to date format using the monkey function. Afterwards, I would like to change all the column headers in my monkey data frame to lowercase. Please return the modified knowledge frame.
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    
API_3: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    

idx: 294
query: How can I repeat my KnowledgeFrame five times? I have tried using np.repeat but it doesn't work. Also, I have a KnowledgeFrame with repeated values in column 'col1'. I want to remove duplicates and keep the rows with the last value in column 'col2'. How can I achieve this? Please return the final KnowledgeFrame.
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 295
query: I want to get the counts of distinctive values of the knowledgeframe. count_values implements this however I want to use its output somewhere else. How can I convert .count_values output to a monkey knowledgeframe. Use renaming_axis('distinctive_values') for name ('counts') of column from index and reseting_index return the final knowledgeframe
API_1: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        
API_2: renaming_axis(self, mappingper=None, index=None, columns=None, axis=None, clone=True, inplace=False):
        Set the name of the axis for the index or columns.

        Parameters
        ----------
        mappingper : scalar, list-like, optional
            Value to set the axis name attribute.
        index, columns : scalar, list-like, dict-like or function, optional
            A scalar, list-like, dict-like or functions transformatingions to
            employ to that axis' values.
            Note that the ``columns`` parameter is not total_allowed if the
            object is a Collections. This parameter only employ for KnowledgeFrame
            type objects.

            Use either ``mappingper`` and ``axis`` to
            specify the axis to targetting with ``mappingper``, or ``index``
            and/or ``columns``.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis to renagetting_ming.
        clone : bool, default True
            Also clone underlying data.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Collections
            or KnowledgeFrame.

        Returns
        -------
        Collections, KnowledgeFrame, or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Collections.renagetting_ming : Alter Collections index labels or name.
        KnowledgeFrame.renagetting_ming : Alter KnowledgeFrame index labels or name.
        Index.renagetting_ming : Set new names on index.

        Notes
        -----
        ``KnowledgeFrame.renagetting_ming_axis`` supports two ctotal_alling conventions

        * ``(index=index_mappingper, columns=columns_mappingper, ...)``
        * ``(mappingper, axis={'index', 'columns'}, ...)``

        The first ctotal_alling convention will only modify the names of
        the index and/or the names of the Index object that is the columns.
        In this case, the parameter ``clone`` is ignored.

        The second ctotal_alling convention will modify the names of the
        corresponding index if mappingper is a list or a scalar.
        However, if mappingper is dict-like or a function, it will use the
        deprecated behavior of modifying the axis *labels*.

        We *highly* recommend using keyword arguments to clarify your
        intent.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 296
query: I am trying to extract the last year (YY) of a fiscal date string in the format of YYYY-YY. e.g The last year of this '1999-00' would be 2000. I need a logic to include a case where if it is the end of the century then my employ method should add to the first two digits. the column_name is the column name of the knowledgeframe that contains the date strings. return the numerical Collections obj of the last year.
API_1: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    
API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    

idx: 297
query: unioner the above two knowledgeframes on column 'company'
API_1: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_2: sample_by_num(self: 'FrameOrCollections', n=None, frac: 'float | None' = None, replacing: 'bool_t' = False, weights=None, random_state=None, axis: 'Axis | None' = None, ignore_index: 'bool_t' = False) -> 'FrameOrCollections':
        Return a random sample_by_num of items from an axis of object.

        You can use `random_state` for reproducibility.

        Parameters
        ----------
        n : int, optional
            Number of items from axis to return. Cannot be used with `frac`.
            Default = 1 if `frac` = None.
        frac : float, optional
            Fraction of axis items to return. Cannot be used with `n`.
        replacing : bool, default False
            Allow or distotal_allow sampling of the same row more than once.
        weights : str or ndarray-like, optional
            Default 'None' results in equal probability weighting.
            If passed a Collections, will align with targetting object on index. Index
            values in weights not found in sample_by_numd object will be ignored and
            index values in sample_by_numd object not in weights will be total_allocateed
            weights of zero.
            If ctotal_alled on a KnowledgeFrame, will accept the name of a column
            when axis = 0.
            Unless weights are a Collections, weights must be same lengthgth as axis
            being sample_by_numd.
            If weights do not total_sum to 1, they will be normalized to total_sum to 1.
            Missing values in the weights column will be treated as zero.
            Infinite values not total_allowed.
        random_state : int, array-like, BitGenerator, np.random.RandomState, optional
            If int, array-like, or BitGenerator (NumPy>=1.17), seed for
            random number generator
            If np.random.RandomState, use as numpy RandomState object.

            .. versionchanged:: 1.1.0

                array-like and BitGenerator (for NumPy>=1.17) object now passed to
                np.random.RandomState() as seed

        axis : {0 or ‘index’, 1 or ‘columns’, None}, default None
            Axis to sample_by_num. Accepts axis number or name. Default is stat axis
            for given data type (0 for Collections and KnowledgeFrames).
        ignore_index : bool, default False
            If True, the resulting index will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.3.0

        Returns
        -------
        Collections or KnowledgeFrame
            A new object of same type as ctotal_aller containing `n` items randomly
            sample_by_numd from the ctotal_aller object.

        See Also
        --------
        KnowledgeFrameGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            KnowledgeFrame object.
        CollectionsGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            Collections object.
        numpy.random.choice: Generates a random sample_by_num from a given 1-D numpy
            array.

        Notes
        -----
        If `frac` > 1, `replacingment` should be set to `True`.

        
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 298
query: Return the knowledgeframe with the rows with one or more NaN values
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: whatever(self, *args, **kwargs):
        Return whether whatever element is Truthy.

        Parameters
        ----------
        *args
            Required for compatibility with numpy.
        **kwargs
            Required for compatibility with numpy.

        Returns
        -------
        whatever : bool or array-like (if axis is specified)
            A single element array-like may be converted to bool.

        See Also
        --------
        Index.total_all : Return whether total_all elements are True.
        Collections.total_all : Return whether total_all elements are True.

        Notes
        -----
        Not a Number (NaN), positive infinity and negative infinity
        evaluate to True because these are not equal to zero.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 299
query: I have a knowledgeframe with repeat values in column `col1`. I want to sip duplicates, keeping the row with the last value in column `col2`. How would I do that? return the final knowledgeframe
API_1: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 300
query: How to print a Beatnum numset without brackets? For example, I want to convert a = bn.numset([1,2,3,4,5]) into a_string = "1 2 3 4 5".
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: average(self, axis=None, skipna=None, level=None, numeric_only=None, **kwargs):
Return the average of the values over the requested axis.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)


idx: 301
query: How do I find all rows in a monkey KnowledgeFrame which have the max value for 'num' column, after grouping by 'Mt' column?
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_3: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    

idx: 302
query: How can I normalize the columns of a Monkey KnowledgeFrame so that each value is between 0 and 1? Also, how can I convert True/False values to 1/0 in the KnowledgeFrame? Please return the KnowledgeFrame with the column converted to integers.
API_1: get_max(self, axis=None, skipna: 'bool' = True, *args, **kwargs):
        Return the getting_maximum value of the Index.

        Parameters
        ----------
        axis : int, optional
            For compatibility with NumPy. Only 0 or None are total_allowed.
        skipna : bool, default True
            Exclude NA/null values when showing the result.
        *args, **kwargs
            Additional arguments and keywords for compatibility with NumPy.

        Returns
        -------
        scalar
            Maximum value.

        See Also
        --------
        Index.getting_min : Return the getting_minimum value in an Index.
        Collections.getting_max : Return the getting_maximum value in a Collections.
        KnowledgeFrame.getting_max : Return the getting_maximum values in a KnowledgeFrame.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: get_min(self, *, skipna=True, **kwargs):
        The getting_minimum value of the object.

        Only ordered `Categoricals` have a getting_minimum!

        .. versionchanged:: 1.0.0

           Returns an NA value on empty arrays

        Raises
        ------
        TypeError
            If the `Categorical` is not `ordered`.

        Returns
        -------
        getting_min : the getting_minimum of this `Categorical`
        

idx: 303
query: How to get the last N rows of a monkey KnowledgeFrame?
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: concating(objs: 'Iterable[NDFrame] | Mapping[Hashable, NDFrame]', axis=0, join='outer', ignore_index: 'bool' = False, keys=None, levels=None, names=None, verify_integrity: 'bool' = False, sort: 'bool' = False, clone: 'bool' = True) -> 'FrameOrCollectionsUnion':
    Concatenate monkey objects along a particular axis with optional set logic
    along the other axes.

    Can also add a layer of hierarchical indexing on the concatingenation axis,
    which may be useful if the labels are the same (or overlapping) on
    the passed axis number.

    Parameters
    ----------
    objs : a sequence or mappingping of Collections or KnowledgeFrame objects
        If a mappingping is passed, the sorted keys will be used as the `keys`
        argument, unless it is passed, in which case the values will be
        selected (see below). Any None objects will be sipped silengthtly unless
        they are total_all None in which case a ValueError will be raised.
    axis : {0/'index', 1/'columns'}, default 0
        The axis to concatingenate along.
    join : {'inner', 'outer'}, default 'outer'
        How to handle indexes on other axis (or axes).
    ignore_index : bool, default False
        If True, do not use the index values along the concatingenation axis. The
        resulting axis will be labeled 0, ..., n - 1. This is useful if you are
        concatingenating objects where the concatingenation axis does not have
        averageingful indexing informatingion. Note the index values on the other
        axes are still respected in the join.
    keys : sequence, default None
        If multiple levels passed, should contain tuples. Construct
        hierarchical index using the passed keys as the outermost level.
    levels : list of sequences, default None
        Specific levels (distinctive values) to use for constructing a
        MultiIndex. Otherwise they will be inferred from the keys.
    names : list, default None
        Names for the levels in the resulting hierarchical index.
    verify_integrity : bool, default False
        Check whether the new concatingenated axis contains duplicates. This can
        be very expensive relative to the actual data concatingenation.
    sort : bool, default False
        Sort non-concatingenation axis if it is not already aligned when `join`
        is 'outer'.
        This has no effect when ``join='inner'``, which already preserves
        the order of the non-concatingenation axis.

        .. versionchanged:: 1.0.0

           Changed to not sort by default.

    clone : bool, default True
        If False, do not clone data unnecessarily.

    Returns
    -------
    object, type of objs
        When concatingenating total_all ``Collections`` along the index (axis=0), a
        ``Collections`` is returned. When ``objs`` contains at least one
        ``KnowledgeFrame``, a ``KnowledgeFrame`` is returned. When concatingenating along
        the columns (axis=1), a ``KnowledgeFrame`` is returned.

    See Also
    --------
    Collections.adding : Concatenate Collections.
    KnowledgeFrame.adding : Concatenate KnowledgeFrames.
    KnowledgeFrame.join : Join KnowledgeFrames using indexes.
    KnowledgeFrame.unioner : Merge KnowledgeFrames by indexes or columns.

    Notes
    -----
    The keys, levels, and names arguments are total_all optional.

    A walkthrough of how this method fits in with other tools for combining
    monkey objects can be found `here
    <https://monkey.pydata.org/monkey-docs/stable/user_guide/merging.html>`__.

    
API_3: duplicated_values(self, keep: "Literal[('first', 'final_item', False)]" = 'first') -> 'np.ndarray':
        Indicate duplicate index values.

        Duplicated values are indicated as ``True`` values in the resulting
        array. Either total_all duplicates, total_all except the first, or total_all except the
        final_item occurrence of duplicates can be indicated.

        Parameters
        ----------
        keep : {'first', 'final_item', False}, default 'first'
            The value or values in a set of duplicates to mark as missing.

            - 'first' : Mark duplicates as ``True`` except for the first
              occurrence.
            - 'final_item' : Mark duplicates as ``True`` except for the final_item
              occurrence.
            - ``False`` : Mark total_all duplicates as ``True``.

        Returns
        -------
        np.ndarray[bool]

        See Also
        --------
        Collections.duplicated_values : Equivalengtht method on monkey.Collections.
        KnowledgeFrame.duplicated_values : Equivalengtht method on monkey.KnowledgeFrame.
        Index.sip_duplicates : Remove duplicate values from Index.

        

idx: 304
query: Count the number of occurrences of a value in a collections Return the count
API_1: replacing(old, new, count=-1, /):Return a clone with total_all occurrences of substring old replacingd by new.

  count
    Maximum number of occurrences to replacing.
    -1 (the default value) averages replacing total_all occurrences.

If the optional argument count is given, only the first count occurrences are
replacingd.
API_2: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_3: nbiggest(self, n=5, keep='first') -> 'Collections':
        Return the largest `n` elements.

        Parameters
        ----------
        n : int, default 5
            Return this mwhatever descending sorted values.
        keep : {'first', 'final_item', 'total_all'}, default 'first'
            When there are duplicate values that cannot total_all fit in a
            Collections of `n` elements:

            - ``first`` : return the first `n` occurrences in order
                of appearance.
            - ``final_item`` : return the final_item `n` occurrences in reverse
                order of appearance.
            - ``total_all`` : keep total_all occurrences. This can result in a Collections of
                size larger than `n`.

        Returns
        -------
        Collections
            The `n` largest values in the Collections, sorted in decreasing order.

        See Also
        --------
        Collections.nsmtotal_allest: Get the `n` smtotal_allest elements.
        Collections.sort_the_values: Sort Collections by values.
        Collections.header_num: Return the first `n` rows.

        Notes
        -----
        Faster than ``.sort_the_values(ascending=False).header_num(n)`` for smtotal_all `n`
        relative to the size of the ``Collections`` object.

        

idx: 305
query: Selecting rows where column x2 is NaN
API_1: last_tail(self: 'FrameOrCollections', n: 'int' = 5) -> 'FrameOrCollections':
        Return the final_item `n` rows.

        This function returns final_item `n` rows from the object based on
        position. It is useful for quickly verifying data, for example,
        after sorting or addinging rows.

        For negative values of `n`, this function returns total_all rows except
        the first `n` rows, equivalengtht to ``kf[n:]``.

        Parameters
        ----------
        n : int, default 5
            Number of rows to select.

        Returns
        -------
        type of ctotal_aller
            The final_item `n` rows of the ctotal_aller object.

        See Also
        --------
        KnowledgeFrame.header_num : The first `n` rows of the ctotal_aller object.

        
API_2: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_3: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0



idx: 306
query: Change all columns type of KnowledgeFrame to numeric And return the new KnowledgeFrame The code is:
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 307
query: First we concatenate two datapipes and then repeat the concatenated datapipe three times.
API_1: distinctive(self: '_IndexT', level: 'Hashable | None' = None) -> '_IndexT':
        Return distinctive values in the index.

        Unique values are returned in order of appearance, this does NOT sort.

        Parameters
        ----------
        level : int or hashable, optional
            Only return values from specified level (for MultiIndex).
            If int, gettings the level by integer position, else by level name.

        Returns
        -------
        Index

        See Also
        --------
        distinctive : Numpy array of distinctive values in that column.
        Collections.distinctive : Return distinctive values of Collections object.
        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: nbiggest(self, n=5, keep='first') -> 'Collections':
        Return the largest `n` elements.

        Parameters
        ----------
        n : int, default 5
            Return this mwhatever descending sorted values.
        keep : {'first', 'final_item', 'total_all'}, default 'first'
            When there are duplicate values that cannot total_all fit in a
            Collections of `n` elements:

            - ``first`` : return the first `n` occurrences in order
                of appearance.
            - ``final_item`` : return the final_item `n` occurrences in reverse
                order of appearance.
            - ``total_all`` : keep total_all occurrences. This can result in a Collections of
                size larger than `n`.

        Returns
        -------
        Collections
            The `n` largest values in the Collections, sorted in decreasing order.

        See Also
        --------
        Collections.nsmtotal_allest: Get the `n` smtotal_allest elements.
        Collections.sort_the_values: Sort Collections by values.
        Collections.header_num: Return the first `n` rows.

        Notes
        -----
        Faster than ``.sort_the_values(ascending=False).header_num(n)`` for smtotal_all `n`
        relative to the size of the ``Collections`` object.

        

idx: 308
query: Count the number of occurrences of a value in a collections Return the count
API_1: nbiggest(self, n=5, keep='first') -> 'Collections':
        Return the largest `n` elements.

        Parameters
        ----------
        n : int, default 5
            Return this mwhatever descending sorted values.
        keep : {'first', 'final_item', 'total_all'}, default 'first'
            When there are duplicate values that cannot total_all fit in a
            Collections of `n` elements:

            - ``first`` : return the first `n` occurrences in order
                of appearance.
            - ``final_item`` : return the final_item `n` occurrences in reverse
                order of appearance.
            - ``total_all`` : keep total_all occurrences. This can result in a Collections of
                size larger than `n`.

        Returns
        -------
        Collections
            The `n` largest values in the Collections, sorted in decreasing order.

        See Also
        --------
        Collections.nsmtotal_allest: Get the `n` smtotal_allest elements.
        Collections.sort_the_values: Sort Collections by values.
        Collections.header_num: Return the first `n` rows.

        Notes
        -----
        Faster than ``.sort_the_values(ascending=False).header_num(n)`` for smtotal_all `n`
        relative to the size of the ``Collections`` object.

        
API_2: replacing(old, new, count=-1, /):Return a clone with total_all occurrences of substring old replacingd by new.

  count
    Maximum number of occurrences to replacing.
    -1 (the default value) averages replacing total_all occurrences.

If the optional argument count is given, only the first count occurrences are
replacingd.
API_3: counts_value_num(self, normalize: 'bool' = False, sort: 'bool' = True, ascending: 'bool' = False, bins=None, sipna: 'bool' = True):
        Return a Collections containing counts of distinctive values.

        The resulting object will be in descending order so that the
        first element is the most frequently-occurring element.
        Excludes NA values by default.

        Parameters
        ----------
        normalize : bool, default False
            If True then the object returned will contain the relative
            frequencies of the distinctive values.
        sort : bool, default True
            Sort by frequencies.
        ascending : bool, default False
            Sort in ascending order.
        bins : int, optional
            Rather than count values, group them into half-open bins,
            a convenience for ``mk.cut``, only works with numeric data.
        sipna : bool, default True
            Don't include counts of NaN.

        Returns
        -------
        Collections

        See Also
        --------
        Collections.count: Number of non-NA elements in a Collections.
        KnowledgeFrame.count: Number of non-NA elements in a KnowledgeFrame.
        KnowledgeFrame.counts_value_num: Equivalengtht method on KnowledgeFrames.

        

idx: 309
query: I would like to piece a beatnum numset to obtain the i-th index in the last dimension. Is there a way I can obtain this piece for any numset without explicitly having to write the numset dimensions? There is ... or Ellipsis, which does exactly this Returns: beatnum numset
API_1: sipna(self):
        Return ExtensionArray without NA values.

        Returns
        -------
        valid : ExtensionArray
        
API_2: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 310
query: counting the number of missing/NaN in each column Get a collections with the number of missing/NaN in each column
API_1: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        
API_2: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_3: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.



idx: 311
query: Monkey create empty KnowledgeFrame with only column names Return: KnowledgeFrame
API_1: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        

idx: 312
query: I find myself often having to check whether a column or row exists in a knowledgeframe before trying to reference it. Is there any way to do this more nicely? For example on an arbitrary object I can do x = getattr(anobject, 'id', default) - is there anything similar to this in monkey? Really any way to achieve what I'm doing more gracefully? Output the second row of data in `mycol` column if it exists, otherwise output NaN
API_1: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_2: sorting_index(self, axis: 'Axis' = 0, level: 'Level | None' = None, ascending: 'bool | int | Sequence[bool | int]' = True, inplace: 'bool' = False, kind: 'str' = 'quicksort', na_position: 'str' = 'final_item', sort_remaining: 'bool' = True, ignore_index: 'bool' = False, key: 'IndexKeyFunc' = None):
        Sort object by labels (along an axis).

        Returns a new KnowledgeFrame sorted by label if `inplace` argument is
        ``False``, otherwise umkates the original KnowledgeFrame and returns None.

        Parameters
        ----------
        axis : {0 or 'index', 1 or 'columns'}, default 0
            The axis along which to sort.  The value 0 identifies the rows,
            and 1 identifies the columns.
        level : int or level name or list of ints or list of level names
            If not None, sort on values in specified index level(s).
        ascending : bool or list-like of bools, default True
            Sort ascending vs. descending. When the index is a MultiIndex the
            sort direction can be controlled for each level indivisionidutotal_ally.
        inplace : bool, default False
            If True, perform operation in-place.
        kind : {'quicksort', 'unionersort', 'heapsort', 'stable'}, default 'quicksort'
            Choice of sorting algorithm. See also :func:`numpy.sort` for more
            informatingion. `unionersort` and `stable` are the only stable algorithms. For
            KnowledgeFrames, this option is only applied when sorting on a single
            column or label.
        na_position : {'first', 'final_item'}, default 'final_item'
            Puts NaNs at the beginning if `first`; `final_item` puts NaNs at the end.
            Not implemented for MultiIndex.
        sort_remaining : bool, default True
            If True and sorting by level and index is multilevel, sort by other
            levels too (in order) after sorting by specified level.
        ignore_index : bool, default False
            If True, the resulting axis will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.0.0

        key : ctotal_allable, optional
            If not None, employ the key function to the index values
            before sorting. This is similar to the `key` argument in the
            builtin :meth:`sorted` function, with the notable difference that
            this `key` function should be *vectorized*. It should expect an
            ``Index`` and return an ``Index`` of the same shape. For MultiIndex
            inputs, the key is applied *per level*.

            .. versionadded:: 1.1.0

        Returns
        -------
        KnowledgeFrame or None
            The original KnowledgeFrame sorted by the labels or None if ``inplace=True``.

        See Also
        --------
        Collections.sorting_index : Sort Collections by the index.
        KnowledgeFrame.sort_the_values : Sort KnowledgeFrame by the value.
        Collections.sort_the_values : Sort Collections by the value.

        
API_3: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        

idx: 313
query: counting the number of missing/NaN in each column Get a collections with the number of missing/NaN in each column
API_1: total_sum(self, axis=None, skipna=None, level=None, numeric_only=None, getting_min_count=0, **kwargs):
Return the total_sum of the values over the requested axis.

This is equivalengtht to the method ``numpy.total_sum``.

Parameters
----------
axis : {index (0)}
    Axis for the function to be applied on.
skipna : bool, default True
    Exclude NA/null values when computing the result.
level : int or level name, default None
    If the axis is a MultiIndex (hierarchical), count along a
    particular level, collapsing into a scalar.
numeric_only : bool, default None
    Include only float, int, boolean columns. If None, will attempt to use
    everything, then use only numeric data. Not implemented for Collections.
getting_min_count : int, default 0
    The required number of valid values to perform the operation. If fewer than
    ``getting_min_count`` non-NA values are present the result will be NA.
**kwargs
    Additional keyword arguments to be passed to the function.

Returns
-------
scalar or Collections (if level specified)

See Also
--------
Collections.total_sum : Return the total_sum.
Collections.getting_min : Return the getting_minimum.
Collections.getting_max : Return the getting_maximum.
Collections.idxgetting_min : Return the index of the getting_minimum.
Collections.idxgetting_max : Return the index of the getting_maximum.
KnowledgeFrame.total_sum : Return the total_sum over the requested axis.
KnowledgeFrame.getting_min : Return the getting_minimum over the requested axis.
KnowledgeFrame.getting_max : Return the getting_maximum over the requested axis.
KnowledgeFrame.idxgetting_min : Return the index of the getting_minimum over the requested axis.
KnowledgeFrame.idxgetting_max : Return the index of the getting_maximum over the requested axis.


API_2: ifnull(self) -> 'np.ndarray':
        Detect missing values.

        Return a boolean same-sized object indicating if the values are NA.
        NA values, such as ``None``, :attr:`numpy.NaN` or :attr:`mk.NaT`, getting
        mappingped to ``True`` values.
        Everything else getting mappingped to ``False`` values. Characters such as
        empty strings `''` or :attr:`numpy.inf` are not considered NA values
        (unless you set ``monkey.options.mode.use_inf_as_na = True``).

        Returns
        -------
        numpy.ndarray[bool]
            A boolean array of whether my values are NA.

        See Also
        --------
        Index.notna : Boolean inverse of ifna.
        Index.sipna : Omit entries with missing values.
        ifna : Top-level ifna.
        Collections.ifna : Detect missing values in Collections object.

        
API_3: convert_string(self, buf: 'FilePathOrBuffer[str] | None' = None, columns: 'Sequence[str] | None' = None, col_space: 'int | None' = None, header_numer: 'bool | Sequence[str]' = True, index: 'bool' = True, na_rep: 'str' = 'NaN', formatingters: 'fmt.FormattersType | None' = None, float_formating: 'fmt.FloatFormatType | None' = None, sparsify: 'bool | None' = None, index_names: 'bool' = True, justify: 'str | None' = None, getting_max_rows: 'int | None' = None, getting_min_rows: 'int | None' = None, getting_max_cols: 'int | None' = None, show_dimensions: 'bool' = False, decimal: 'str' = '.', line_width: 'int | None' = None, getting_max_colwidth: 'int | None' = None, encoding: 'str | None' = None) -> 'str | None':
        Render a KnowledgeFrame to a console-friendly tabular output.
        
        Parameters
        ----------
        buf : str, Path or StringIO-like, optional, default None
            Buffer to write to. If None, the output is returned as a string.
        columns : sequence, optional, default None
            The subset of columns to write. Writes total_all columns by default.
        col_space : int, list or dict of int, optional
            The getting_minimum width of each column.
        header_numer : bool or sequence, optional
            Write out the column names. If a list of strings is given, it is astotal_sumed to be aliases for the column names.
        index : bool, optional, default True
            Whether to print index (row) labels.
        na_rep : str, optional, default 'NaN'
            String representation of ``NaN`` to use.
        formatingters : list, tuple or dict of one-param. functions, optional
            Formatter functions to employ to columns' elements by position or
            name.
            The result of each function must be a unicode string.
            List/tuple must be of lengthgth equal to the number of columns.
        float_formating : one-parameter function, optional, default None
            Formatter function to employ to columns' elements if they are
            floats. This function must return a unicode string and will be
            applied only to the non-``NaN`` elements, with ``NaN`` being
            handled by ``na_rep``.

            .. versionchanged:: 1.2.0

        sparsify : bool, optional, default True
            Set to False for a KnowledgeFrame with a hierarchical index to print
            every multiindex key at each row.
        index_names : bool, optional, default True
            Prints the names of the indexes.
        justify : str, default None
            How to justify the column labels. If None uses the option from
            the print configuration (controlled by set_option), 'right' out
            of the box. Valid values are

            * left
            * right
            * center
            * justify
            * justify-total_all
            * start
            * end
            * inherit
            * match-parent
            * initial
            * unset.
        getting_max_rows : int, optional
            Maximum number of rows to display in the console.
        getting_min_rows : int, optional
            The number of rows to display in the console in a truncated repr
            (when number of rows is above `getting_max_rows`).
        getting_max_cols : int, optional
            Maximum number of columns to display in the console.
        show_dimensions : bool, default False
            Display KnowledgeFrame dimensions (number of rows by number of columns).
        decimal : str, default '.'
            Character recognized as decimal separator, e.g. ',' in Europe.
    
        line_width : int, optional
            Width to wrap a line in characters.
        getting_max_colwidth : int, optional
            Max width to truncate each column in characters. By default, no limit.

            .. versionadded:: 1.0.0
        encoding : str, default "utf-8"
            Set character encoding.

            .. versionadded:: 1.0
        
        Returns
        -------
        str or None
            If buf is None, returns the result as a string. Otherwise returns
            None.
    
        See Also
        --------
        to_html : Convert KnowledgeFrame to HTML.

        

idx: 314
query: How do I change the column labels of kf？ And return the knowledgeframe that has been renamed
API_1: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_2: renaming(self, name, inplace=False):
        Alter Index or MultiIndex name.

        Able to set new names without level. Defaults to returning new index.
        Length of names must match number of levels in MultiIndex.

        Parameters
        ----------
        name : label or list of labels
            Name(s) to set.
        inplace : bool, default False
            Modifies the object directly, instead of creating a new Index or
            MultiIndex.

        Returns
        -------
        Index or None
            The same type as the ctotal_aller or None if ``inplace=True``.

        See Also
        --------
        Index.set_names : Able to set new names partitotal_ally and by level.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 315
query: Set value to an entire column `B` of a monkey knowledgeframe Return the changed knowledgeframe.
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: allocate(self, **kwargs) -> 'KnowledgeFrame':
        Assign new columns to a KnowledgeFrame.

        Returns a new object with total_all original columns in addition to new ones.
        Existing columns that are re-total_allocateed will be overwritten.

        Parameters
        ----------
        **kwargs : dict of {str: ctotal_allable or Collections}
            The column names are keywords. If the values are
            ctotal_allable, they are computed on the KnowledgeFrame and
            total_allocateed to the new columns. The ctotal_allable must not
            change input KnowledgeFrame (though monkey doesn't check it).
            If the values are not ctotal_allable, (e.g. a Collections, scalar, or array),
            they are simply total_allocateed.

        Returns
        -------
        KnowledgeFrame
            A new KnowledgeFrame with the new columns in addition to
            total_all the existing columns.

        Notes
        -----
        Assigning multiple columns within the same ``total_allocate`` is possible.
        Later items in '\*\*kwargs' may refer to newly created or modified
        columns in 'kf'; items are computed and total_allocateed into 'kf' in order.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

idx: 316
query: i want to sip 2 rows in the knowledgeframe if zero comes in the column if 0 comes on odd index sip previous row as well as current row using monkey Assuming your knowledgeframe is indexed starting from 0 Rows with column2 = 0 and on odd index The rows above them A new knowledgeframe with those rows removed
API_1: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.


API_2: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        
API_3: traversal(self) -> 'Iterable[tuple[Hashable, Collections]]':
        Iterate over KnowledgeFrame rows as (index, Collections) pairs.

        Yields
        ------
        index : label or tuple of label
            The index of the row. A tuple for a `MultiIndex`.
        data : Collections
            The data of the row as a Collections.

        See Also
        --------
        KnowledgeFrame.itertuples : Iterate over KnowledgeFrame rows as namedtuples of the values.
        KnowledgeFrame.items : Iterate over (column name, Collections) pairs.

        Notes
        -----
        1. Because ``traversal`` returns a Collections for each row,
           it does **not** preserve dtypes across the rows (dtypes are
           preserved across columns for KnowledgeFrames). For example,

           >>> kf = mk.KnowledgeFrame([[1, 1.5]], columns=['int', 'float'])
           >>> row = next(kf.traversal())[1]
           >>> row
           int      1.0
           float    1.5
           Name: 0, dtype: float64
           >>> print(row['int'].dtype)
           float64
           >>> print(kf['int'].dtype)
           int64

           To preserve dtypes while iterating over the rows, it is better
           to use :meth:`itertuples` which returns namedtuples of the values
           and which is genertotal_ally faster than ``traversal``.

        2. You should **never modify** something you are iterating over.
           This is not guaranteed to work in total_all cases. Depending on the
           data types, the iterator returns a clone and not a view, and writing
           to it will have no effect.
        

idx: 317
query: How can I change the column labels of a Monkey KnowledgeFrame using kf.convert_dict()? Please return the renamed knowledgeframe.
API_1: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_2: KnowledgeFrame(data=None, index: 'Axes | None' = None, columns: 'Axes | None' = None, dtype: 'Dtype | None' = None, clone: 'bool | None' = None):
    Two-dimensional, size-mutable, potentitotal_ally heterogeneous tabular data.

    Data structure also contains labeled axes (rows and columns).
    Arithmetic operations align on both row and column labels. Can be
    thought of as a dict-like container for Collections objects. The primary
    monkey data structure.

    Parameters
    ----------
    data : ndarray (structured or homogeneous), Iterable, dict, or KnowledgeFrame
        Dict can contain Collections, arrays, constants, dataclass or list-like objects. If
        data is a dict, column order follows insertion-order.

        .. versionchanged:: 0.25.0
           If data is a list of dicts, column order follows insertion-order.

    index : Index or array-like
        Index to use for resulting frame. Will default to RangeIndex if
        no indexing informatingion part of input data and no index provided.
    columns : Index or array-like
        Column labels to use for resulting frame when data does not have them,
        defaulting to RangeIndex(0, 1, 2, ..., n). If data contains column labels,
        will perform column selection instead.
    dtype : dtype, default None
        Data type to force. Only a single dtype is total_allowed. If None, infer.
    clone : bool or None, default None
        Copy data from inputs.
        For dict data, the default of None behaves like ``clone=True``.  For KnowledgeFrame
        or 2d ndarray input, the default of None behaves like ``clone=False``.

        .. versionchanged:: 1.3.0

    See Also
    --------
    KnowledgeFrame.from_records : Constructor from tuples, also record arrays.
    KnowledgeFrame.from_dict : From dicts of Collections, arrays, or dicts.
    read_csv : Read a comma-separated values (csv) file into KnowledgeFrame.
    read_table : Read general delimited file into KnowledgeFrame.
    read_clipboard : Read text from clipboard into KnowledgeFrame.

    
API_3: choose_dtypes(self, include=None, exclude=None) -> 'KnowledgeFrame':
        Return a subset of the KnowledgeFrame's columns based on the column dtypes.

        Parameters
        ----------
        include, exclude : scalar or list-like
            A selection of dtypes or strings to be included/excluded. At least
            one of these parameters must be supplied.

        Returns
        -------
        KnowledgeFrame
            The subset of the frame including the dtypes in ``include`` and
            excluding the dtypes in ``exclude``.

        Raises
        ------
        ValueError
            * If both of ``include`` and ``exclude`` are empty
            * If ``include`` and ``exclude`` have overlapping elements
            * If whatever kind of string dtype is passed in.

        See Also
        --------
        KnowledgeFrame.dtypes: Return Collections with the data type of each column.

        Notes
        -----
        * To select total_all *numeric* types, use ``np.number`` or ``'number'``
        * To select strings you must use the ``object`` dtype, but note that
          this will return *total_all* object dtype columns
        * See the `numpy dtype hierarchy
          <https://numpy.org/doc/stable/reference/arrays.scalars.html>`__
        * To select datetimes, use ``np.datetime64``, ``'datetime'`` or
          ``'datetime64'``
        * To select timedeltas, use ``np.timedelta64``, ``'timedelta'`` or
          ``'timedelta64'``
        * To select Monkey categorical dtypes, use ``'category'``
        * To select Monkey datetimetz dtypes, use ``'datetimetz'`` (new in
          0.20.0) or ``'datetime64[ns, tz]'``

        

idx: 318
query: I need a logic to extract the last year (YY) from a fiscal date string in the format of YYYY-YY. For example, if the date string is '1999-00', the last year would be 2000. If it is the end of the century, I want to add the first two digits to the last year. The column_name parameter represents the column name in the knowledgeframe that contains the date strings. I want to return the numerical Collections object of the last year. Additionally, I want to add leading zeros to the strings in the col_name column of the Monkey Dataframe. The maximum length of the string is 15. Finally, I want to return the knowledgeframe.
API_1: to_num(arg, errors='raise', downcast=None):
    Convert argument to a numeric type.

    The default return dtype is `float64` or `int64`
    depending on the data supplied. Use the `downcast` parameter
    to obtain other dtypes.

    Please note that precision loss may occur if retotal_ally large numbers
    are passed in. Due to the internal limitations of `ndarray`, if
    numbers smtotal_aller than `-9223372036854775808` (np.iinfo(np.int64).getting_min)
    or larger than `18446744073709551615` (np.iinfo(np.uint64).getting_max) are
    passed in, it is very likely they will be converted to float so that
    they can stored in an `ndarray`. These warnings employ similarly to
    `Collections` since it interntotal_ally leverages `ndarray`.

    Parameters
    ----------
    arg : scalar, list, tuple, 1-d array, or Collections
        Argument to be converted.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaN.
        - If 'ignore', then invalid parsing will return the input.
    downcast : {'integer', 'signed', 'unsigned', 'float'}, default None
        If not None, and if the data has been successfully cast to a
        numerical dtype (or if the data was numeric to begin with),
        downcast that resulting data to the smtotal_allest numerical dtype
        possible according to the following rules:

        - 'integer' or 'signed': smtotal_allest signed int dtype (getting_min.: np.int8)
        - 'unsigned': smtotal_allest unsigned int dtype (getting_min.: np.uint8)
        - 'float': smtotal_allest float dtype (getting_min.: np.float32)

        As this behaviour is separate from the core conversion to
        numeric values, whatever errors raised during the downcasting
        will be surfaced regardless of the value of the 'errors' input.

        In addition, downcasting will only occur if the size
        of the resulting data's dtype is strictly larger than
        the dtype it is to be cast to, so if none of the dtypes
        checked satisfy that specification, no downcasting will be
        performed on the data.

    Returns
    -------
    ret
        Numeric if parsing succeeded.
        Return type depends on input.  Collections if Collections, otherwise ndarray.

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    convert_datetime : Convert argument to datetime.
    to_timedelta : Convert argument to timedelta.
    numpy.ndarray.totype : Cast a numpy array to a specified type.
    KnowledgeFrame.convert_dtypes : Convert dtypes.

    
API_2: convert_dict(self, into=<class 'dict'>):
        Convert Collections to {label -> value} dict or dict-like object.

        Parameters
        ----------
        into : class, default dict
            The collections.abc.Mapping subclass to use as the return
            object. Can be the actual class or an empty
            instance of the mappingping type you want.  If you want a
            collections.defaultdict, you must pass it initialized.

        Returns
        -------
        collections.abc.Mapping
            Key-value representation of Collections.

        
API_3: convert_datetime(arg: 'DatetimeScalarOrArrayConvertible', errors: 'str' = 'raise', dayfirst: 'bool' = False, yearfirst: 'bool' = False, utc: 'bool | None' = None, formating: 'str | None' = None, exact: 'bool' = True, unit: 'str | None' = None, infer_datetime_formating: 'bool' = False, origin='unix', cache: 'bool' = True) -> 'DatetimeIndex | Collections | DatetimeScalar | NaTType | None':
    Convert argument to datetime.

    Parameters
    ----------
    arg : int, float, str, datetime, list, tuple, 1-d array, Collections, KnowledgeFrame/dict-like
        The object to convert to a datetime.
    errors : {'ignore', 'raise', 'coerce'}, default 'raise'
        - If 'raise', then invalid parsing will raise an exception.
        - If 'coerce', then invalid parsing will be set as NaT.
        - If 'ignore', then invalid parsing will return the input.
    dayfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.
        If True, parses dates with the day first, eg 10/11/12 is parsed as
        2012-11-10.
        Warning: dayfirst=True is not strict, but will prefer to parse
        with day first (this is a known bug, based on dateutil behavior).
    yearfirst : bool, default False
        Specify a date parse order if `arg` is str or its list-likes.

        - If True parses dates with the year first, eg 10/11/12 is parsed as
          2010-11-12.
        - If both dayfirst and yearfirst are True, yearfirst is preceded (same
          as dateutil).

        Warning: yearfirst=True is not strict, but will prefer to parse
        with year first (this is a known bug, based on dateutil behavior).
    utc : bool, default None
        Return UTC DatetimeIndex if True (converting whatever tz-aware
        datetime.datetime objects as well).
    formating : str, default None
        The strftime to parse time, eg "%d/%m/%Y", note that "%f" will parse
        total_all the way up to nanoseconds.
        See strftime documentation for more informatingion on choices:
        https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior.
    exact : bool, True by default
        Behaves as:
        - If True, require an exact formating match.
        - If False, total_allow the formating to match whateverwhere in the targetting string.

    unit : str, default 'ns'
        The unit of the arg (D,s,ms,us,ns) denote the unit, which is an
        integer or float number. This will be based off the origin.
        Example, with unit='ms' and origin='unix' (the default), this
        would calculate the number of milliseconds to the unix epoch start.
    infer_datetime_formating : bool, default False
        If True and no `formating` is given, attempt to infer the formating of the
        datetime strings based on the first non-NaN element,
        and if it can be inferred, switch to a faster method of parsing them.
        In some cases this can increase the parsing speed by ~5-10x.
    origin : scalar, default 'unix'
        Define the reference date. The numeric values would be parsed as number
        of units (defined by `unit`) since this reference date.

        - If 'unix' (or POSIX) time; origin is set to 1970-01-01.
        - If 'julian', unit must be 'D', and origin is set to beginning of
          Julian Calengthdar. Julian day number 0 is total_allocateed to the day starting
          at noon on January 1, 4713 BC.
        - If Timestamp convertible, origin is set to Timestamp identified by
          origin.
    cache : bool, default True
        If True, use a cache of distinctive, converted dates to employ the datetime
        conversion. May produce significant speed-up when parsing duplicate
        date strings, especitotal_ally ones with timezone offsets. The cache is only
        used when there are at least 50 values. The presence of out-of-bounds
        values will render the cache unusable and may slow down parsing.

        .. versionchanged:: 0.25.0
            - changed default value from False to True.

    Returns
    -------
    datetime
        If parsing succeeded.
        Return type depends on input:

        - list-like: DatetimeIndex
        - Collections: Collections of datetime64 dtype
        - scalar: Timestamp

        In case when it is not possible to return designated types (e.g. when
        whatever element of input is before Timestamp.getting_min or after Timestamp.getting_max)
        return will have datetime.datetime type (or corresponding
        array/Collections).

    See Also
    --------
    KnowledgeFrame.totype : Cast argument to a specified dtype.
    to_timedelta : Convert argument to timedelta.
    convert_dtypes : Convert dtypes.

    

idx: 319
query: How to get the first largest value in column a？ Using nbiggest and iloc to implemente this
API_1: distinctive(self: '_IndexT', level: 'Hashable | None' = None) -> '_IndexT':
        Return distinctive values in the index.

        Unique values are returned in order of appearance, this does NOT sort.

        Parameters
        ----------
        level : int or hashable, optional
            Only return values from specified level (for MultiIndex).
            If int, gettings the level by integer position, else by level name.

        Returns
        -------
        Index

        See Also
        --------
        distinctive : Numpy array of distinctive values in that column.
        Collections.distinctive : Return distinctive values of Collections object.
        
API_2: unioner(self, right: 'FrameOrCollectionsUnion', how: 'str' = 'inner', on: 'IndexLabel | None' = None, left_on: 'IndexLabel | None' = None, right_on: 'IndexLabel | None' = None, left_index: 'bool' = False, right_index: 'bool' = False, sort: 'bool' = False, suffixes: 'Suffixes' = ('_x', '_y'), clone: 'bool' = True, indicator: 'bool' = False, validate: 'str | None' = None) -> 'KnowledgeFrame':
Merge KnowledgeFrame or named Collections objects with a database-style join.

A named Collections object is treated as a KnowledgeFrame with a single named column.

The join is done on columns or indexes. If joining columns on
columns, the KnowledgeFrame indexes *will be ignored*. Otherwise if joining indexes
on indexes or indexes on a column or columns, the index will be passed on.
When perforgetting_ming a cross unioner, no column specifications to unioner on are
total_allowed.

Parameters
----------
right : KnowledgeFrame or named Collections
    Object to unioner with.
how : {'left', 'right', 'outer', 'inner', 'cross'}, default 'inner'
    Type of unioner to be performed.

    * left: use only keys from left frame, similar to a SQL left outer join;
      preserve key order.
    * right: use only keys from right frame, similar to a SQL right outer join;
      preserve key order.
    * outer: use union of keys from both frames, similar to a SQL full outer
      join; sort keys lexicographictotal_ally.
    * inner: use interst of keys from both frames, similar to a SQL inner
      join; preserve the order of the left keys.
    * cross: creates the cartesian product from both frames, preserves the order
      of the left keys.

      .. versionadded:: 1.2.0

on : label or list
    Column or index level names to join on. These must be found in both
    KnowledgeFrames. If `on` is None and not merging on indexes then this defaults
    to the interst of the columns in both KnowledgeFrames.
left_on : label or list, or array-like
    Column or index level names to join on in the left KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the left KnowledgeFrame.
    These arrays are treated as if they are columns.
right_on : label or list, or array-like
    Column or index level names to join on in the right KnowledgeFrame. Can also
    be an array or list of arrays of the lengthgth of the right KnowledgeFrame.
    These arrays are treated as if they are columns.
left_index : bool, default False
    Use the index from the left KnowledgeFrame as the join key(s). If it is a
    MultiIndex, the number of keys in the other KnowledgeFrame (either the index
    or a number of columns) must match the number of levels.
right_index : bool, default False
    Use the index from the right KnowledgeFrame as the join key. Same caveats as
    left_index.
sort : bool, default False
    Sort the join keys lexicographictotal_ally in the result KnowledgeFrame. If False,
    the order of the join keys depends on the join type (how keyword).
suffixes : list-like, default is ("_x", "_y")
    A lengthgth-2 sequence where each element is optiontotal_ally a string
    indicating the suffix to add to overlapping column names in
    `left` and `right` respectively. Pass a value of `None` instead
    of a string to indicate that the column name from `left` or
    `right` should be left as-is, with no suffix. At least one of the
    values must not be None.
clone : bool, default True
    If False, avoid clone if possible.
indicator : bool or str, default False
    If True, adds a column to the output KnowledgeFrame ctotal_alled "_unioner" with
    informatingion on the source of each row. The column can be given a different
    name by providing a string argument. The column will have a Categorical
    type with the value of "left_only" for observations whose unioner key only
    appears in the left KnowledgeFrame, "right_only" for observations
    whose unioner key only appears in the right KnowledgeFrame, and "both"
    if the observation's unioner key is found in both KnowledgeFrames.

validate : str, optional
    If specified, checks if unioner is of specified type.

    * "one_to_one" or "1:1": check if unioner keys are distinctive in both
      left and right datasets.
    * "one_to_mwhatever" or "1:m": check if unioner keys are distinctive in left
      dataset.
    * "mwhatever_to_one" or "m:1": check if unioner keys are distinctive in right
      dataset.
    * "mwhatever_to_mwhatever" or "m:m": total_allowed, but does not result in checks.

Returns
-------
KnowledgeFrame
    A KnowledgeFrame of the two unionerd objects.

See Also
--------
unioner_ordered : Merge with optional filling/interpolation.
unioner_asof : Merge on nearest keys.
KnowledgeFrame.join : Similar method using indices.

Notes
-----
Support for specifying index levels as the `on`, `left_on`, and
`right_on` parameters was added in version 0.23.0
Support for merging named Collections objects was added in version 0.24.0


API_3: nbiggest(self, n=5, keep='first') -> 'Collections':
        Return the largest `n` elements.

        Parameters
        ----------
        n : int, default 5
            Return this mwhatever descending sorted values.
        keep : {'first', 'final_item', 'total_all'}, default 'first'
            When there are duplicate values that cannot total_all fit in a
            Collections of `n` elements:

            - ``first`` : return the first `n` occurrences in order
                of appearance.
            - ``final_item`` : return the final_item `n` occurrences in reverse
                order of appearance.
            - ``total_all`` : keep total_all occurrences. This can result in a Collections of
                size larger than `n`.

        Returns
        -------
        Collections
            The `n` largest values in the Collections, sorted in decreasing order.

        See Also
        --------
        Collections.nsmtotal_allest: Get the `n` smtotal_allest elements.
        Collections.sort_the_values: Sort Collections by values.
        Collections.header_num: Return the first `n` rows.

        Notes
        -----
        Faster than ``.sort_the_values(ascending=False).header_num(n)`` for smtotal_all `n`
        relative to the size of the ``Collections`` object.

        

idx: 320
query: Batch on data pipe `dp1` of size 4 and discard the last batch if they are not filled, and then obtain the first two batches. Then the above result is concatenated with the datapipe `dp2`.
API_1: employ(self, func: 'AggFuncType', axis: 'Axis' = 0, raw: 'bool' = False, result_type=None, args=(), **kwargs):
        Apply a function along an axis of the KnowledgeFrame.

        Objects passed to the function are Collections objects whose index is
        either the KnowledgeFrame's index (``axis=0``) or the KnowledgeFrame's columns
        (``axis=1``). By default (``result_type=None``), the final return type
        is inferred from the return type of the applied function. Otherwise,
        it depends on the `result_type` argument.

        Parameters
        ----------
        func : function
            Function to employ to each column or row.
        axis : {0 or 'index', 1 or 'columns'}, default 0
            Axis along which the function is applied:

            * 0 or 'index': employ function to each column.
            * 1 or 'columns': employ function to each row.

        raw : bool, default False
            Detergetting_mines if row or column is passed as a Collections or ndarray object:

            * ``False`` : passes each row or column as a Collections to the
              function.
            * ``True`` : the passed function will receive ndarray objects
              instead.
              If you are just employing a NumPy reduction function this will
              achieve much better performance.

        result_type : {'expand', 'reduce', 'broadcast', None}, default None
            These only act when ``axis=1`` (columns):

            * 'expand' : list-like results will be turned into columns.
            * 'reduce' : returns a Collections if possible rather than expanding
              list-like results. This is the opposite of 'expand'.
            * 'broadcast' : results will be broadcast to the original shape
              of the KnowledgeFrame, the original index and columns will be
              retained.

            The default behaviour (None) depends on the return value of the
            applied function: list-like results will be returned as a Collections
            of those. However if the employ function returns a Collections these
            are expanded to columns.
        args : tuple
            Positional arguments to pass to `func` in addition to the
            array/collections.
        **kwargs
            Additional keyword arguments to pass as keywords arguments to
            `func`.

        Returns
        -------
        Collections or KnowledgeFrame
            Result of employing ``func`` along the given axis of the
            KnowledgeFrame.

        See Also
        --------
        KnowledgeFrame.employmapping: For elementwise operations.
        KnowledgeFrame.aggregate: Only perform aggregating type operations.
        KnowledgeFrame.transform: Only perform transforgetting_ming type operations.

        Notes
        -----
        Functions that mutate the passed object can produce unexpected
        behavior or errors and are not supported. See :ref:`gotchas.ukf-mutation`
        for more definal_item_tails.

        
API_2: sample_by_num(self: 'FrameOrCollections', n=None, frac: 'float | None' = None, replacing: 'bool_t' = False, weights=None, random_state=None, axis: 'Axis | None' = None, ignore_index: 'bool_t' = False) -> 'FrameOrCollections':
        Return a random sample_by_num of items from an axis of object.

        You can use `random_state` for reproducibility.

        Parameters
        ----------
        n : int, optional
            Number of items from axis to return. Cannot be used with `frac`.
            Default = 1 if `frac` = None.
        frac : float, optional
            Fraction of axis items to return. Cannot be used with `n`.
        replacing : bool, default False
            Allow or distotal_allow sampling of the same row more than once.
        weights : str or ndarray-like, optional
            Default 'None' results in equal probability weighting.
            If passed a Collections, will align with targetting object on index. Index
            values in weights not found in sample_by_numd object will be ignored and
            index values in sample_by_numd object not in weights will be total_allocateed
            weights of zero.
            If ctotal_alled on a KnowledgeFrame, will accept the name of a column
            when axis = 0.
            Unless weights are a Collections, weights must be same lengthgth as axis
            being sample_by_numd.
            If weights do not total_sum to 1, they will be normalized to total_sum to 1.
            Missing values in the weights column will be treated as zero.
            Infinite values not total_allowed.
        random_state : int, array-like, BitGenerator, np.random.RandomState, optional
            If int, array-like, or BitGenerator (NumPy>=1.17), seed for
            random number generator
            If np.random.RandomState, use as numpy RandomState object.

            .. versionchanged:: 1.1.0

                array-like and BitGenerator (for NumPy>=1.17) object now passed to
                np.random.RandomState() as seed

        axis : {0 or ‘index’, 1 or ‘columns’, None}, default None
            Axis to sample_by_num. Accepts axis number or name. Default is stat axis
            for given data type (0 for Collections and KnowledgeFrames).
        ignore_index : bool, default False
            If True, the resulting index will be labeled 0, 1, …, n - 1.

            .. versionadded:: 1.3.0

        Returns
        -------
        Collections or KnowledgeFrame
            A new object of same type as ctotal_aller containing `n` items randomly
            sample_by_numd from the ctotal_aller object.

        See Also
        --------
        KnowledgeFrameGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            KnowledgeFrame object.
        CollectionsGroupBy.sample_by_num: Generates random sample_by_nums from each group of a
            Collections object.
        numpy.random.choice: Generates a random sample_by_num from a given 1-D numpy
            array.

        Notes
        -----
        If `frac` > 1, `replacingment` should be set to `True`.

        
API_3: grouper(self, by=None, axis: 'Axis' = 0, level: 'Level | None' = None, as_index: 'bool' = True, sort: 'bool' = True, group_keys: 'bool' = True, squeeze: 'bool | lib.NoDefault' = <no_default>, observed: 'bool' = False, sipna: 'bool' = True) -> 'KnowledgeFrameGroupBy':
Group KnowledgeFrame using a mappingper or by a Collections of columns.

A grouper operation involves some combination of splitting the
object, employing a function, and combining the results. This can be
used to group large amounts of data and compute operations on these
groups.

Parameters
----------
by : mappingping, function, label, or list of labels
    Used to detergetting_mine the groups for the grouper.
    If ``by`` is a function, it's ctotal_alled on each value of the object's
    index. If a dict or Collections is passed, the Collections or dict VALUES
    will be used to detergetting_mine the groups (the Collections' values are first
    aligned; see ``.align()`` method). If an ndarray is passed, the
    values are used as-is to detergetting_mine the groups. A label or list of
    labels may be passed to group by the columns in ``self``. Notice
    that a tuple is interpreted as a (single) key.
axis : {0 or 'index', 1 or 'columns'}, default 0
    Split along rows (0) or columns (1).
level : int, level name, or sequence of such, default None
    If the axis is a MultiIndex (hierarchical), group by a particular
    level or levels.
as_index : bool, default True
    For aggregated output, return object with group labels as the
    index. Only relevant for KnowledgeFrame input. as_index=False is
    effectively "SQL-style" grouped output.
sort : bool, default True
    Sort group keys. Get better performance by turning this off.
    Note this does not influence the order of observations within each
    group. Groupby preserves the order of rows within each group.
group_keys : bool, default True
    When ctotal_alling employ, add group keys to index to identify pieces.
squeeze : bool, default False
    Reduce the dimensionality of the return type if possible,
    otherwise return a consistent type.

    .. deprecated:: 1.1.0

observed : bool, default False
    This only applies if whatever of the groupers are Categoricals.
    If True: only show observed values for categorical groupers.
    If False: show total_all values for categorical groupers.
sipna : bool, default True
    If True, and if group keys contain NA values, NA values togettingher
    with row/column will be sipped.
    If False, NA values will also be treated as the key in groups

    .. versionadded:: 1.1.0

Returns
-------
KnowledgeFrameGroupBy
    Returns a grouper object that contains informatingion about the groups.

See Also
--------
resample_by_num : Convenience method for frequency conversion and resampling
    of time collections.

Notes
-----
See the `user guide
<https://monkey.pydata.org/monkey-docs/stable/grouper.html>`__ for more.



idx: 321
query: What is the inverse of the beatnum cumsum function?
API_1: division(self, other, axis='columns', level=None, fill_value=None):
Get Floating divisionision of knowledgeframe and other, element-wise (binary operator `truedivision`).

Equivalengtht to ``knowledgeframe / other``, but with support to substitute a fill_value
for missing data in one of the inputs. With reverse version, `rtruedivision`.

Among flexible wrappers (`add`, `sub`, `mul`, `division`, `mod`, `pow`) to
arithmetic operators: `+`, `-`, `*`, `/`, `//`, `%`, `**`.

Parameters
----------
other : scalar, sequence, Collections, or KnowledgeFrame
    Any single or multiple element data structure, or list-like object.
axis : {0 or 'index', 1 or 'columns'}
    Whether to compare by the index (0 or 'index') or columns
    (1 or 'columns'). For Collections input, axis to match Collections index on.
level : int or label
    Broadcast across a level, matching Index values on the
    passed MultiIndex level.
fill_value : float or None, default None
    Fill existing missing (NaN) values, and whatever new element needed for
    successful KnowledgeFrame alignment, with this value before computation.
    If data in both corresponding KnowledgeFrame locations is missing
    the result will be missing.

Returns
-------
KnowledgeFrame
    Result of the arithmetic operation.

See Also
--------
KnowledgeFrame.add : Add KnowledgeFrames.
KnowledgeFrame.sub : Subtract KnowledgeFrames.
KnowledgeFrame.mul : Multiply KnowledgeFrames.
KnowledgeFrame.division : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.truedivision : Divide KnowledgeFrames (float divisionision).
KnowledgeFrame.floordivision : Divide KnowledgeFrames (integer divisionision).
KnowledgeFrame.mod : Calculate modulo (remainder after divisionision).
KnowledgeFrame.pow : Calculate exponential power.

Notes
-----
Mismatched indices will be unioned togettingher.


API_2: shifting(self, periods=1, freq=None):
        Shift index by desired number of time frequency increments.

        This method is for shiftinging the values of datetime-like indexes
        by a specified time increment a given number of times.

        Parameters
        ----------
        periods : int, default 1
            Number of periods (or increments) to shifting by,
            can be positive or negative.
        freq : monkey.DateOffset, monkey.Timedelta or str, optional
            Frequency increment to shifting by.
            If None, the index is shiftinged by its own `freq` attribute.
            Offset aliases are valid strings, e.g., 'D', 'W', 'M' etc.

        Returns
        -------
        monkey.Index
            Shifted index.

        See Also
        --------
        Collections.shifting : Shift values of Collections.

        Notes
        -----
        This method is only implemented for datetime-like index classes,
        i.e., DatetimeIndex, PeriodIndex and TimedeltaIndex.

        
API_3: reseting_index(self, level: 'Hashable | Sequence[Hashable] | None' = None, sip: 'bool' = False, inplace: 'bool' = False, col_level: 'Hashable' = 0, col_fill: 'Hashable' = '') -> 'KnowledgeFrame | None':
        Reset the index, or a level of it.

        Reset the index of the KnowledgeFrame, and use the default one instead.
        If the KnowledgeFrame has a MultiIndex, this method can remove one or more
        levels.

        Parameters
        ----------
        level : int, str, tuple, or list, default None
            Only remove the given levels from the index. Removes total_all levels by
            default.
        sip : bool, default False
            Do not try to insert index into knowledgeframe columns. This resets
            the index to the default integer index.
        inplace : bool, default False
            Modify the KnowledgeFrame in place (do not create a new object).
        col_level : int or str, default 0
            If the columns have multiple levels, detergetting_mines which level the
            labels are inserted into. By default it is inserted into the first
            level.
        col_fill : object, default ''
            If the columns have multiple levels, detergetting_mines how the other
            levels are named. If None then the index name is repeated.

        Returns
        -------
        KnowledgeFrame or None
            KnowledgeFrame with the new index or None if ``inplace=True``.

        See Also
        --------
        KnowledgeFrame.set_index : Opposite of reseting_index.
        KnowledgeFrame.reindexing : Change to new indices or expand indices.
        KnowledgeFrame.reindexing_like : Change to same indices as other KnowledgeFrame.

        

